# CLAUDE.md — MK Timekeeping & Compliance POC

FastAPI + SQLAlchemy + Jinja2 internal tool replacing three legacy spreadsheets for
time/leave/compliance tracking (~45 staff, originally all India-based offshore, now also
US-based). Spec: docs/PRD.md. Onboarding: HANDOFF.md.

## Commands

```bash
.venv/bin/python -m uvicorn app.main:app --port 8127     # run (http://localhost:8127, dev pick-a-user auth)
.venv/bin/python -m pytest tests/ -q                     # 382 tests — must stay green
.venv/bin/python -m legacy.verify_strikes                # acceptance: MUST print 168/168
rm tms.db && .venv/bin/python -m legacy.import_legacy    # rebuild DB from the 3 legacy .ods files
```

## Hard rules

- **The project directory name ends with a trailing space** — always quote absolute paths.
- Python 3.9 (system). No `X | Y` unions in runtime annotations; use `Optional[...]`.
- All durations/targets/variances are **integer minutes**; times-of-day are minutes since midnight. No floats. Format via Jinja filters `hm`/`hm_signed`/`clock`.
- **One fixed reference timezone, not per-employee ones** (manager request, 2026-08-10): every `start_minute`/`end_minute`/"today" the app captures from the real world is expressed in `app/util.py`'s `BUSINESS_TZ` (`America/Chicago`, handles CST/CDT automatically) — never the server container's own OS clock (Cloud Run defaults to UTC) and never wherever an employee physically is. Always capture "now"/"today" via `util.now_local()`/`util.today_local()` — never call `dt.datetime.now()`/`dt.date.today()` directly anywhere a clock-face value or business day is being determined. (`dt.datetime.utcnow()` is still correct and unchanged for audit-trail timestamps like `reviewed_at`/`updated_at`/`started_at` — those are pure elapsed-time/audit values, not clock-face-of-day, so they stay in UTC.)
- Every human-read date/timestamp is **MM/DD/YYYY** (manager request, 2026-08-03) via the `mdy`/`mdy_dt` Jinja filters or `app/util.py`'s `fmt_date`/`fmt_datetime` — never a raw `.isoformat()`/`.strftime()` in a template or export. Exception: `<input type="date">` values, hidden form fields, and option/checkbox submit values stay ISO (`YYYY-MM-DD`) — that's the HTML date-input spec and what `parse_date_field`/`dt.date.fromisoformat` expect back, not something a human reads.
- **Date *pickers* display mm/dd/yyyy too** (Ganesh, 2026-08-21) — `app/static/mdy_datepicker.js`, loaded once in `base.html`, progressively enhances every `<input type="date">` on every page automatically. Don't build a per-template date-picker or add a text-format hint another way — just use a plain `<input type="date">` like always; the script finds it. The native input stays in the DOM (hidden) and is still what actually submits, still holding the ISO value — the hard rule above is unchanged, this only touches what's displayed.
- `DayStatus.source='imported'` rows are **frozen legacy fact** — never recompute, migrate, or "fix" them (raw sheet cell is in `imported_token`). Admins change history via overrides only.
- `strike_exempt=True` rows (pre-policy days before 2026-04-15) must never count as strikes — the legacy sheets' own formulas excluded them. Changing this breaks `verify_strikes`.
- Status precedence: override → compensation (`comp_erases_strike` config) → base. Implemented in `DayStatus.effective_status()` and `engine.strikes_in()` — change both or neither.
- Any change to `app/engine.py`, `app/validation.py`, or `legacy/import_legacy.py` ⇒ run pytest **and** verify_strikes before calling it done.
- Schema changes: no alembic in the POC — `rm tms.db` + re-import is the migration path.
- Config lives in the `config` table (defaults: `CONFIG_DEFAULTS` in app/models.py); read via `engine.get_config(db)`, never hardcode thresholds.
- **Holidays are one shared company-wide list, not per-country** (manager request, 2026-08-14 — reverted a brief 2026-08-12 per-country split; holidays apply to every employee regardless of location). `engine.holidays_set(db, location=None)` always returns every Holiday row unscoped — `location` is accepted for backward compatibility but ignored, so it's fine (and preferred going forward) to just call `holidays_set(db)`. `Employee.location` and `Holiday.location` still exist as columns (see Holiday's docstring in app/models.py for why — dropping them isn't an additive schema change, and this app has real production data now) but nothing reads `Holiday.location` to filter anything anymore; new Holiday rows just get `DEFAULT_LOCATION` stamped on invisibly. Don't reintroduce per-employee holiday scoping without checking with Ganesh first — it was tried once and reverted within days.
- **Holiday Management is behind `HOLIDAY_MANAGEMENT_ENABLED`** (env var, `app/templating.py`, defaults **on**). Briefly defaulted off for the 2026-08-13 deploy so My Month's read-only lookback and a button-highlight fix could ship first; back to on the same day once that landed. Guarded server-side either direction, not just hidden from nav: `holidays_page`/`update_location` in `app/routes/employee.py` and all six `holiday_*` routes in `app/routes/admin.py` 404 while off. Roster add/edit pass `None` instead of the submitted `location` to `_emp_from_form` while off, so editing an employee for an unrelated reason never silently resets their country back to the default. This flag also still gates the Roster/Profile Country fields even though they're no longer wired to Holidays at all (kept as general employee metadata). Set `HOLIDAY_MANAGEMENT_ENABLED=0` on a host to take it dark again without a code change.
- **Leave Management V2 is behind `LEAVE_MANAGEMENT_V2_ENABLED`** (env var, `app/templating.py`, defaults **on** as of 2026-08-22 — built 2026-08-21). Ganesh ran the real `pytest` (468/469, the one failure was an unrelated pre-existing SQLAlchemy-version-drift flake, fixed the same day but not yet re-confirmed with a second run) and `legacy.verify_strikes`, which could not run at all — his checkout is missing the legacy `.ods`/`tms.db`/`import_report.json` bundle (see the missing-legacy-data note, still pending from Steve). He chose to enable V2 anyway rather than wait — a known, accepted gap, not an oversight; if strike counts ever look off on a day with leave once the bundle is finally available and `verify_strikes` can run, check `leave_minutes_on()`/`leave_balance_v2()` first, since those are the two functions this flag actually changed in `app/engine.py`. Set `LEAVE_MANAGEMENT_V2_ENABLED=0` to fall back to the old 4 leave types without a code change. Replaces the 4 old leave types (Casual/Sick/Vacation/Other, `m.LEAVE_TYPES`) with 5 new ones (Planned/Unplanned/Unpaid/Bereavement/Special Paid Time, `m.LEAVE_TYPES_V2`) for *new* requests only — old `LeaveRecord.type` values on existing rows are frozen history, never migrated. Half Day/Full Day/Custom duration, automatic monthly Planned Time accrual by tenure (`engine.planned_time_accrued_minutes`, band table in `Config`: `planned_days_year_0_2`/`_2_5`/`_5_plus`), blocked only during `Employee.probation_days` (falls back to `Config.probation_days_default`) — every other leave type stays available during it. Used/Pending/Remaining shown separately (`engine.leave_balance_v2`); partial approval (`LeaveRecord.approved_minutes_per_day`) requires a `review_note` and is what `engine.leave_minutes_on()` now prefers over `minutes_per_day` when set — change that fallback order and partial approvals silently stop reducing the compliance target. Bereavement requires `LeaveRecord.relation` (`m.BEREAVEMENT_RELATIONS`). Special Paid Time has no separate approval step — a SuperAdmin's `SpecialPaidGrant` row *is* the approval (`/admin/leave/special-paid`, 404s while the flag is off). PIP (`Employee.is_on_pip`) forces every *requested* leave type to Unpaid Time via `engine.effective_leave_type()` at submission time only, never retroactively — Special Paid Time is exempt since it's a grant, not a request. Overtime-for-Missed-Hours match is employee-requested (`POST /leave/match-request`) and admin-approved/rejected (`POST /admin/complink/{id}/approve|reject`, both 404 while off) — reuses `CompensationLink` (`status`, `requested_by_employee` columns) rather than a new table; approval re-checks `engine.compensation_window_ok()` (~21 days or same calendar month) before calling the same `engine.evaluate_link()` the admin-direct link path already uses. Guarded server-side everywhere, not just hidden from nav — see `app/routes/employee.py`'s `my_leave`/`request_leave`/`request_compensation_match` and `app/routes/admin.py`'s `leave_page`/`leave_approve`/`leave_add`/`special_paid_page`/`special_paid_grant`/`approve_complink`/`reject_complink`. While off, `/leave` and `/admin/leave` behave exactly as before. Full design + interpretation calls flagged for Ganesh (the 3-week notice tier, the compensation-match window boundary) in `docs/LEAVE_MANAGEMENT_PLAN.md`.

- **Task Planning ("Plan for the Day" + Start/Pause/Resume/Stop, Ganesh, 2026-08-21) ships without a feature flag** — unlike Leave V2 above, it touches neither `app/engine.py` nor `app/validation.py`, so the pytest+verify_strikes hard rule two bullets up doesn't gate it. Why: every Start-to-Pause (or Start-to-Stop) segment becomes its OWN ordinary `TaskEntry` row through the exact same `_finish_task_timer()`/`validate_entry()` path the pre-existing Auto time capture timer already used — a paused-and-resumed plan just produces two normal rows with real clock times, not one row with an internal gap, so there was never a new overlap/cap/duration case for `validate_entry`/`compute_day` to learn. New tables/columns are additive-only: `PlannedTask` (`status`: `planned`/`running`/`paused`/`done`, see `m.PLAN_STATUSES`) and a nullable `ActiveTaskTimer.planned_task_id` — set only when the single shared active timer (same `UniqueConstraint("employee_id")` Auto time capture already enforces) was opened from a plan's Start/Resume rather than the ad-hoc timer form; NULL for every ad-hoc timer, unchanged. `app/routes/employee.py`'s `_stop_current_timer_if_any()` is what both `start_task_timer` and `/plan/{id}/start` now share for "starting one auto-finishes whatever else was running" — if the finished timer was plan-linked, that plan goes back to `paused` rather than being left stranded in `running`. Task log rows past the day's target get a distinct color via `util.overtime_row_flags()` — a pure, tested cumulative-sum helper, not compliance math (`DayStatus.target_minutes` is untouched). Plans are always "today" only, same live convention as Break/Punch/Auto time capture (`today.html` hides the whole section on any other day). **Admins see the same coloring** (Ganesh asked 2026-08-21) — `admin/person.html`'s Task log reuses `overtime_row_flags()` too, but driven by each day's already-computed `DayStatus.target_minutes` (set by `recompute_employee`, which this route already runs) rather than a fresh live leave/break calc, since it's rendering a whole month of past days, not just "today." See `docs/TASK_PLANNING_TIMER_PLAN.md` for the original bigger design and exactly how much of it this narrowed down to.

- **Task Planning got 4 follow-on changes (Ganesh, 2026-08-22)** — two touch `app/validation.py`, so unlike the rest of Task Planning above, these DO fall under the pytest+verify_strikes hard rule; the sandbox that built them still has neither, so run both before calling this live. (1) `validation.all_gap_windows()` is a new, standalone function returning every currently-unexplained gap (not just the earliest, which `earliest_gap_window()` — unchanged — still alone drives the server-side Add Row prefill). It duplicates gap_flags()'s break-overlap math rather than refactoring it out, specifically so `gap_flags()`/`earliest_gap_window()` stay byte-for-byte unchanged; if the definition of "what counts as a gap" ever changes, update the covered-break-overlap logic in both places together. (2) `_day_context()` uses it to show each gap as its own dashed, fillable placeholder row in the task log table (`_GapLogRow`, same `id=None` display-only convention as `_BreakLogRow`) — only for `day_unlocked_today` (today, not yet submitted); a past day or a locked today still just shows the plain ⚠ label. Its "Fill" button is pure client-side JS (`fillGapRow()` in today.html) that pre-fills Add Row's Start/End and scrolls to it — filling only part of a gap and leaving the rest is how "split a gap into more than one entry" works, since whatever's left just becomes a smaller gap row on the next load; there's no separate multi-segment endpoint. (3) `add_plan()` auto Punch-Ins an employee the moment they add the day's *first* plan (not when they Start it) if they aren't already punched in — duplicates `punch_in()`'s own "any open session today" check rather than calling that route, since `punch_in()` flashes/redirects on its own. (4) `submit_day()` now copies (never moves) any plan still `planned`/`paused` at submit time to a fresh `PLAN_PLANNED` row dated tomorrow — the original row's status/date are untouched (frozen history, same instinct as `DayStatus.source='imported'`); `PlannedTask.carried_at` (new nullable column) is set once carried so resubmitting the same day after an admin unlock can't duplicate the copy. A plan still `running` at submit time is left alone on purpose.

- **Auto time capture's Stop/Cancel now branch on `active_timer.planned_task_id` (Ganesh, 2026-08-22 bugfix)** — a plan-linked timer showed the same generic "Stop & Log"/"Cancel" controls an ad-hoc timer gets, but neither `/task-timer/stop` nor `/task-timer/cancel` ever touched `PlannedTask.status`: Stop & Log left the plan stuck showing "running" with a dead timer behind it (it self-healed on the *next* Pause/Stop click via `_finish_plan_segment`'s no-timer-found no-op, but read wrong until then), and Cancel silently discarded the segment with no entry logged at all. today.html's Auto time capture card now shows Pause/Stop for a plan-linked timer, posting straight to the same `/plan/{id}/pause|stop` Today's Plan's own buttons use — one code path, not two that can disagree. `stop_task_timer`/`cancel_task_timer` also sync `plan.status` themselves now (`PLAN_DONE`/`PLAN_PAUSED`) as defense-in-depth, since both routes are still directly reachable regardless of what the UI offers.
- **A plan's `white-space:pre-wrap` details div needs `{%- -%}` whitespace-trimmed Jinja tags (Ganesh, 2026-08-22 bugfix)** — same reason the task log table's own details/edit toggle already uses `{%- if -%}`/`{%- endif %}` around it (`today.html`, both `<td style="white-space:pre-wrap">` and the plan-item's `<div class="small" style="white-space:pre-wrap">`): this app's Jinja env doesn't set `trim_blocks`/`lstrip_blocks`, so an untrimmed `{% if %}`/`{% endif %}` on its own source line leaves a literal newline+indentation text node in the output, which `pre-wrap` then renders as a visible blank line. Any *new* `white-space:pre-wrap` block with a conditional inside it needs the same `-%}`/`{%-` treatment or it'll show the same phantom gap.
- **Free-text `details` gets its first letter capitalized on save, org-wide scope deliberately not attempted (Ganesh, 2026-08-22)** — `util.capitalize_first()` (distinct from `normalize_title_case()`, which Title-Cases every word and exists only for short Project/Task suggestion names), applied at every place `TaskEntry.details`/`PlannedTask.details` actually gets persisted: `add_entry`, the entry-details edit route, `_finish_task_timer` (covers both Auto time capture's Stop and every Plan Pause/Stop segment, since they share this one function), `add_plan`, `edit_plan`. Deliberately NOT applied to the intermediate `ActiveTaskTimer.details` at Start time or Stop's optional top-up — both eventually flow through `_finish_task_timer`'s own call, so capitalizing earlier would just be redundant. Scope was explicitly asked and answered narrow (AskUserQuestion, 2026-08-22): this does not touch status badges, nav labels, or any other lowercase microcopy site-wide — only the "today" label in the date dropdown/page header on Today, and these 5 details-saving call sites. **Per-line, not just the string's first character (Ganesh, 2026-08-22 bugfix, same day)** — real usage turned out to almost always be a manually-typed numbered list with no space after the number (`"1.need to work on X\n2.reviewed Y"`), so capitalizing only index 0 capitalized the leading `"1"` (a no-op) and left every actual word lowercase — the feature looked like it did nothing. `capitalize_first()` now uppercases the first `[a-zA-Z]` character of *each* line independently (a line with no letters at all is left untouched), so `"1.need to work"` → `"1.Need to work"`. This only takes effect on save — it's not retroactive, so already-submitted rows (locked days) keep whatever casing they were saved with unless an admin re-edits them (admins bypass the today-only/lock rule on the details edit toggle).
- **Time by Project/Task gained a "By Project" breakdown + pie chart (Ganesh, 2026-08-24)** — `reports.time_by_project_report()` is a companion to `time_by_activity_report()` on the same `/admin/reports/time` page, same filter semantics (department/employee/project/task, same date range), but grouped the other way: per project, total time and its split across tasks worked on within it. Deliberately does NOT include zero-minute projects the way the employee table includes zero-minute employees — there are ~300 Project/Employer rows org-wide (mostly client names), so an all-zero row here would be noise, not signal (an all-zero *employee* row is signal precisely because there are only ~45 of them). The "Pie chart" button (top-right of the By Project card) toggles a hand-rolled inline SVG pie chart — no charting library pulled in, matching every other bit of page-specific JS in this app (`msfilter.js`, `mdy_datepicker.js`) being small and dependency-free. Caps to the top 8 projects by time + an "Other" bucket for the rest, using a new fixed 9-color qualitative palette (`PIE_COLORS` in the template) — deliberately NOT the app's semantic status colors (`--ok`/`--warn`/`--bad`/...), since those mean something specific elsewhere and reusing them for arbitrary project names would be misleading. `reports.time_by_project_report()`'s return also includes `chart_data` (a plain JSON-safe `[{name, minutes}, ...]` list, already sorted busiest-first) specifically so the template's `|tojson` doesn't need to serialize ORM objects. On-slice text labels were tried the same day and then explicitly reverted (Ganesh: "no please remove text") — slice names live only in the legend and each arc's `<title>` hover, not drawn inside the wedge. `pieEscape()` in `renderProjectPie()` (`admin/reports_time.html`) stayed from that attempt and is still worth keeping — it HTML-escapes every project name used in markup (legend + arc `<title>`), since names are free-typed by whoever created the Project/Employer row (same reasoning as Jinja's autoescaping, just done by hand because this string is assembled in JS).
- **Compensation match requests weren't notifying admins, and looked identical to approved links (Ganesh, 2026-08-22 bugfix)** — three related gaps, all display/notification/placement-only, no engine.py changes. (1) `_admin_nav_badges()` (`app/templating.py`) never counted pending `CompensationLink` requests (`status='requested', requested_by_employee=True`, created by `POST /leave/match-request`) at all, so a super admin had no signal one existed. Now folded into `pending_overtime` via `_pending_complink_count()` — deliberately NOT added for a department-scoped admin, since `approve_complink`/`reject_complink` are both `require_super_admin` and a Team Lead has no route that can act on one anyway. (2) Overtime Management's and Person Detail's "Compensation links" tables showed every link — pending, approved, or rejected — through the same fully/partial coverage badge (`lk.fully_compensated`), which for a still-`requested` link is just its unset default, since `engine.evaluate_link()` (the function that actually computes coverage) only ever runs from `approve_complink()`, never at request time. That made an unreviewed request look exactly like an already-approved, already-evaluated one. Both tables now show `status`-aware badges instead: "pending approval" (+ a link to `/admin/leave` to act on it) for `requested`, "rejected" for `rejected`, fully/partial only for anything else (i.e. approved). (3) The pending-decision card itself ("Overtime ↔ Missed Hours match requests", Approve/Reject forms posting to `/admin/complink/{id}/approve|reject`) moved from Leave Management to Overtime Management the same day (Ganesh: "this should be not in leave management it should be in overtime management under pending requests") — same `CompensationLink` rows/routes, just relocated in `overtime_page()`/`overtime.html` instead of `leave_page()`/`leave.html`, sitting right after the existing OvertimeApproval "Pending requests" card (renamed "Pending overtime requests" the same day to disambiguate from the new card, both instances) and gated the same `scope is None` (super-admin-only) way. The "review →" links on the Compensation links tables (Overtime Management, Person Detail) were updated from `/admin/leave` to `/admin/overtime` to match. (4) The matching employee-facing self-service card ("Request an Overtime ↔ Missed Hours match", `POST /leave/match-request`) moved the same way — from `leave.html`/`my_leave()` to `overtime.html`/`my_overtime()` — same route path, same `CompensationLink` creation logic, just relocated + its post-submit redirect changed from `/leave` to `/overtime`. The underlying compliance math was never wrong — a pending request was never silently marked `DayStatus.compensated` — this was purely an admin-visibility/placement gap.
- **An already-linked shortfall day stayed pickable in the Shortfall day dropdown (Ganesh, 2026-08-25 bugfix)** — the quick-link forms on Person Detail and Overtime Management (`_shortfalls_surpluses()`, `app/routes/admin.py`) never excluded or flagged a day that already had a `CompensationLink`, so re-picking it and submitting only failed via `add_complink`'s *surplus*-side clash check ("Surplus day(s) already linked to another shortfall") once its surplus day(s) had already been consumed by the first link — a confusing error that didn't say the shortfall itself was the real reason. Display-only fix, no `engine.py`/`validation.py` change: a new `_shortfall_links_by_date(db, employee_id)` helper returns every date this employee already has a link for; both `person()` and `overtime_page()` pass it to their templates as `already_linked_shortfalls`/`link_already_linked_shortfalls`, and `person.html`/`overtime.html` render that option disabled + struck-through + "✓ … already linked" instead of silently letting it be picked again. Overtime Management's Link button disabled-state also now checks `link_has_unlinked_shortfall` (at least one shortfall NOT already linked) instead of just `link_shortfalls` being non-empty, so a person whose only shortfall day is already linked correctly shows "No shortfall days left to link" rather than an enabled button that would just hit the same clash error.
- **Compensation links now allocate by the MINUTE, not the whole day (Ganesh, 2026-08-25)** — before this, linking consumed a surplus day's *entire* variance and blocked it from ever being reused, even if the shortfall only needed part of it, and a shortfall day could only ever be targeted by one link. Ganesh's own example: an 8-hour shortfall with a surplus day worth 10 hours should leave that day with 2 hours still available afterward; three 2-hour surplus days linked against an 8-hour shortfall should leave the shortfall at -2:00 (not silently treated as done, and not rejected outright) so a later link can finish it off. New nullable `CompensationLink.surplus_minutes` column (`app/models.py`, JSON `{"YYYY-MM-DD": minutes}` — how many minutes of each surplus day THIS link actually used, not just which days). Reading side lives in `app/engine.py`: `_link_allocated_minutes()` (one link's own total, with an old-format fallback — a pre-2026-08-25 link has no `surplus_minutes`, so it's read as having consumed each `surplus_dates` day in full, which is the correct frozen reading for it, not a gap to backfill), `shortfall_allocated_minutes()`/`shortfall_allocated_minutes_by_date()` (sum across every link that targets a given shortfall day — a day can now be covered by more than one partial link), `surplus_minutes_used_by_date()` (sum across every link for a given surplus day). Writing side: `allocate_surplus_minutes()` greedily consumes ticked surplus days oldest-first until the shortfall's remaining deficit is covered, skipping (not erroring on) a day with nothing left — shared by both `add_complink()` (admin-direct, `app/routes/admin.py`) and `request_compensation_match()` (employee self-service, `app/routes/employee.py`) so the two flows can't drift apart; `approve_complink()` doesn't need its own allocation step since the employee's request already computed and stored it at submission time. `evaluate_link()` was split into two related but distinct signals: `link.fully_compensated` (did THIS link alone cover the whole deficit — drives that link's own row badge and flash message) vs. `DayStatus.compensated` (the day's TRUE aggregate across every link targeting it — drives strike erasure via `comp_erases_strike`, and is what actually matters when a second link finishes off a day an earlier partial link started). `compensated_dates()` (read by `recompute_employee()` on every regular recompute pass — i.e. on most page loads) was rewritten the same way; missing this would have meant a day fully covered by two partial links got silently un-flagged the next time anyone loaded a page for that employee. The Shortfall day / Surplus day pickers on Person Detail, Overtime Management, and the employee-facing Overtime page all show remaining balances now (e.g. "2:00 remaining of 8:00 — partially linked") instead of a binary already-linked/not-linked state. No `verify_strikes` confirmation possible here either (same missing-legacy-bundle gap as Leave V2) — verified instead via a standalone Python simulation of the allocation algorithm (exact match on both of Ganesh's worked examples plus edge cases: exact-match, under-supply, a second link finishing a partial day, order-sensitive greedy allocation, skipping an exhausted day) and a real-SQLite integration check against hand-seeded `imported`-source test rows in `tms_demo.db` (Alok Gupta 08/06, Tanvi Banerjee 08/08) reproducing both examples' numbers exactly.
- **Today page cards got a depth pass, Pause/Stop swapped colors (Ganesh, 2026-08-22)** — `.today-page .card` now carries a soft `rgba(7,41,38,..)` box-shadow (same brand-dark tint every dropdown/popover already shadows with, just softer) and a touch more `margin-bottom` (8px → 10px), since the earlier compacting pass (padding/margin tightened, see the comment above it) had made every section blur into one wall — border color alone wasn't enough contrast against the page background. `button.warn` (Pause, on both the Today's Plan card and the Auto time capture card) now uses `--accent-light`/`--brand-dark`, the exact pair `.topbar nav a.on` already uses for the active "Today" nav tab; `button.danger` (Stop) took over Pause's old `--warn` olive/brown. Both classes are only ever used on these 4 buttons in `today.html` (confirmed via grep before changing them), so this was safe to change at the shared CSS-class level rather than needing new one-off classes.
- **Dashboard readability pass (Ganesh, 2026-08-22)** — template/CSS-only, no engine/validation changes. "Needs attention" groups its three row types (leave requests, support tickets, strikes & shortfalls) under small count-labeled headers instead of one flat list of same-looking pills, and bolds the strike/shortfall count per row — the list was already sorted worst-first (`admin.dashboard()`'s `violations.sort()`, unchanged), this just makes that ordering visible. Department tiles: "All departments" always gets the accent treatment (`deptcard.all`) since it's the primary entry point, and any tile with `dept_present.get(d, 0) == 0` gets dimmed (`deptcard.zero`, full opacity on hover) so a glance at the grid shows which teams have logged in today. Recent activity now runs `AuditLog.action` through `util.humanize_audit_action()` (new Jinja global) instead of `action.replace('_',' ')` + the raw entity class name — e.g. "config change · Config" reads as "Updated config". Deliberately scoped to the Dashboard's preview widget only; `/admin/audit`'s full log keeps raw action codes since that page is a searchable technical trail, not a summary — don't route it through the same helper. The "On leave today" KPI card gets `--leave` color (`kpi.leavek`) when non-zero, matching the accent scheme "Logged"/"Not yet logged" already had (`kpi.okk`/`kpi.badk`). `humanize_audit_action()`'s dict isn't required to track every `audit()` call site — an unmapped action code falls back to the same title-cased text the Dashboard showed before this existed.
- **Past days show a read-only "Plan" card (Ganesh, 2026-08-22)** — the interactive "Plan for the day"/"Today's Plan" section in `today.html` still only renders `if day == today` (a `PlannedTask` only ever carries the date it was added on — see the comment on `_day_context`'s `plans` query), but browsing a past day via the date dropdown no longer shows nothing: `_day_context` now also returns `past_plans` (same query, just `date != today_local()`, no branch shared with `plans`) and `today.html` renders it in its own `{% if day != today and past_plans %}` card, reusing the exact same `plan-item plan-{{ p.status }}` markup/badge classes as the live card for visual consistency but with every Start/Pause/Stop/edit/delete form stripped out — it's read-only context ("what was planned/left running/done that day"), not something this page can act on for a past date. No new CSS, no engine/validation changes.
- **Suggestions and Overtime Management got read-only "quick view" additions (Ganesh, 2026-08-21)** — neither touches `engine.py`/`validation.py`. `/admin/suggestions` now shows an **Approved** table under each Pending one (`admin._approved_suggestions()`), filtered to `status == LIST_APPROVED and created_by_employee_id is not None` — that second condition is what separates an actual approved suggestion from a Project/TaskType an admin added directly via Lists (those also land on `LIST_APPROVED`, just with no submitter, since that's Project/TaskType's own default — see their docstrings). `/admin/overtime` gained a collapsed-by-default **Compensation links** card (every `CompensationLink` in the same `led_by()` scope as the rest of the page — previously only ever visible one employee at a time via Person Detail) and a monthly **Who worked overtime** table that just reuses `reports.attendance_report()`'s existing `overtime_minutes`/`approved_overtime_minutes` summary figures rather than computing its own — don't duplicate that math a third time if this needs extending, extend `attendance_report()` instead. The Compensation links card also has its own employee picker + shortfall/surplus preview so a link can be *created* without leaving Overtime Management — it posts to the same `POST /admin/person/{id}/complink` Person Detail's form already used, just with a hidden `return_to=overtime` field so `_complink_redirect()` sends the admin back here (with the same employee/month still selected) instead of to Person Detail. Deleting a link is still Person Detail-only. `_shortfalls_surpluses()` (pulled out of `person()`) is the shared piece both routes call — change it once, not twice.
- **`TaskEntry.entry_method` tracks usage from 2026-08-21 onward only** — one of `m.ENTRY_METHODS` (`plan`/`auto_timer`/`manual_add`), stamped at each of the 3 row-creation sites in `app/routes/employee.py` (`add_entry`, `_finish_task_timer`), NULL on every older/imported row. `app/reports.py`'s `feature_usage_report()` (backing `/admin/reports/usage`, the Developer Usage Report) must keep treating NULL as excluded from every method's count, never folded into `manual_add` — don't "clean up" old NULLs by backfilling a guess, they're correctly unknown. Gated by `require_developer_or_admin` (`app/auth.py`), not `require_admin` — a plain-employee `is_developer` can reach it without being an admin.

## Layout

- `app/util.py` — formatting/audit helpers shared by routes and templates; also the one place `now_local()`/`today_local()`/`BUSINESS_TZ` live (see Hard rules above)
- `app/engine.py` — all business math (statuses, variance, strikes, recompute, ledger)
- `app/validation.py` — PRD §4 entry rules (overlaps block, gaps flag, 4h cap, backdate window)
- `app/routes/` — auth (dev login), employee (Today/My Month/Leave/Overtime/Holidays/Support/Profile — Overtime includes the V2 self-service Missed-Hours-match-request flow behind `LEAVE_MANAGEMENT_V2_ENABLED` (moved here from Leave 2026-08-22; its `POST /leave/match-request` route path is unchanged, only the page it's requested from moved); Today includes Task Planning's `/plan/*` routes, no flag), admin (dashboard/roster/lists/leave/overtime/holidays/config/audit/support — leave includes the Special Paid Time grant behind the same flag; overtime includes the compensation-match approve/reject decision card, also moved from leave 2026-08-22), tickets (Ticketing System — raise/list/detail/comment/status-change), exports (XLSX/CSV)
- `app/holiday_bulk_upload.py` — Excel upload for Holiday rows (name/date only — one shared calendar), same shape as `app/leave_bulk_upload.py`
- `app/auth.py` — the Entra ID swap point; session/role gating stays
- `legacy/` — streaming ODS reader (survives the 700 MB file), extractor, importer, strike verifier
- `legacy/cache/import_report.json` — every oddity the import tolerated
