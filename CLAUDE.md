# CLAUDE.md — MK Timekeeping & Compliance POC

FastAPI + SQLAlchemy + Jinja2 internal tool replacing three legacy spreadsheets for
time/leave/compliance tracking (~45 staff, originally all India-based offshore, now also
US-based). Spec: docs/PRD.md. Onboarding: HANDOFF.md.

## Commands

```bash
.venv/bin/python -m uvicorn app.main:app --port 8127     # run (http://localhost:8127, dev pick-a-user auth)
.venv/bin/python -m pytest tests/ -q                     # 382 tests — must stay green
.venv/bin/python -m legacy.verify_strikes                # acceptance: MUST print 168/168
rm tms.db && .venv/bin/python -m legacy.import_legacy    # rebuild DB from the 3 legacy .ods files
```

## Hard rules

- **The project directory name ends with a trailing space** — always quote absolute paths.
- Python 3.9 (system). No `X | Y` unions in runtime annotations; use `Optional[...]`.
- All durations/targets/variances are **integer minutes**; times-of-day are minutes since midnight. No floats. Format via Jinja filters `hm`/`hm_signed`/`clock`.
- **One fixed reference timezone, not per-employee ones** (manager request, 2026-08-10): every `start_minute`/`end_minute`/"today" the app captures from the real world is expressed in `app/util.py`'s `BUSINESS_TZ` (`America/Chicago`, handles CST/CDT automatically) — never the server container's own OS clock (Cloud Run defaults to UTC) and never wherever an employee physically is. Always capture "now"/"today" via `util.now_local()`/`util.today_local()` — never call `dt.datetime.now()`/`dt.date.today()` directly anywhere a clock-face value or business day is being determined. (`dt.datetime.utcnow()` is still correct and unchanged for audit-trail timestamps like `reviewed_at`/`updated_at`/`started_at` — those are pure elapsed-time/audit values, not clock-face-of-day, so they stay in UTC.)
- Every human-read date/timestamp is **MM/DD/YYYY** (manager request, 2026-08-03) via the `mdy`/`mdy_dt` Jinja filters or `app/util.py`'s `fmt_date`/`fmt_datetime` — never a raw `.isoformat()`/`.strftime()` in a template or export. Exception: `<input type="date">` values, hidden form fields, and option/checkbox submit values stay ISO (`YYYY-MM-DD`) — that's the HTML date-input spec and what `parse_date_field`/`dt.date.fromisoformat` expect back, not something a human reads.
- **Date *pickers* display mm/dd/yyyy too** (Ganesh, 2026-08-21) — `app/static/mdy_datepicker.js`, loaded once in `base.html`, progressively enhances every `<input type="date">` on every page automatically. Don't build a per-template date-picker or add a text-format hint another way — just use a plain `<input type="date">` like always; the script finds it. The native input stays in the DOM (hidden) and is still what actually submits, still holding the ISO value — the hard rule above is unchanged, this only touches what's displayed.
- `DayStatus.source='imported'` rows are **frozen legacy fact** — never recompute, migrate, or "fix" them (raw sheet cell is in `imported_token`). Admins change history via overrides only.
- `strike_exempt=True` rows (pre-policy days before 2026-04-15) must never count as strikes — the legacy sheets' own formulas excluded them. Changing this breaks `verify_strikes`.
- Status precedence: override → compensation (`comp_erases_strike` config) → base. Implemented in `DayStatus.effective_status()` and `engine.strikes_in()` — change both or neither.
- Any change to `app/engine.py`, `app/validation.py`, or `legacy/import_legacy.py` ⇒ run pytest **and** verify_strikes before calling it done.
- Schema changes: no alembic in the POC — `rm tms.db` + re-import is the migration path.
- Config lives in the `config` table (defaults: `CONFIG_DEFAULTS` in app/models.py); read via `engine.get_config(db)`, never hardcode thresholds.
- **Holidays are one shared company-wide list, not per-country** (manager request, 2026-08-14 — reverted a brief 2026-08-12 per-country split; holidays apply to every employee regardless of location). `engine.holidays_set(db, location=None)` always returns every Holiday row unscoped — `location` is accepted for backward compatibility but ignored, so it's fine (and preferred going forward) to just call `holidays_set(db)`. `Employee.location` and `Holiday.location` still exist as columns (see Holiday's docstring in app/models.py for why — dropping them isn't an additive schema change, and this app has real production data now) but nothing reads `Holiday.location` to filter anything anymore; new Holiday rows just get `DEFAULT_LOCATION` stamped on invisibly. Don't reintroduce per-employee holiday scoping without checking with Ganesh first — it was tried once and reverted within days.
- **Holiday Management is behind `HOLIDAY_MANAGEMENT_ENABLED`** (env var, `app/templating.py`, defaults **on**). Briefly defaulted off for the 2026-08-13 deploy so My Month's read-only lookback and a button-highlight fix could ship first; back to on the same day once that landed. Guarded server-side either direction, not just hidden from nav: `holidays_page`/`update_location` in `app/routes/employee.py` and all six `holiday_*` routes in `app/routes/admin.py` 404 while off. Roster add/edit pass `None` instead of the submitted `location` to `_emp_from_form` while off, so editing an employee for an unrelated reason never silently resets their country back to the default. This flag also still gates the Roster/Profile Country fields even though they're no longer wired to Holidays at all (kept as general employee metadata). Set `HOLIDAY_MANAGEMENT_ENABLED=0` on a host to take it dark again without a code change.
- **Leave Management V2 is behind `LEAVE_MANAGEMENT_V2_ENABLED`** (env var, `app/templating.py`, defaults **on** as of 2026-08-22 — built 2026-08-21). Ganesh ran the real `pytest` (468/469, the one failure was an unrelated pre-existing SQLAlchemy-version-drift flake, fixed the same day but not yet re-confirmed with a second run) and `legacy.verify_strikes`, which could not run at all — his checkout is missing the legacy `.ods`/`tms.db`/`import_report.json` bundle (see the missing-legacy-data note, still pending from Steve). He chose to enable V2 anyway rather than wait — a known, accepted gap, not an oversight; if strike counts ever look off on a day with leave once the bundle is finally available and `verify_strikes` can run, check `leave_minutes_on()`/`leave_balance_v2()` first, since those are the two functions this flag actually changed in `app/engine.py`. Set `LEAVE_MANAGEMENT_V2_ENABLED=0` to fall back to the old 4 leave types without a code change. Replaces the 4 old leave types (Casual/Sick/Vacation/Other, `m.LEAVE_TYPES`) with 5 new ones (Planned/Unplanned/Unpaid/Bereavement/Special Paid Time, `m.LEAVE_TYPES_V2`) for *new* requests only — old `LeaveRecord.type` values on existing rows are frozen history, never migrated. Half Day/Full Day/Custom duration, automatic monthly Planned Time accrual by tenure (`engine.planned_time_accrued_minutes`, band table in `Config`: `planned_days_year_0_2`/`_2_5`/`_5_plus`), blocked only during `Employee.probation_days` (falls back to `Config.probation_days_default`) — every other leave type stays available during it. Used/Pending/Remaining shown separately (`engine.leave_balance_v2`); partial approval (`LeaveRecord.approved_minutes_per_day`) requires a `review_note` and is what `engine.leave_minutes_on()` now prefers over `minutes_per_day` when set — change that fallback order and partial approvals silently stop reducing the compliance target. Bereavement requires `LeaveRecord.relation` (`m.BEREAVEMENT_RELATIONS`). Special Paid Time has no separate approval step — a SuperAdmin's `SpecialPaidGrant` row *is* the approval (`/admin/leave/special-paid`, 404s while the flag is off). PIP (`Employee.is_on_pip`) forces every *requested* leave type to Unpaid Time via `engine.effective_leave_type()` at submission time only, never retroactively — Special Paid Time is exempt since it's a grant, not a request. Overtime-for-Missed-Hours match is employee-requested (`POST /leave/match-request`) and admin-approved/rejected (`POST /admin/complink/{id}/approve|reject`, both 404 while off) — reuses `CompensationLink` (`status`, `requested_by_employee` columns) rather than a new table; approval re-checks `engine.compensation_window_ok()` (~21 days or same calendar month) before calling the same `engine.evaluate_link()` the admin-direct link path already uses. Guarded server-side everywhere, not just hidden from nav — see `app/routes/employee.py`'s `my_leave`/`request_leave`/`request_compensation_match` and `app/routes/admin.py`'s `leave_page`/`leave_approve`/`leave_add`/`special_paid_page`/`special_paid_grant`/`approve_complink`/`reject_complink`. While off, `/leave` and `/admin/leave` behave exactly as before. Full design + interpretation calls flagged for Ganesh (the 3-week notice tier, the compensation-match window boundary) in `docs/LEAVE_MANAGEMENT_PLAN.md`.

- **Task Planning ("Plan for the Day" + Start/Pause/Resume/Stop, Ganesh, 2026-08-21) ships without a feature flag** — unlike Leave V2 above, it touches neither `app/engine.py` nor `app/validation.py`, so the pytest+verify_strikes hard rule two bullets up doesn't gate it. Why: every Start-to-Pause (or Start-to-Stop) segment becomes its OWN ordinary `TaskEntry` row through the exact same `_finish_task_timer()`/`validate_entry()` path the pre-existing Auto time capture timer already used — a paused-and-resumed plan just produces two normal rows with real clock times, not one row with an internal gap, so there was never a new overlap/cap/duration case for `validate_entry`/`compute_day` to learn. New tables/columns are additive-only: `PlannedTask` (`status`: `planned`/`running`/`paused`/`done`, see `m.PLAN_STATUSES`) and a nullable `ActiveTaskTimer.planned_task_id` — set only when the single shared active timer (same `UniqueConstraint("employee_id")` Auto time capture already enforces) was opened from a plan's Start/Resume rather than the ad-hoc timer form; NULL for every ad-hoc timer, unchanged. `app/routes/employee.py`'s `_stop_current_timer_if_any()` is what both `start_task_timer` and `/plan/{id}/start` now share for "starting one auto-finishes whatever else was running" — if the finished timer was plan-linked, that plan goes back to `paused` rather than being left stranded in `running`. Task log rows past the day's target get a distinct color via `util.overtime_row_flags()` — a pure, tested cumulative-sum helper, not compliance math (`DayStatus.target_minutes` is untouched). Plans are always "today" only, same live convention as Break/Punch/Auto time capture (`today.html` hides the whole section on any other day). **Admins see the same coloring** (Ganesh asked 2026-08-21) — `admin/person.html`'s Task log reuses `overtime_row_flags()` too, but driven by each day's already-computed `DayStatus.target_minutes` (set by `recompute_employee`, which this route already runs) rather than a fresh live leave/break calc, since it's rendering a whole month of past days, not just "today." See `docs/TASK_PLANNING_TIMER_PLAN.md` for the original bigger design and exactly how much of it this narrowed down to.

- **Task Planning got 4 follow-on changes (Ganesh, 2026-08-22)** — two touch `app/validation.py`, so unlike the rest of Task Planning above, these DO fall under the pytest+verify_strikes hard rule; the sandbox that built them still has neither, so run both before calling this live. (1) `validation.all_gap_windows()` is a new, standalone function returning every currently-unexplained gap (not just the earliest, which `earliest_gap_window()` — unchanged — still alone drives the server-side Add Row prefill). It duplicates gap_flags()'s break-overlap math rather than refactoring it out, specifically so `gap_flags()`/`earliest_gap_window()` stay byte-for-byte unchanged; if the definition of "what counts as a gap" ever changes, update the covered-break-overlap logic in both places together. (2) `_day_context()` uses it to show each gap as its own dashed, fillable placeholder row in the task log table (`_GapLogRow`, same `id=None` display-only convention as `_BreakLogRow`) — only for `day_unlocked_today` (today, not yet submitted); a past day or a locked today still just shows the plain ⚠ label. Its "Fill" button is pure client-side JS (`fillGapRow()` in today.html) that pre-fills Add Row's Start/End and scrolls to it — filling only part of a gap and leaving the rest is how "split a gap into more than one entry" works, since whatever's left just becomes a smaller gap row on the next load; there's no separate multi-segment endpoint. (3) `add_plan()` auto Punch-Ins an employee the moment they add the day's *first* plan (not when they Start it) if they aren't already punched in — duplicates `punch_in()`'s own "any open session today" check rather than calling that route, since `punch_in()` flashes/redirects on its own. (4) `submit_day()` now copies (never moves) any plan still `planned`/`paused` at submit time to a fresh `PLAN_PLANNED` row dated tomorrow — the original row's status/date are untouched (frozen history, same instinct as `DayStatus.source='imported'`); `PlannedTask.carried_at` (new nullable column) is set once carried so resubmitting the same day after an admin unlock can't duplicate the copy. A plan still `running` at submit time is left alone on purpose.

- **Auto time capture's Stop/Cancel now branch on `active_timer.planned_task_id` (Ganesh, 2026-08-22 bugfix)** — a plan-linked timer showed the same generic "Stop & Log"/"Cancel" controls an ad-hoc timer gets, but neither `/task-timer/stop` nor `/task-timer/cancel` ever touched `PlannedTask.status`: Stop & Log left the plan stuck showing "running" with a dead timer behind it (it self-healed on the *next* Pause/Stop click via `_finish_plan_segment`'s no-timer-found no-op, but read wrong until then), and Cancel silently discarded the segment with no entry logged at all. today.html's Auto time capture card now shows Pause/Stop for a plan-linked timer, posting straight to the same `/plan/{id}/pause|stop` Today's Plan's own buttons use — one code path, not two that can disagree. `stop_task_timer`/`cancel_task_timer` also sync `plan.status` themselves now (`PLAN_DONE`/`PLAN_PAUSED`) as defense-in-depth, since both routes are still directly reachable regardless of what the UI offers.
- **A plan's `white-space:pre-wrap` details div needs `{%- -%}` whitespace-trimmed Jinja tags (Ganesh, 2026-08-22 bugfix)** — same reason the task log table's own details/edit toggle already uses `{%- if -%}`/`{%- endif %}` around it (`today.html`, both `<td style="white-space:pre-wrap">` and the plan-item's `<div class="small" style="white-space:pre-wrap">`): this app's Jinja env doesn't set `trim_blocks`/`lstrip_blocks`, so an untrimmed `{% if %}`/`{% endif %}` on its own source line leaves a literal newline+indentation text node in the output, which `pre-wrap` then renders as a visible blank line. Any *new* `white-space:pre-wrap` block with a conditional inside it needs the same `-%}`/`{%-` treatment or it'll show the same phantom gap.
- **Free-text `details` gets its first letter capitalized on save, org-wide scope deliberately not attempted (Ganesh, 2026-08-22)** — `util.capitalize_first()` (distinct from `normalize_title_case()`, which Title-Cases every word and exists only for short Project/Task suggestion names), applied at every place `TaskEntry.details`/`PlannedTask.details` actually gets persisted: `add_entry`, the entry-details edit route, `_finish_task_timer` (covers both Auto time capture's Stop and every Plan Pause/Stop segment, since they share this one function), `add_plan`, `edit_plan`. Deliberately NOT applied to the intermediate `ActiveTaskTimer.details` at Start time or Stop's optional top-up — both eventually flow through `_finish_task_timer`'s own call, so capitalizing earlier would just be redundant. Scope was explicitly asked and answered narrow (AskUserQuestion, 2026-08-22): this does not touch status badges, nav labels, or any other lowercase microcopy site-wide — only the "today" label in the date dropdown/page header on Today, and these 5 details-saving call sites. **Per-line, not just the string's first character (Ganesh, 2026-08-22 bugfix, same day)** — real usage turned out to almost always be a manually-typed numbered list with no space after the number (`"1.need to work on X\n2.reviewed Y"`), so capitalizing only index 0 capitalized the leading `"1"` (a no-op) and left every actual word lowercase — the feature looked like it did nothing. `capitalize_first()` now uppercases the first `[a-zA-Z]` character of *each* line independently (a line with no letters at all is left untouched), so `"1.need to work"` → `"1.Need to work"`. This only takes effect on save — it's not retroactive, so already-submitted rows (locked days) keep whatever casing they were saved with unless an admin re-edits them (admins bypass the today-only/lock rule on the details edit toggle).
- **Time by Project/Task gained a "By Project" breakdown + pie chart (Ganesh, 2026-08-24)** — `reports.time_by_project_report()` is a companion to `time_by_activity_report()` on the same `/admin/reports/time` page, same filter semantics (department/employee/project/task, same date range), but grouped the other way: per project, total time and its split across tasks worked on within it. Deliberately does NOT include zero-minute projects the way the employee table includes zero-minute employees — there are ~300 Project/Employer rows org-wide (mostly client names), so an all-zero row here would be noise, not signal (an all-zero *employee* row is signal precisely because there are only ~45 of them). The "Pie chart" button (top-right of the By Project card) toggles a hand-rolled inline SVG pie chart — no charting library pulled in, matching every other bit of page-specific JS in this app (`msfilter.js`, `mdy_datepicker.js`) being small and dependency-free. Caps to the top 8 projects by time + an "Other" bucket for the rest, using a new fixed 9-color qualitative palette (`PIE_COLORS` in the template) — deliberately NOT the app's semantic status colors (`--ok`/`--warn`/`--bad`/...), since those mean something specific elsewhere and reusing them for arbitrary project names would be misleading. `reports.time_by_project_report()`'s return also includes `chart_data` (a plain JSON-safe `[{name, minutes}, ...]` list, already sorted busiest-first) specifically so the template's `|tojson` doesn't need to serialize ORM objects. On-slice text labels were tried the same day and then explicitly reverted (Ganesh: "no please remove text") — slice names live only in the legend and each arc's `<title>` hover, not drawn inside the wedge. `pieEscape()` in `renderProjectPie()` (`admin/reports_time.html`) stayed from that attempt and is still worth keeping — it HTML-escapes every project name used in markup (legend + arc `<title>`), since names are free-typed by whoever created the Project/Employer row (same reasoning as Jinja's autoescaping, just done by hand because this string is assembled in JS).
- **Compensation match requests weren't notifying admins, and looked identical to approved links (Ganesh, 2026-08-22 bugfix)** — three related gaps, all display/notification/placement-only, no engine.py changes. (1) `_admin_nav_badges()` (`app/templating.py`) never counted pending `CompensationLink` requests (`status='requested', requested_by_employee=True`, created by `POST /leave/match-request`) at all, so a super admin had no signal one existed. Now folded into `pending_overtime` via `_pending_complink_count()` — deliberately NOT added for a department-scoped admin, since `approve_complink`/`reject_complink` are both `require_super_admin` and a Team Lead has no route that can act on one anyway. (2) Overtime Management's and Person Detail's "Compensation links" tables showed every link — pending, approved, or rejected — through the same fully/partial coverage badge (`lk.fully_compensated`), which for a still-`requested` link is just its unset default, since `engine.evaluate_link()` (the function that actually computes coverage) only ever runs from `approve_complink()`, never at request time. That made an unreviewed request look exactly like an already-approved, already-evaluated one. Both tables now show `status`-aware badges instead: "pending approval" (+ a link to `/admin/leave` to act on it) for `requested`, "rejected" for `rejected`, fully/partial only for anything else (i.e. approved). (3) The pending-decision card itself ("Overtime ↔ Missed Hours match requests", Approve/Reject forms posting to `/admin/complink/{id}/approve|reject`) moved from Leave Management to Overtime Management the same day (Ganesh: "this should be not in leave management it should be in overtime management under pending requests") — same `CompensationLink` rows/routes, just relocated in `overtime_page()`/`overtime.html` instead of `leave_page()`/`leave.html`, sitting right after the existing OvertimeApproval "Pending requests" card (renamed "Pending overtime requests" the same day to disambiguate from the new card, both instances) and gated the same `scope is None` (super-admin-only) way. The "review →" links on the Compensation links tables (Overtime Management, Person Detail) were updated from `/admin/leave` to `/admin/overtime` to match. (4) The matching employee-facing self-service card ("Request an Overtime ↔ Missed Hours match", `POST /leave/match-request`) moved the same way — from `leave.html`/`my_leave()` to `overtime.html`/`my_overtime()` — same route path, same `CompensationLink` creation logic, just relocated + its post-submit redirect changed from `/leave` to `/overtime`. The underlying compliance math was never wrong — a pending request was never silently marked `DayStatus.compensated` — this was purely an admin-visibility/placement gap.
- **An already-linked shortfall day stayed pickable in the Shortfall day dropdown (Ganesh, 2026-08-25 bugfix)** — the quick-link forms on Person Detail and Overtime Management (`_shortfalls_surpluses()`, `app/routes/admin.py`) never excluded or flagged a day that already had a `CompensationLink`, so re-picking it and submitting only failed via `add_complink`'s *surplus*-side clash check ("Surplus day(s) already linked to another shortfall") once its surplus day(s) had already been consumed by the first link — a confusing error that didn't say the shortfall itself was the real reason. Display-only fix, no `engine.py`/`validation.py` change: a new `_shortfall_links_by_date(db, employee_id)` helper returns every date this employee already has a link for; both `person()` and `overtime_page()` pass it to their templates as `already_linked_shortfalls`/`link_already_linked_shortfalls`, and `person.html`/`overtime.html` render that option disabled + struck-through + "✓ … already linked" instead of silently letting it be picked again. Overtime Management's Link button disabled-state also now checks `link_has_unlinked_shortfall` (at least one shortfall NOT already linked) instead of just `link_shortfalls` being non-empty, so a person whose only shortfall day is already linked correctly shows "No shortfall days left to link" rather than an enabled button that would just hit the same clash error.
- **Compensation links now allocate by the MINUTE, not the whole day (Ganesh, 2026-08-25)** — before this, linking consumed a surplus day's *entire* variance and blocked it from ever being reused, even if the shortfall only needed part of it, and a shortfall day could only ever be targeted by one link. Ganesh's own example: an 8-hour shortfall with a surplus day worth 10 hours should leave that day with 2 hours still available afterward; three 2-hour surplus days linked against an 8-hour shortfall should leave the shortfall at -2:00 (not silently treated as done, and not rejected outright) so a later link can finish it off. New nullable `CompensationLink.surplus_minutes` column (`app/models.py`, JSON `{"YYYY-MM-DD": minutes}` — how many minutes of each surplus day THIS link actually used, not just which days). Reading side lives in `app/engine.py`: `_link_allocated_minutes()` (one link's own total, with an old-format fallback — a pre-2026-08-25 link has no `surplus_minutes`, so it's read as having consumed each `surplus_dates` day in full, which is the correct frozen reading for it, not a gap to backfill), `shortfall_allocated_minutes()`/`shortfall_allocated_minutes_by_date()` (sum across every link that targets a given shortfall day — a day can now be covered by more than one partial link), `surplus_minutes_used_by_date()` (sum across every link for a given surplus day). Writing side: `allocate_surplus_minutes()` greedily consumes ticked surplus days oldest-first until the shortfall's remaining deficit is covered, skipping (not erroring on) a day with nothing left — shared by both `add_complink()` (admin-direct, `app/routes/admin.py`) and `request_compensation_match()` (employee self-service, `app/routes/employee.py`) so the two flows can't drift apart; `approve_complink()` doesn't need its own allocation step since the employee's request already computed and stored it at submission time. `evaluate_link()` was split into two related but distinct signals: `link.fully_compensated` (did THIS link alone cover the whole deficit — drives that link's own row badge and flash message) vs. `DayStatus.compensated` (the day's TRUE aggregate across every link targeting it — drives strike erasure via `comp_erases_strike`, and is what actually matters when a second link finishes off a day an earlier partial link started). `compensated_dates()` (read by `recompute_employee()` on every regular recompute pass — i.e. on most page loads) was rewritten the same way; missing this would have meant a day fully covered by two partial links got silently un-flagged the next time anyone loaded a page for that employee. The Shortfall day / Surplus day pickers on Person Detail, Overtime Management, and the employee-facing Overtime page all show remaining balances now (e.g. "2:00 remaining of 8:00 — partially linked") instead of a binary already-linked/not-linked state. No `verify_strikes` confirmation possible here either (same missing-legacy-bundle gap as Leave V2) — verified instead via a standalone Python simulation of the allocation algorithm (exact match on both of Ganesh's worked examples plus edge cases: exact-match, under-supply, a second link finishing a partial day, order-sensitive greedy allocation, skipping an exhausted day) and a real-SQLite integration check against hand-seeded `imported`-source test rows in `tms_demo.db` (Alok Gupta 08/06, Tanvi Banerjee 08/08) reproducing both examples' numbers exactly.
- **Today page cards got a depth pass, Pause/Stop swapped colors (Ganesh, 2026-08-22)** — `.today-page .card` now carries a soft `rgba(7,41,38,..)` box-shadow (same brand-dark tint every dropdown/popover already shadows with, just softer) and a touch more `margin-bottom` (8px → 10px), since the earlier compacting pass (padding/margin tightened, see the comment above it) had made every section blur into one wall — border color alone wasn't enough contrast against the page background. `button.warn` (Pause, on both the Today's Plan card and the Auto time capture card) now uses `--accent-light`/`--brand-dark`, the exact pair `.topbar nav a.on` already uses for the active "Today" nav tab; `button.danger` (Stop) took over Pause's old `--warn` olive/brown. Both classes are only ever used on these 4 buttons in `today.html` (confirmed via grep before changing them), so this was safe to change at the shared CSS-class level rather than needing new one-off classes.
- **Dashboard readability pass (Ganesh, 2026-08-22)** — template/CSS-only, no engine/validation changes. "Needs attention" groups its three row types (leave requests, support tickets, strikes & shortfalls) under small count-labeled headers instead of one flat list of same-looking pills, and bolds the strike/shortfall count per row — the list was already sorted worst-first (`admin.dashboard()`'s `violations.sort()`, unchanged), this just makes that ordering visible. Department tiles: "All departments" always gets the accent treatment (`deptcard.all`) since it's the primary entry point, and any tile with `dept_present.get(d, 0) == 0` gets dimmed (`deptcard.zero`, full opacity on hover) so a glance at the grid shows which teams have logged in today. Recent activity now runs `AuditLog.action` through `util.humanize_audit_action()` (new Jinja global) instead of `action.replace('_',' ')` + the raw entity class name — e.g. "config change · Config" reads as "Updated config". Deliberately scoped to the Dashboard's preview widget only; `/admin/audit`'s full log keeps raw action codes since that page is a searchable technical trail, not a summary — don't route it through the same helper. The "On leave today" KPI card gets `--leave` color (`kpi.leavek`) when non-zero, matching the accent scheme "Logged"/"Not yet logged" already had (`kpi.okk`/`kpi.badk`). `humanize_audit_action()`'s dict isn't required to track every `audit()` call site — an unmapped action code falls back to the same title-cased text the Dashboard showed before this existed.
- **Past days show a read-only "Plan" card (Ganesh, 2026-08-22)** — the interactive "Plan for the day"/"Today's Plan" section in `today.html` still only renders `if day == today` (a `PlannedTask` only ever carries the date it was added on — see the comment on `_day_context`'s `plans` query), but browsing a past day via the date dropdown no longer shows nothing: `_day_context` now also returns `past_plans` (same query, just `date != today_local()`, no branch shared with `plans`) and `today.html` renders it in its own `{% if day != today and past_plans %}` card, reusing the exact same `plan-item plan-{{ p.status }}` markup/badge classes as the live card for visual consistency but with every Start/Pause/Stop/edit/delete form stripped out — it's read-only context ("what was planned/left running/done that day"), not something this page can act on for a past date. No new CSS, no engine/validation changes.
- **Suggestions and Overtime Management got read-only "quick view" additions (Ganesh, 2026-08-21)** — neither touches `engine.py`/`validation.py`. `/admin/suggestions` now shows an **Approved** table under each Pending one (`admin._approved_suggestions()`), filtered to `status == LIST_APPROVED and created_by_employee_id is not None` — that second condition is what separates an actual approved suggestion from a Project/TaskType an admin added directly via Lists (those also land on `LIST_APPROVED`, just with no submitter, since that's Project/TaskType's own default — see their docstrings). `/admin/overtime` gained a collapsed-by-default **Compensation links** card (every `CompensationLink` in the same `led_by()` scope as the rest of the page — previously only ever visible one employee at a time via Person Detail) and a monthly **Who worked overtime** table, originally backed by `reports.attendance_report()`'s Punch-In/Out-based `overtime_minutes` figures. Switched (Ganesh, 2026-08-25: "we are considering only based on task log times, so we dont need punch in punch out time overtime") to a new `reports.task_log_overtime_report()` — sums positive `DayStatus.variance_minutes` per employee instead, the same task-log "surplus" figure the Compensation Links picker above already sums, deliberately kept separate from `attendance_report()`'s own Punch-based Overtime column rather than changing that function (two genuinely different, still-valid measures — only this card switched which one it shows). Also moved the same day to the very last card on the page, after the searchable "Pending overtime requests" card, per the same instruction ("keep that section at last after pending overtime requests"). The Compensation links card also has its own employee picker + shortfall/surplus preview so a link can be *created* without leaving Overtime Management — it posts to the same `POST /admin/person/{id}/complink` Person Detail's form already used, just with a hidden `return_to=overtime` field so `_complink_redirect()` sends the admin back here (with the same employee/month still selected) instead of to Person Detail. Deleting a link is still Person Detail-only. `_shortfalls_surpluses()` (pulled out of `person()`) is the shared piece both routes call — change it once, not twice.
- **Unplanned Time got a real 40-hour/year cap; compensation-match window tightened to same-calendar-month only (Ganesh, 2026-08-27, both from a pasted policy clarification from management)** — two independent `engine.py`/`Config` changes, both under the pytest+verify_strikes hard rule (still unavailable in this sandbox — see the missing-legacy-bundle note; verified instead via standalone Python simulations, listed below). (1) New `Config.unplanned_hours_year_cap` (`CONFIG_DEFAULTS` in `app/models.py`, default `"40"`, hours/year — not exposed on `/admin/config`, same as `probation_days_default`/`planned_days_year_*` before it, DB-default-only unless someone writes a `Config` row directly). `engine.leave_balance_v2()` now gives `LEAVE_UNPLANNED` a real `entitlement` (`cfg_int(cfg, "unplanned_hours_year_cap") * 60`) instead of `None` — this reverses what its own docstring said as recently as this same week ("Unplanned... has no pool to run out of"), which was correct until this policy clarification arrived. Unlike Planned Time's entitlement (a running tenure total, never reset — see `planned_time_accrued_minutes_pure`'s docstring), Unplanned's cap resets every calendar year: the same used/pending loop in `leave_balance_v2()` now skips any `LeaveRecord` whose `start_date.year != as_of.year` for this one type only, so a request from a prior (or future) year never counts against the current year's pool. No "borrow from next year" mechanism was added — once exhausted, `remaining` just goes negative like Planned Time already could; nothing new blocks the request itself, matching Planned Time's existing precedent (balance is informational, the admin still approves/rejects using judgment — see `request_leave()`, unchanged). Unpaid/Bereavement Time are untouched, still uncapped (`entitlement=None`) per the same policy clarification ("no available leave... they can request Unpaid Leave" implies Unpaid itself has no ceiling to hit). (2) `engine.compensation_window_ok()` — previously either/or (within 21 calendar days OR same calendar month, its own docstring flagging this as an unconfirmed guess at the source PDF's "3-week/same-calendar-month window" wording) — is now strictly same-calendar-month only, per the clarification's explicit "only... within the same calendar month." The 21-day allowance is gone outright, which matters most at month boundaries (e.g. missed hours 07/31, made up 08/02 — 2 days apart, used to pass under the old 21-day rule, now correctly fails). Only `approve_complink()` (`app/routes/admin.py`) calls this function — by design: it's the server-side re-check on the employee self-service match-request flow (`request_compensation_match()`), not on `add_complink()`'s admin-direct linking, which stays admin discretion with no window restriction (unchanged, not an oversight — see the function's own docstring). The employee-facing picker (`my_overtime()` in `app/routes/employee.py`) already only ever shows shortfall/surplus days from one calendar month at a time (the 2026-08-25 month-navigation fix), so this tightening didn't need any template/picker change to enforce itself in the UI — it only changes what happens if an employee's request (or an admin retroactively editing dates) tries to cross a month boundary. Updated the two remaining pieces of UI copy that still described the old 21-day/3-week wording: `overtime.html`'s match-request card description and `admin/overtime.html`'s "Approving re-checks..." note. Verified via two standalone Python simulations (no sqlalchemy/pytest access, same gap as every other `engine.py` change this session): `compensation_window_ok()` checked against 6 cases including both month-boundary edge cases that the old OR-logic would have gotten wrong; the year-scoped Unplanned balance math checked against exactly-exhausted, over-cap-pending (negative remaining, not blocked), and cross-year-reset cases. No `tms_demo.db` reseed needed — `Config` values are a plain dict merge (`engine.get_config()`) over `CONFIG_DEFAULTS`, not a mapped column, so there's no SQLite `ADD COLUMN` backfill gap to work around here (unlike the `CompensationLink.surplus_minutes` column two bullets up).
- **Project-scoped tasks (Ganesh, 2026-08-27)** — before this, `TaskType` was one flat global list shared by every `Project`/Employer with zero relation between them (`TaskEntry` always carried both `project_id` AND `task_type_id` as two fully independent FKs), so the Today page's Task dropdown showed the exact same ~38 tasks no matter which project was picked, and there was no way to give one client's work a different task list than another's. New many-to-many join table `ProjectTask` (`app/models.py`, `UniqueConstraint("project_id", "task_type_id")`) fixes this — deliberately **enforced**, not advisory like `ProjectAssignment`/`TaskAssignment` above: `validation.task_allowed_for_project(db, project_id, task_type_id)` is the one rule both `validate_entry()` (the real gate for every `TaskEntry`, via `add_entry`/`_finish_task_timer`) and `add_plan()` (immediate feedback before a plan is even Started) call. Its rule is deliberately asymmetric and backward-compatible: a task with **zero** `ProjectTask` rows is unrestricted — pickable under every project, exactly like every pre-existing task/project combo already in the DB, since none of them have any links yet — while a task with **one or more** links is restricted to exactly those projects. This is a one-way narrowing an admin opts into per task (Lists page's new per-task "manage" panel, `POST /admin/lists/task/{id}/projects`, replace-all checkboxes same pattern as `assignments_save()`), never a migration this feature runs on its own — nobody could guess which of ~38 tasks belongs to which of ~300 real projects except the admin. Creating a task now always requires picking its (first) project — both the Lists page's quick-add form (`lists_add()`) and an employee's "Suggest a new task" (`suggest_list_item()`, `app/routes/employee.py` — the suggested `TaskType` and its `ProjectTask` link are both created at suggestion time, even while still pending; approving only flips `TaskType.status`, never touches the link) create exactly one link immediately; the same task can be linked to more projects later via the Lists page's manage panel. `combo.js` gained `initProjectTaskCombo()` (wraps two paired `initCombo()`s — an `onSelect` callback on the Project combo re-filters the Task combo's pool via a new `setItems()` method the returned handle exposes) so `today.html`'s 3 independent Project+Task pairs (Add Row, Auto time capture timer, Plan for the Day) all filter live as soon as a project is picked; each task item now carries a `project_ids` key (`null` = unrestricted) from `_combo_items()`/`_task_project_links()` in `app/routes/employee.py`. Also touched `combo.js`'s form-submit validation: a combo whose root is currently `.hidden` is no longer required, needed so the "Suggest a new task" form's Project picker (only shown/required when Type=Task, toggled by plain JS) doesn't block suggesting a *project*. Tasks bulk-upload (`app/lists_bulk_upload.py`) changed shape from one column to two — **Task Name + Project Name, one row per task+project pair** (not one row per task — to link a task to 3 projects, list it on 3 rows), since that's the simplest parse consistent with this sheet's existing add-only convention; a Project Name that doesn't exactly match an existing Project is skipped with a reason rather than silently creating one (this sheet only ever adds Tasks/links, never Projects, even from the Tasks sheet). Both the sample template and the existing-values download now also include a **Projects** reference sheet — inserted before Instructions specifically so an admin can cross-check exact spelling before uploading (Ganesh: "before instructions sub sheet, we should have existed projects list sheet") — real project names via a new optional `db` param on `build_sample_workbook()`, not placeholders. New table needs no manual migration — `init_db()`'s `Base.metadata.create_all(engine)` creates any missing table automatically on next startup; only *new columns on an existing table* need `_add_missing_columns()`'s backfill, which doesn't apply here. No `pytest`/`verify_strikes` run possible (same missing-legacy-bundle gap as ever) despite `validation.py` changing — verified instead via: `py_compile`/Jinja-parse on every changed file, a Node-run trace of `combo.js`'s exact project→task filter predicate against 3 worked cases (unrestricted task always included, project-1-only task, project-2-only task), a standalone Python mirror of `_process_task_upload()`'s dedup/skip algorithm against 8 worked rows (new task linked to 2 projects creates the `TaskType` exactly once; already-linked pair skipped; unknown project skipped; missing cells skipped), and new/updated tests in `tests/test_validation.py` (`TestProjectScopedTasks`) and `tests/test_lists_bulk_upload.py` (`TestReadTaskRows`, `TestProcessTaskUpload`) that need a real `pytest` run to confirm, not just `py_compile`, before calling this fully done.
- **Bug fix: "Something went wrong" on Projects & Tasks -> Add, plus new Rename (Norine via Teams, 2026-08-27)** — `util.audit()` now truncates `actor`/`action`/`entity`/`entity_id` to their column widths (120/60/60/60) before insert. Root cause: several call sites (`lists_add`, `lists_toggle`, `suggestion_approve`, `suggestion_edit`, and now `lists_rename`) pass a raw admin/employee-typed name straight through as `entity_id` (`AuditLog.entity_id` is `VARCHAR(60)`) with no length check. SQLite (local dev) doesn't enforce VARCHAR length at all, so this was invisible there; production is Postgres (see the GCP deploy notes below), which does enforce it and throws — landing on `main.py`'s catch-all `Exception` handler, i.e. exactly this generic error page — the instant a name runs past 60 characters. The underlying row (Project/TaskType/etc.) had already `db.commit()`ed one line earlier in every affected route, which is why Norine saw the error yet the project was still added. Fixed once, centrally, in `audit()` itself rather than at each call site, so any future call site gets the same protection automatically. Also added `POST /admin/lists/{kind}/{item_id}/rename` (SuperAdmin-only, same gating as every other Lists route) since the *reason* she hit Deactivate on a mistyped project instead of fixing it is that there was no rename option — a separate route from `suggestion_edit()` above (that one is scoped to still-pending suggestions only and 403s outside an admin's department); this one works on any Project/TaskType regardless of status or origin, reusing the same `original_name`/`edited_by`/`edited_at`/`employee_notified_at` columns those suggestion routes already added — so renaming something that happens to be an approved former employee suggestion still fires the existing "an admin rewrote your suggestion" Today-page banner via `_pending_edit_notices()`, for free. Renaming is safe on a live value since every relationship (`TaskEntry`, `ProjectTask`, etc.) is keyed by id, never by name.
- **Employee-initiated Unlock requests (Ganesh, 2026-08-27)** — before this, the only way to ask an admin to unlock a locked day was outside the app entirely (Teams/email/in person), and an admin had no signal one was needed. New `UnlockRequest` table (`app/models.py`) reuses the generic `LEAVE_REQUESTED`/`LEAVE_APPROVED`/`LEAVE_REJECTED` status strings, same reuse-not-a-new-enum precedent `CompensationLink` already set. Employee side: the locked-day banner on `today.html` now has a "Request unlock" mini-form (note optional — AskUserQuestion, 2026-08-27, lowest friction) that posts to `POST /unlock-request`; once one's pending, the banner shows "⏳ Unlock requested" instead and the form disappears until it's resolved. This is deliberately a notification/queue layer in front of the EXISTING unlock mechanism, not a second way to unlock a day: `unlock_day()` (`app/routes/admin.py`) is otherwise unchanged and is still the only thing that ever flips `DaySubmission.locked` — it just also auto-resolves any pending request for that employee+date to `LEAVE_APPROVED` the moment an admin unlocks, whether they arrived via the request or unlocked directly without noticing one existed, so the queue can never disagree with what actually happened. A super admin can also decline a request without unlocking (`POST /admin/unlock-request/{id}/reject`, `reject_unlock_request()`) — day stays locked, request just stops showing as pending. Admin visibility: a new "Unlock requests" group on the Dashboard's "Needs attention" card (super-admin-only preview, same scoping as Support tickets/Audit Log there, since only a super admin can act on one), and inline on Person Detail right next to that day's existing "unlock" control — the employee's own note shows there too, so the admin has context before typing their unlock reason.
- **"By Project" pie chart colors lightened (Ganesh, 2026-08-27)** — same 9-hue qualitative palette from its original build, same hue order, just each color lifted toward pastel (`PIE_COLORS` in `admin/reports_time.html`). Legend/hover-title text color comes from the page's own CSS, not the fill colors, so this was a pure swap with no readability side effects to check.
- **Department-scoped admin ("Team Lead") access narrowed to 5 capabilities (Ganesh, 2026-08-28)** — before this, `is_admin=True` alone (department-scoped, as opposed to `is_super_admin=True`, org-wide) reached nearly every admin screen, department-filtered. Ganesh's exact list: add Project/Task names, assign Projects/Tasks to team members, approve suggested Project/Task names from team members, view task logs for team members, view Time/Project/Strikes/Attendance reports for team members — everything else moved to Super-Admin-only. Two follow-up calls (both AskUserQuestion, both answered "Remove it — Super Admin only"): Leave Management and Overtime Management, previously department-scoped, are now gated Super-Admin-only end to end — `leave_page`/`leave_approve`/`leave_reject`/`leave_add`/`leave_delete` and `overtime_page`/`overtime_approve`/`overtime_reject`/`overtime_grant`/`overtime_delete` (`app/routes/admin.py`) all switched from `Depends(require_admin)` to `Depends(require_super_admin)`; their nav links moved inside `base.html`'s existing `{% if user.is_super_admin %}` block (previously wrapping only Roster/Settings/Support). A third call ("Add form + Bulk Upload") narrowed rather than removed: Projects & Tasks moved the other direction — `lists_page`/`lists_add`/`lists_bulk_upload_page`/`lists_bulk_upload_sample`/`lists_bulk_upload_existing`/`lists_bulk_upload_post` switched from `Depends(require_super_admin)` to `Depends(require_admin)`, so a department admin now gets the Lists page, its Add form, and full Bulk Upload — but `lists_toggle` (Deactivate/Reactivate), `lists_rename`, and `lists_task_projects` (a task's "manage projects" linking panel — not explicitly asked about, kept on the conservative/narrow side since the ask was specifically "Add form + Bulk Upload") all stay `require_super_admin`, unchanged. `admin/lists.html` had zero `is_super_admin` guards around its Rename/Deactivate/Reactivate/manage-projects UI before this (harmless while the whole page was super-admin-only) — now that a department admin can reach the page, those three write actions are wrapped in `{% if user.is_super_admin %}` (manage-projects panel falls back to a plain read-only project count) so a department admin doesn't see buttons that 403 on click. Same latent-bug pattern existed on `admin/person.html` — Override, Compensation-link add/delete, Unlock, Reject-unlock-request, and the compensation-link "review →" link to `/admin/overtime` were all rendered unconditionally even though their routes were already `require_super_admin` (confirmed via grep, none of these 5 routes needed a gating change) — now wrapped the same way, so `person()` staying `require_admin` (matches "view task logs") gives a department admin a clean read-only Task log instead of dead-end buttons. Dashboard's "Needs attention" `pending_leave_rows` query (`app/routes/admin.py`'s `dashboard()`) moved from "always computed, then filtered by scope" to "only computed when `scope is None`" — showing a department admin a "leave requests need attention" group they have no route left to act on would be a dead end, not a signal (existing `{% if pending_leave_rows %}` guard in `admin/dashboard.html` needed no change, an empty list already renders nothing). `_admin_nav_badges()` (`app/templating.py`) dropped `pending_leave`/`pending_overtime` from the department-scoped branch's return dict entirely (those nav links no longer exist for them) and the now-unused `led_by` import was removed. `require_super_admin()`'s docstring (`app/auth.py`) and the `is_admin`/`is_super_admin` column comments (`app/models.py`) were rewritten to state the new boundary explicitly rather than the pre-2026-08-28 screen list. No `app/engine.py`/`app/validation.py`/`legacy/import_legacy.py` changes, so the pytest+verify_strikes hard rule doesn't gate this one — verified instead via `py_compile` across every touched `.py` file and a Jinja `Environment(...).parse()` check across every touched template.
- **Case Type / Client field (Ganesh, 2026-08-28)** — new `Project.is_case_type` boolean flag (`app/models.py`), admin-set per project, NOT a rename of Project/Employer and NOT a separate dropdown: most of the ~300 existing Project rows aren't case work (Internal, Admin, Training, etc.), so an admin marks the ones that are. Settable at creation via the Lists "Add" form (`is_case_type` checkbox, both admin tiers — matches the "Add Project/Task names" access item), toggle-able afterward on a live project via `POST /admin/lists/project/{id}/case-type/toggle` (`lists_project_case_type_toggle`, Super-Admin-only, same tier as Rename/Deactivate). The moment an employee picks a case-type project on the Today page (Add Row, Auto time capture Start, or Plan for the Day — all three share the same Project combo via `initProjectTaskCombo`), a Client text field appears and becomes required — one freeform field, not split into company/individual (Ganesh's own call: "one free-text field"), for either a person's name or "Company Name - Beneficiary Name". `combo.js`'s `initProjectTaskCombo()` gained two optional trailing params (`clientWrap`, `preselectClientValue`) that show/hide + toggle `required` on the paired Client input based on the selected project's `is_case_type` — same "a script that hides a field clears its value" convention the Suggest-a-new-task Project picker already established. Required-ness itself (`_client_required_error()` in `app/routes/employee.py`) is enforced at every point Client can first be set — `add_entry`, `start_task_timer`, `add_plan` — plus defense-in-depth inside `_finish_task_timer()` (mirrors the multi-layer precedent `task_allowed_for_project()` already set). Deliberately a plain route-level helper, NOT added to `app/validation.py`'s `validate_entry()` — this is a simple presence check tied to which project was picked, not a PRD §4 entry rule like overlap/gap/cap/backdate, so this feature never touches `engine.py`/`validation.py` and isn't gated by the pytest+verify_strikes hard rule (verified instead via `py_compile`, a Jinja parse across every touched template, `node -c` on `combo.js`, and standalone Python/Node simulations of `_client_required_error()` and the show/hide/required-toggle logic). New `client` column added to `TaskEntry`, `ActiveTaskTimer`, and `PlannedTask` (all `Text, default=""`, same additive-migration/NULL-is-falsy reasoning as `Employee.is_developer` — no dedicated backfill needed), carried through the exact same path `details` already travels: `PlannedTask.client` → copied into `ActiveTaskTimer.client` when Start/Resume opens a segment → copied into `TaskEntry.client` when `_finish_task_timer()` closes it. Not editable after creation (same as Project/Task themselves — delete-and-re-add is the path for a wrong Client); shown read-only on Today's own task log and plan items, My Month's per-day expand, and admin Person Detail's Task log. Out of scope for this pass, callable out if wanted later: Projects bulk-upload has no Case Type column yet, and Time/Project reports don't break out Client. **Bug fix, same day** — the new Client field showed up on Add Row even for a project NOT marked Case Type (Ganesh caught it live: "Cedarline Consulting LLC" wasn't marked, yet Client still appeared). Root cause was a pre-existing CSS gap, not the show/hide JS: `.formrow > div { display: flex; ... }` (`app.css`) is an author-stylesheet rule, and author rules always beat the browser's built-in `[hidden] { display: none }` UA rule regardless of specificity — so a plain `<div hidden>` direct child of any `.formrow` never actually disappeared, it just kept rendering as an empty flex item with `hidden` reduced to a no-op attribute. Fixed with one added rule, `.formrow > div[hidden] { display: none; }`, right after the existing `.formrow > div` rule — high enough specificity to win. This also silently fixes a second, older instance of the identical bug: the "Suggest a new task" Project picker (`#combo-suggest-project`, 2026-08-27) had the same latent issue and simply never surfaced because it sits inside a collapsed `<details>`. No JS/Python changed for this fix — CSS-only, verified by re-reading the rule's cascade order rather than a runtime check (no live server in this sandbox — see the missing-legacy-data/no-pytest memory notes).
- **Combo picker (Project/Task) couldn't be reselected without clearing the text first (Ganesh, 2026-08-28 bugfix, same day)** — caught while testing Case Type/Client: picking "Bluepeak Systems LLC" then trying to click the field again to pick a different project did nothing; only deleting the typed text first reopened the list. Root cause in `combo.js`'s shared `initCombo()` (used by every Project/Task combo across Add Row, Auto time capture, Plan for the Day, and admin Lists): the dropdown only reopens on a `focus` event or an `input` (typing) event, but `select()`'s mousedown handler deliberately calls `preventDefault()` so picking an option doesn't blur the input — meaning the input stays focused after a selection, so a follow-up click never fires a NEW `focus` event and the menu stays hidden. Fixed by adding a plain `click` listener that reopens the full list the same way `focus` already does (factored into one shared `showAll()` function both listeners call) — a click can now always reopen the dropdown regardless of whether the field already holds a confirmed value, matching normal combobox behavior. Applies everywhere `initCombo`/`initProjectTaskCombo` are used since it's the one shared widget, not a per-page fix.
- **`TaskEntry.entry_method` tracks usage from 2026-08-21 onward only** — one of `m.ENTRY_METHODS` (`plan`/`auto_timer`/`manual_add`), stamped at each of the 3 row-creation sites in `app/routes/employee.py` (`add_entry`, `_finish_task_timer`), NULL on every older/imported row. `app/reports.py`'s `feature_usage_report()` (backing `/admin/reports/usage`, the Developer Usage Report) must keep treating NULL as excluded from every method's count, never folded into `manual_add` — don't "clean up" old NULLs by backfilling a guess, they're correctly unknown. Gated by `require_developer_or_admin` (`app/auth.py`), not `require_admin` — a plain-employee `is_developer` can reach it without being an admin.
- **Department-scoped Projects (Ganesh, 2026-08-28)** — before this, every active Project/Employer (and every task under it, per the Project-scoped Tasks rule above) was pickable by any employee regardless of department, with no way to give one department's client work its own project list. New many-to-many join table `ProjectDepartment` (`app/models.py`, `UniqueConstraint("project_id", "department")`) fixes this, reusing the exact "zero links = unrestricted, one-or-more links = narrowed to exactly those" convention `ProjectTask` already established — a project with no `ProjectDepartment` rows stays visible to every department (AskUserQuestion, 2026-08-28: "Stay visible to everyone" was the explicit answer, not opt-in-required), and unlike `ProjectTask` (one task → many projects but each *link* is still one task+one project), a project here can span **multiple** departments at once (AskUserQuestion: "A project can span multiple departments", not the single-department alternative). Same dual-layer enforcement precedent as `task_allowed_for_project()`: `validation.project_allowed_for_department(db, project_id, department) -> bool` is the one rule, and `validate_entry()` (the real gate, via `add_entry`/`_finish_task_timer`) and `add_plan()` (immediate feedback before Start) both call it — **this touches `app/validation.py`**, so it falls under the pytest+verify_strikes hard rule below. Deliberately scoped **employee-facing only** (the third AskUserQuestion, answered "Employee-facing only", not the alternative that would have also restricted what a department-scoped admin/Team Lead can see on the Lists page) — a Team Lead's own Lists-page view and Add/Bulk-upload access (see the "Department-scoped admin" bullet above) are completely unaffected; this only changes which projects a regular employee's Today-page combo offers. `app/routes/employee.py`'s `_visible_projects_and_tasks()` filters the `projects` list by `db.employee.department` using a new `_project_department_links(db)` helper (same shape/spirit as `_task_project_links()`); `start_task_timer()` deliberately does NOT get its own department check, since every path into a running timer already goes through `add_entry`/`add_plan`/`_finish_task_timer`, which do. `acting_admin=True` on `validate_entry()` bypasses this the same way it already bypasses the backdate window — no current call site actually passes it, so this is currently dead-but-consistent code, same as before this feature. Admin side: Lists page gained a per-project "Manage departments" panel (Super-Admin-only, `POST /admin/lists/project/{id}/departments`, replace-all checkboxes fed by `reports.departments_list(db)` plus a free-text "other department" box, mirroring the Tasks "Manage projects" panel's own pattern) — `lists_page()` computes `project_department_names`/`all_departments` for it. Projects bulk-upload (`app/lists_bulk_upload.py`) gained an optional second **Department** column — optional at the header level too (unlike the Tasks sheet's required Project Name column), so every pre-existing single-column Projects sheet, including `read_upload_names()`'s own tested shape, still uploads exactly as before; the same one-row-per-pair convention Tasks established applies (list a project on more than one row to link it to more than one department, not a comma-separated cell). New `read_project_rows(wb)` is a superset reader alongside the untouched `read_upload_names()`; `_process_project_upload()` was rewritten to report two independent counts — `added` (new Project rows, same meaning as before this feature) and `department_links_added` (new `ProjectDepartment` rows, separately, since a department link can be added to a project that already existed before this upload) — `admin.py`'s `lists_bulk_upload_post()` now flashes/audits on `anything_added = result["added"] or dept_links_added` so a department-links-only upload no longer falsely shows a red/error flash. Sample template and existing-values download both gained a **Departments** reference sheet (`_add_departments_reference_sheet()`, mirroring the Tasks sheet's own Projects reference sheet) sourced from `reports.departments_list(db)` — the app's existing, non-enum-backed canonical list of in-use department names, reused rather than duplicated a third time. No `pytest`/`verify_strikes` run possible in this sandbox (same missing-legacy-bundle gap as every other `validation.py` change this session) — verified instead via: `py_compile` across every touched `.py` file, a Jinja `Environment(...).parse()` check across every touched template, `read_project_rows()` executed directly (not just hand-traced) against 6 worked cases via a stubbed-import harness (sandbox has no `sqlalchemy`/network access, so `app.models`/`app.reports`/`sqlalchemy` were stubbed only for this parsing function, which doesn't touch the DB), and a careful hand-trace of `_process_project_upload()`'s allocation logic against 7 worked scenarios (new project+department created together; same new project linked to 2 departments in one file, project created once; blank-department row creates an unrestricted project; a department link added to an already-existing project; an already-linked department skipped not duplicated; a duplicate department pair within one file linked once; a plain single-column sheet still uploads exactly as before) — transcribed into real pytest tests in `tests/test_lists_bulk_upload.py` (`TestReadProjectRows`, `TestProcessProjectUploadDepartments`) that still need a real `pytest` run to confirm, not just `py_compile`, before calling this fully done.

- **Auto time capture: max-row auto-split (Ganesh, 2026-08-28)** — before this, a timer left running past `Config.max_row_minutes` (the same §4 4h cap `validate_entry()` enforces on every manually-typed row) just got stuck: Stop & Log failed outright with validate_entry's own "Single row longer than 4h 0m — break the work down" message, and Pause/Stop on a plan-linked timer failed the identical way since both funnel through `_finish_task_timer`. Reported live via screenshot — a timer left running 6h+ with Stop & Log doing nothing. This app has no background scheduler (plain request/response, see the missing-legacy-data/no-pytest sandbox notes), so "automatically" means lazily, on next touch, same lazy-self-heal convention `stop_task_timer`/`cancel_task_timer` already used to keep `PlannedTask.status` in sync (2026-08-22 bugfix). New `_log_timer_as_entry()` (`app/routes/employee.py`) factors the "turn this timer into one validated TaskEntry from start_minute to a given end_minute" step out of `_finish_task_timer`, flushing (not committing — this session is `autoflush=False`, see `app/db.py`) after each `db.add()` so a second chunk logged in the same request sees the first one in its own overlap check. New `_auto_split_timer_if_over_cap()` loops off full cap-length chunks from the FRONT of a timer that's run past the cap — each chunk becomes a real logged `TaskEntry` via `_log_timer_as_entry()` — then advances the SAME `ActiveTaskTimer` row's `start_minute`/`started_at` forward by one cap-length instead of deleting it, so the timer keeps running under its existing id and the live JS clock (which reads `started_at` fresh) naturally reads back near 0:00 on next load — that's the "timer restarts from 00:00 for the same task" behavior asked for, not a separate stop-then-start action. Bounded loop (`(1440 // cap) + 2` iterations) so a misconfigured tiny cap can't spin; stops early (leaving the remainder running, nothing lost) if a chunk's boundary would reach 1440 (no rows span midnight — same existing rule) or if a chunk fails `validate_entry` for an unrelated reason (day got locked mid-run, project deactivated). Called from two places: `_day_context()` (moved to the very top, before `entries` is queried, so a chunk it just logged shows up in that *same* page load's task log/total/gap flags rather than needing a second refresh — every GET /today, i.e. every page load, is what heals a stuck timer even if the employee never clicks anything) and the top of `_finish_task_timer()` itself (so Stop & Log / Pause / Stop-plan / starting-a-new-timer's own auto-finish via `_stop_current_timer_if_any` never hit the "longer than 4h" error in the first place — by the time `_finish_task_timer` computes its own final segment against "now", only the within-cap remainder is left). Deliberately does **not** run from `cancel_task_timer` — Cancel means "this timer was a mistake, discard the whole thing," and retroactively logging cap-length chunks the employee is actively trying to throw away would contradict that. Client-side, `today.html`'s existing epoch-based ticking script for the Auto time capture widget (shared by both plan-linked and ad-hoc timers, since it sits outside that branch) now does a one-time `location.reload()` once its live elapsed crosses the same `max_row_minutes` cap (already exposed to the template) — the actual split still only ever happens server-side on page load, this just makes "next page load" happen on its own for a tab left open past the cap, instead of the employee having to notice and click something. `entry_method` on each auto-split chunk is set the same way `_finish_task_timer` already set it (`ENTRY_METHOD_PLAN` if `timer.planned_task_id`, else `ENTRY_METHOD_AUTO_TIMER`) — a split chunk looks identical to any other auto-captured row in reports. No `app/validation.py` change (every chunk goes through the existing, unchanged `validate_entry()`), so the pytest+verify_strikes hard rule doesn't gate this one — verified instead via `py_compile`/Jinja-parse on both touched files and a standalone Python simulation of `_auto_split_timer_if_over_cap()`'s pure chunk-boundary loop against 6 worked scenarios (the exact screenshot numbers — started 11:53 AM, 6h+ elapsed, cap 240 — split into one logged 4h chunk plus a 2h02m remainder still running; two full chunks after a page left open past 2×cap; several small-cap chunks near end of day; a chunk that would cross midnight, correctly left unsplit; well-within-cap, no split; and a mid-run failure leaving only the already-successful chunks logged).

- **Auto-count logged hours — Missing no longer requires a Submit click (Ganesh, 2026-08-28)** — before this, `engine.compute_day()`'s `actual` minutes came ONLY from `DaySubmission.total_minutes`, a snapshot written the moment Submit Day was clicked; a past day with real `TaskEntry` rows but no Submit Day click still computed as Missing with `actual=0` — a false strike for work that was genuinely logged. Raised as a proposal (pasted verbatim from the drafted message to Ganesh) with 2 open design questions, both answered via AskUserQuestion, 2026-08-28: (1) day-locking stays a **manual "Lock day" click** — same route/UX as the existing Submit Day button (`POST /submit-day`, `submit_day()` in `app/routes/employee.py`, unchanged) — not auto-lock-at-next-day or admin-batch-cutoff; (2) a day only reads **Missing if literally nothing was logged at all** — never just for skipping the Lock/Submit click; a short (non-zero) unlocked day still reads Partial and still strikes, this only fixes the false-Missing-with-real-hours case, never softens an actual shortfall. `compute_day()`'s signature changed: `submitted_total: Optional[int]` → `logged_minutes: int` (always the live sum of that day's `TaskEntry` rows, via a new `day_locked: bool` companion param) — `logged_minutes` is never `None` now, and the old `if submitted_total is None: if d >= today: return None ...` pending/Missing branching collapsed into `if d >= today and not day_locked: return None` followed by `if actual == 0: MISSING`. `day_locked` is the ONE thing lock state still changes: submitting/locking **today** (the normal end-of-day close-out) still computes that day's status immediately rather than waiting for `d < today` — without it, submitting today would wrongly stay "pending" until tomorrow. `recompute_employee()` still fetches `DaySubmission` rows (`subs`, now read only for `.locked`, never `.total_minutes`) and adds a new grouped `task_totals` query (`select(TaskEntry.date, sum(end_minute - start_minute)) ... group_by(TaskEntry.date)`) as the live per-day actual, replacing the snapshot dependency — this also means a locked day's DayStatus now derives from live `TaskEntry` rows rather than the frozen `sub.total_minutes` snapshot (only matters if an admin edits entries on an already-locked day via override; `source='imported'` rows are completely unaffected either way, still skipped outright by `recompute_employee`'s own loop, so `verify_strikes`' 168/168 legacy-fidelity number is not at risk). `today.html`'s totalbar gained one line of copy under Submit Day (only shown `not day_locked and entries`) clarifying hours already count before submitting. This changes `app/engine.py`, so it falls under the pytest+verify_strikes hard rule — same missing-legacy-bundle gap as every other engine.py change this session, so neither could run here. Verified instead via: `py_compile` across every touched file, a Jinja parse across every touched template, and — since `compute_day()` is a pure function with no DB/sqlalchemy calls in its body — its REAL source was extracted from `app/engine.py` and executed directly (not hand-copied/simulated) against every pre-existing `TestStatuses`/`TestLeave`/`TestBreakExcess` scenario in `tests/test_engine.py` (all pass byte-for-byte unchanged) plus 7 new scenarios covering the actual behavior change (logged-but-unlocked full/short/zero days, locked-vs-unlocked "today", and confirming `locked` is irrelevant once `d < today`). New pytest coverage added: `TestAutoCountLoggedHours` (pure `compute_day()`, 6 tests) and `TestRecomputeEmployeeAutoCountsLoggedHours` (real in-memory SQLite, exercises the new `task_totals` grouped query end-to-end, 5 tests) in `tests/test_engine.py` — both still need a real `pytest` run to confirm, not just `py_compile`/the standalone source-execution check, before calling this fully done.

- **Bug fix: Today page stuck "loading" forever, task log/Add Row missing (Ganesh, 2026-08-28, same day as the max-row auto-split feature above)** — reported live via screenshot: browser tab stuck loading, Auto time capture showing 20:25:19 elapsed, nothing rendered below it. Root cause: a timer left running across midnight has `ActiveTaskTimer.date` no longer equal to `today_local()`, which `_auto_split_timer_if_over_cap()` deliberately leaves untouched (documented as a known, not-yet-handled edge case — see its own docstring). The reload-on-cap-crossing script added earlier the same day had no such guard: it fires whenever live elapsed crosses `max_row_minutes`, regardless of WHY the server couldn't shrink it back down — so a timer stuck across midnight reloaded, the server couldn't fix it, the new page immediately saw elapsed still over cap, and reloaded again, forever. The browser never finished receiving one full page, which is why everything below Auto time capture was missing (not a rendering bug — the response was simply cut off mid-stream by the next reload each time). Fixed in `today.html`'s ticking script: `alreadyOverCapOnLoad` is computed once, before `setInterval` starts, from the elapsed at the moment the page loads — if already over cap on load, this tab stops trying to reload at all (same as before the auto-split feature existed: just shows the large elapsed number, no loop). The reload still fires normally for the case it was built for: a timer that was under cap when the page loaded and organically crosses it while the tab stays open. The reload-loop fix alone left the underlying stuck timer itself unfixed — Stop & Log on one still failed (`clamp_break_end` reads the numerically-earlier "now" as having wrapped past midnight, clamping to 1440, producing a same-day span far longer than the cap that `validate_entry` still rejects) — only Cancel (discard, no time logged) could clear it. Raised with Ganesh as an explicit follow-up decision (AskUserQuestion, same day) rather than assumed during the urgent unblock, since recovering the time has real data-loss/timezone-correctness implications — he chose to build it properly rather than accept the Cancel/discard fallback. **Cross-midnight rollover**, same function: before touching "now," `_auto_split_timer_if_over_cap()` first closes out the remainder of the ORIGINAL day — same cap-sized `_log_timer_as_entry()` chunks as the same-day case, just capped at day-end (1440) instead of stopping there — then rolls the SAME `ActiveTaskTimer` row forward to start fresh at 00:00 BUSINESS_TZ the next calendar day (`started_at` recomputed via `dt.datetime.combine(next_day, dt.time(0,0), tzinfo=BUSINESS_TZ).astimezone(UTC)`, not naive timedelta arithmetic, so a DST boundary can't shift it) and repeats — bounded to 7 calendar days so a timer stuck for a very long time doesn't loop indefinitely, that's a sign something else needs a human. Once `timer.date == today`, falls through to the pre-existing same-day loop unchanged. Same "leave it alone and stop, nothing lost" fallback as every other failure path in this function if a chunk fails `validate_entry` (e.g. that old day is now locked) — whatever succeeded before the failure stays committed. No `app/validation.py`/`app/engine.py` change (still routes through the same unchanged `validate_entry()`, same as the original max-row split feature) — verified via `py_compile`, Jinja parse, and a standalone Python simulation of the pure chunk/day-roll control flow against 4 worked scenarios: the exact screenshot numbers (started Thu 3:01 PM, now Fri ~11:26 AM, cap 240 — closes out 3 Thursday chunks including a short 59-min final one, 2 Friday chunks, lands running from Fri 8:00 AM); a 2-day-stuck timer crossing two full midnights; the old day already locked (nothing logged, timer left untouched); and a failure partway through the old day's closeout (first chunk stays committed, timer stops there) — matches this project's own established precedent of `py_compile`+simulation for route-level (non-engine/validation) changes, since `tests/` has no route-level test file for `app/routes/employee.py` to extend.

- **TK-04 — admin/team-lead can assign a Project+Task into an employee's plan (Ganesh, 2026-08-28)** — before this, `PlannedTask` rows could only ever be self-created (`add_plan()`, `created_by_employee_id=user.id` hardcoded); there was no way for an admin to pre-load a task onto someone's day. `PlannedTask.created_by_employee_id` was already on the model from an earlier session specifically for this ticket and had gone unused until now — no schema change needed for the "who assigned it" marker itself, only one small additive column: `PlannedTask.assigned_notified_at` (`app/models.py`, nullable `DateTime`, no backfill needed — `_add_missing_columns()` picks up any new ORM column automatically on next startup, same as every other additive column this session) tracks whether the employee has dismissed the assignment banner, mirroring the `edit_notices`/`_pending_edit_notices()` convention already used for admin-rewrote-your-suggestion banners. Three new department-scoped (`require_admin`, `admin_department_scope()`) routes in `app/routes/admin.py`: `admin_add_plan()` (`POST /admin/person/{emp_id}/plan/add`), `admin_edit_plan()` (`POST /admin/plan/{plan_id}/edit`), `admin_delete_plan()` (`POST /admin/plan/{plan_id}/delete`) — edit/delete both re-check `PlannedTask.status == PLAN_PLANNED` server-side (the ticket's own boundary: "Admin can edit or remove the entry while status is Planned"), and work on **any** still-Planned row in the admin's scope, not just ones that admin personally created — same general-oversight precedent Person Detail's other write actions (Override, Compensation links) already have over an employee's own data, not something newly invented for this feature. `admin_add_plan()`/`admin_edit_plan()` both reuse the exact same validation chain `add_plan()` already runs — `task_allowed_for_project()` (imported from `app.validation`) and `_client_required_error()` (imported from `app.routes.employee` — confirmed no circular import, `employee.py` never imports from `admin.py`, done specifically so this Case-Type/Client rule can't drift between the two entry points) — so an admin assigning a task is held to the identical project/task/client rules an employee would be. Deliberately does **not** call `project_allowed_for_department()` — an admin picking a task on someone's behalf is treated the same way `acting_admin=True` already bypasses the backdate window elsewhere in `validate_entry()`, i.e. admin action is trusted to already know the right department; this bypass is not exercised through any employee-facing path. Employee-facing changes, all in `app/routes/employee.py`/`today.html`: `delete_plan()` now blocks (flash + redirect, not a 403 — matches this app's existing flash-then-redirect convention for a blocked self-service action) when `plan.created_by_employee_id` is set and differs from the employee's own id — satisfies "Employee cannot delete an assigned entry" — while `edit_plan()` (details-only) is deliberately left unrestricted, since the acceptance criteria only named delete. New `_pending_plan_assignment_notices()` helper + `dismiss_plan_assignment_notice()` route (`POST /plan/{id}/assignment-notice/dismiss`) drive a Today-page banner ("**{name}** assigned you a task for {date}: ...", dismissible) satisfying "Employee is notified when an entry is assigned to them" — shown once per assignment via the `assigned_notified_at IS NULL` convention, same NULL-means-not-yet-seen pattern used elsewhere in this app. Today's Plan items (both the live and read-only past-day views, `today.html`) gained an "assigned · {name}" badge whenever `p.created_by_employee_id != user.id`, and the delete (✕) button on a still-Planned assigned item is replaced with a "can't remove" label — this is the actual UI enforcement layer *in addition to* `delete_plan()`'s server-side block, not a substitute for it. `app/templates/admin/person.html` gained a new "Assigned tasks" card (placed directly above the existing Task log card) with a table of the employee's currently-Planned assigned rows (inline edit via plain `<select>` dropdowns for Project/Task — deliberately NOT the live-filtering combo widget for the per-row edit form, to avoid needing one JS combo instance per table row; server-side `task_allowed_for_project` still catches an invalid pick either way) plus an "Assign a new task" form that DOES use the same `initProjectTaskCombo()` search widget every employee-facing Project/Task picker already uses, fed by a new `_plan_combo_items()` helper in `admin.py` (same `{id, name, is_case_type}`/`{id, name, project_ids}` item shape `_combo_items()` already produces in `employee.py`). This is the second admin page (after `admin/lists.html`) to need `combo.js` — it isn't loaded globally by `base.html`, so it's pulled in at the bottom of `person.html` same as `lists.html` already does. No `app/engine.py`/`app/validation.py` changes (the one `task_allowed_for_project()` call is a read, not a new rule), so the pytest+verify_strikes hard rule doesn't gate this feature — verified via `py_compile` across every touched `.py` file and a Jinja `Environment(...).parse()` check across both touched templates (`today.html`, `admin/person.html`).

- **Reports redesign (all 3) + new Task Logs report with AI daily summaries + unplanned-task flag (Ganesh, 2026-08-29)** — three related asks from one message, each confirmed via AskUserQuestion rather than assumed: (1) restyle "every report" to match a pasted mockup (`attendance-report-mockup.html`) — answered "All reports now"; (2) auto-summarize a day's free-typed task-log details into 4-5 bullets naming projects — answered "Use an LLM API (e.g. Claude)"; (3) how to flag a logged entry as "unplanned" — answered "No matching Planned Task that day (Recommended)". New shared CSS in `app/static/app.css` (`.kpi.alertk`/`.kpi.warnk`, `.daystrip`, `.pctbar-wrap`, `.report-legend`, `.tasklog-*`, `.b-unplanned`) built from this app's own existing tokens/`.kpi`/`.badge` classes, not the mockup's own hardcoded hex palette — Attendance and Strikes (`admin/reports_attendance.html`, `admin/reports_strikes.html`) both gained a KPI tile row (`reports.attendance_kpis()`/`reports.strikes_kpis()`, pure post-processing over the already-computed report dict, no extra DB access) and a day-strip mini calendar per employee row (both `attendance_report()`'s and `strikes_report()`'s summary-mode rows now also return a `"daily"` list, reusing the same `by_emp` query already loaded rather than a second one); rows needing action now sort to the top on both (Missing-day employees first on Attendance, worst-strikes-first was already the existing order on Strikes). Time by Project/Task (`admin/reports_time.html`) got a KPI tile row only (`reports.time_kpis()`) — no day-strip, since that report's shape is month buckets, not day-by-day. **Deliberately NOT built:** the mockup's 8-week trend sparkline/line chart at the bottom of each report — that needs a week-over-week historical rollup nothing in this app computes yet; flagged as a follow-up, not attempted here.

  New **Task Logs report** (`/admin/reports/tasklogs`, `reports.daily_task_log_report()`) is a fourth report alongside Attendance/Strikes/Time, same Department → Employee → Date range filter bar and the same "All Employees → summary row per person, one employee → day-by-day detail" drill-down convention the other two established. Daily mode shows every real `TaskEntry` for that employee/day (the messy `"1.did X\n2.did Y"` free-text `details` admins were struggling to skim, same field CLAUDE.md's `capitalize_first()` bullet already covers) grouped into `.tasklog-day-card`s, most-recent-day-first (deliberately the opposite order from Attendance/Strikes' oldest-first daily mode — this report is for reading today's/yesterday's actual work, not auditing a whole range in sequence). **Unplanned rule**, implemented exactly as specified: `daily_task_log_report()` compares each `TaskEntry.(project_id, task_type_id)` against `_planned_pairs_by_date()` — every `PlannedTask` that employee had for that exact date, any status (planned/running/paused/done), carried-forward copies included since they land on their own new date already. No match → `unplanned: True`, shown as a `.b-unplanned` pill on the entry row, with a day-level and employee-level count rolling up into the KPI/summary tables. Known consequence, not a bug: an employee who never uses Plan for the Day at all has nothing to match against, so literally every entry they log reads as "unplanned" — this is what "no matching Planned Task that day" literally means, flagged here in case it turns out too noisy in practice once Plan for the Day adoption is uneven across the team (see the Developer Usage Report, which already tracks Plan adoption specifically).

  **AI summary** is genuinely new capability for this app — its first-ever outbound network call to an LLM, isolated in a new `app/llm.py` (`summarize_day()`, wraps the Anthropic Messages API over `httpx`, already a dependency, no new package needed). `ANTHROPIC_API_KEY` is read from an env var, never stored in the `config` table like an ordinary threshold — it's a secret, not a business setting an admin should be able to browse via `/admin/config`. A new `TASK_SUMMARY_ENABLED` env var (`app/templating.py`, same on/off-switch pattern as `TICKETING_ENABLED`/`HOLIDAY_MANAGEMENT_ENABLED`/`LEAVE_MANAGEMENT_V2_ENABLED`, default on) doubles as a cost kill-switch independent of the key — the report's raw entries and unplanned flags are never gated by it, only the LLM step is, since those are plain DB reads with zero cost. Cost/latency control, the thing flagged during clarification, is handled two ways: (1) summaries are **never** generated automatically on a GET — each day card has its own "Generate summary" button (`POST /admin/reports/tasklogs/summarize`, one employee + one date per call) so a single page view of a 30-day range can never trigger 30 sequential API calls; (2) results are cached in a new additive `TaskDaySummary` table (`app/models.py`, unique on employee_id+date) keyed against `_entries_hash()` — a hash of that day's entries' times/project/task/details — so a day only ever regenerates when its entries actually changed since the cached hash, or on an explicit Regenerate click, never on every page view. `summarize_day()` never raises — a missing key, a network error, a bad status code, anything at all comes back as `(None, "<reason>")`; `TaskDaySummary.error` stores that reason so the report shows *why* a summary is missing (and doesn't silently retry-and-pay-for the same failing call on every view) instead of erroring the page. No `app/engine.py`/`app/validation.py` changes anywhere in this feature, so the pytest+verify_strikes hard rule doesn't gate it — verified via `py_compile` across every touched `.py` file, a Jinja `Environment(...).parse()` check across every touched template, and a standalone Python harness (real function source extracted from `app/reports.py`/`app/llm.py` and executed directly, not hand-traced) confirming `_entries_hash()` is order-independent and content-sensitive, and that the bullet-line parser correctly handles `-`/`•`-prefixed lines, blank lines, and plain unprefixed lines.

- **Projects & Tasks page redesigned as a Department → Project → Task tree (Ganesh, 2026-08-29, from a pasted mockup)** — before this, `/admin/lists` (nav label "Projects & Tasks", template historically titled "Dropdown lists") was two flat tables with no department grouping at all. Scope was narrowed via two AskUserQuestion rounds rather than building the mockup literally: (1) "just the tree UI" using only what already exists — no new fields/entities — not the mockup's Billable flag, per-node hours-worked totals, or the duplicate-project/duplicate-task merge tooling (all explicitly deferred, not attempted); (2) the mockup's per-department "Default shift" (India IST vs US CT hours) was dropped outright, since it directly conflicts with the existing hard rule above this one — ONE fixed reference timezone (`BUSINESS_TZ`), a 2026-08-10 decision made specifically to stop per-employee/per-region timezones from causing confusion. `lists_page()` (`app/routes/admin.py`) computes the tree grouping from data that already existed — `shared_projects` (every `Project` with zero `ProjectDepartment` rows, shown once under an "All departments" group, first) and `dept_groups` (one entry per `reports.departments_list()` name, holding only projects explicitly linked to it) — no new query beyond what `project_department_names` already loaded. A project linked to **multiple** departments (allowed, see `ProjectDepartment`'s docstring) is filed under only its alphabetically-first linked department to avoid rendering — and duplicate-DOM-id-ing — the same project's edit panel more than once; its "Manage departments" checklist still reflects every department it's actually linked to, that placement choice is purely about where it physically sits in the tree. The real scale check that shaped this: `tms_demo.db` has 303 Projects, 38 Tasks, but only 5 `ProjectTask` links and 0 `ProjectDepartment` links today — meaning almost every task is still unrestricted (usable under every project) and no project is department-scoped yet. Given that, a literal per-project nested task list (as the mockup shows) would repeat the same ~33 unrestricted tasks under all 303 projects — so a project's tree node only lists tasks **explicitly** linked to it via `project_task_ids` (the inverse of the existing `task_project_ids`), with a one-line note when there are none ("every task usable everywhere applies here too"); the full task list, including every unrestricted one, stays a single flat "All Tasks" card below the tree (basically the old page's Tasks table, unchanged) — that's still the one place to rename/narrow/deactivate any task regardless of which project(s) it's linked to. Every action inside a tree node — rename, Case Type toggle, Manage departments, Manage projects, Deactivate/Reactivate — reuses the exact same forms and routes the flat page already had (`lists_rename`/`lists_project_case_type_toggle`/`lists_project_departments`/`lists_task_projects`/`lists_toggle`, all unchanged); this was a template + read-only route-context change only, no new routes, no new `Project`/`TaskType`/`ProjectTask`/`ProjectDepartment` columns. Expand/collapse is plain nested `<details>`/`<summary>` (`.tree-dept`/`.tree-proj` in `app/static/app.css`) rather than hand-rolled JS state — the mockup's own `closed`-Set/re-render approach isn't needed when the browser already does this natively; the only new JS (`admin/lists.html`, same "small hand-rolled, no framework" convention as `combo.js`/`msfilter.js`) is the search-filter (matches department/project/explicitly-linked-task names, auto-opening a match's ancestors) and Expand-all/Collapse-all buttons. No `app/engine.py`/`app/validation.py` changes, so the pytest+verify_strikes hard rule doesn't gate this — verified via `py_compile`, a Jinja `Environment(...).parse()` check, and a full `Environment(...).render()` against hand-built fake Project/TaskType objects covering a shared project, a multi-department project, a project-narrowed task, and an inactive project/task, confirming the page actually renders (not just parses) and every expected name shows up in the output.

- **Projects & Tasks tree: visual polish pass (Ganesh, 2026-08-29, same day as the tree build above)** — the first pass was functionally right but looked unstyled compared to the mockup; root cause was a generic `details > summary { color: var(--accent) }` rule (`app/static/app.css`) leaking its bronze link-color onto plain department/project names, since nothing more specific overrode it. Confirmed via AskUserQuestion this was styling-only, not a request to bring back the Billable flag or merge tooling also visible in the reference screenshot (both still explicitly out of scope — see the bullet above). Fixes, all CSS/markup, no route/model changes: explicit `.tree-dept-nm`/`.tree-proj-nm`/`.tree-meta` classes so row text no longer inherits that link color; a project's rename/Case-Type/Active/departments controls moved behind their own `.tree-edit-toggle` (`<details>` again, "✎ Edit") nested inside the project's body, so expanding a project shows its tasks first without the edit form always taking up space; Case Type and Active are now real-looking toggle switches (`.tgl`/`.knob`, still just a `<button>` per existing form, still one immediate POST per click — no combined save endpoint was built, since that would've meant merging 4 independent routes into one); the department checklist reuses Roster's existing `.deptpill` chip class (`:has(input:checked)` styles a checked one like Roster's `.active` state, plus a small "×") instead of a bare checkbox list, so the same chip look is used everywhere in the app rather than a one-off. Verified via `py_compile`, a Jinja `Environment(...).parse()` check, and the same fake-data `Environment(...).render()` harness as the tree build itself, re-run to confirm the new classes/markup actually render (not just parse) with a mix of shared/multi-department/inactive projects and tasks. **Same-day follow-up: per-project "+ Task" quick-add** (Ganesh: "we can add task also for each project") — a project's summary row now shows hover-revealed "+ Task" / "Edit" / "Deactivate" links (`.tree-row-acts`, opacity 0 until `summary:hover`), matching the mockup's row layout more closely than the first polish pass did. "+ Task" reveals a small inline form (`.tree-add-task`, hidden by default) posting to the same existing `/admin/lists/add` route the All Tasks card's own Add form uses, with `project_id` pre-filled from this node instead of picked via combo — no new route. "Edit" now reveals `.tree-edit-panel` (same fields as before) via a new small `treeToggle(btn, selector)` JS helper rather than a nested `<details>`, since a `<details>` can't itself live inside a `<summary>`; clicking either button calls `event.stopPropagation()` so it doesn't also toggle the row's own expand/collapse, and reveals the sibling hidden div while forcing the parent `<details>` open so the newly-shown form is actually visible. Deactivate/Reactivate is now available both as this direct one-click row action and inside the Edit panel's existing Active toggle — intentionally both, matching the mockup, not a duplicate to clean up. No Python changes at all for this follow-up (CSS + one template + ~10 lines of JS) — verified via the same render harness (now also asserting `.tree-add-task`/`.tree-row-acts`/`treeToggle` appear in the output) plus `node --check` on the page's extracted inline JS.

- **Projects & Tasks tree: task rows became editable (Ganesh, 2026-08-29, "i should able to edit the tasks also")** — the tree's inline per-project task rows (`.tree-task-row`, added in the tree build above) were plain read-only text; they now get an always-visible (not hover-reveal, since a task row is only ever shown once its parent project is already expanded, unlike `.tree-row-acts` on the collapsed project summary line) "Edit"/"Deactivate" pair, reusing the exact same `/admin/lists/task/{id}/rename`/`/toggle` routes the "All Tasks" master table below already posted to — no new routes, no model changes. "Edit" reveals an inline rename `<form>` via a new `treeToggleTaskRename(btn)` JS helper (`app/templates/admin/lists.html`) — simpler than the project-level `treeToggle()` since a task row is never itself inside a collapsed `<summary>`, so there's no parent `<details>` to force open. New `.tree-task-acts`/`.tree-task-rename` CSS in `app/static/app.css` (`.tree-task-row` gained `flex-wrap: wrap` so the rename form drops to its own full-width line via `flex-basis: 100%` instead of squeezing inline). Super-admin-only, matching every other write action on this page. Verified via CSS brace-balance check, a Jinja render test (super-admin/non-super-admin × active/inactive task, confirming the Edit/Deactivate markup and its absence render correctly), and a `node --check` pass on the page's inline JS.

- **Time by Project/Task: "Pie chart" button restyled to the accent (highlighted) button look (Ganesh, 2026-08-29)** — was `class="small secondary"` (the muted gray-beige look), now plain `class="small"` so it inherits the app's default solid-accent button style, the same look every other primary action (Add/Save) uses. One-line template change in `admin/reports_time.html`, no JS/route/logic touched.

- **"Assign a task to someone's plan" (TK-04) moved from Person Detail into the "Assignments" page, which was renamed to "Assign Work" (Ganesh, 2026-08-29: "i want this assign tasks feature to be in Assignments and i want to rename Assignments to [a name] relevant to Assign tasks to plan for the day employee")** — two AskUserQuestion calls settled scope: (1) the page's older, unrelated feature — `ProjectAssignment`/`TaskAssignment` checkboxes marking which projects/tasks an employee is generally associated with (2026-08-01, still read by `app/routes/employee.py`'s `_visible_projects_and_tasks()` to star-sort Today-page dropdowns) — stays, as its own separate card, not replaced or removed; (2) the new page name is "Assign Work" (nav label in `base.html`, and the page's own `<h1>`), not "Task Assignments" or "Assign to Plan". `admin/assignments.html` now has three cards top to bottom: the employee picker (unchanged), a new "Assigned tasks for {name}" card (the TK-04 list + "Assign a new task" form, byte-for-byte the same markup Person Detail's own card already had, including the same `combo.js`/`initProjectTaskCombo()` wiring — this is now a second admin page alongside Person Detail and Lists that loads `combo.js`), and the pre-existing "assigned projects/tasks" checkbox card (retitled from a bare `<h2>` to include the employee's name, otherwise unchanged). Person Detail's own "Assigned tasks" card is untouched and still there too — this is an additional entry point, not a replacement, since reviewing one employee's full task-log history in one place is still useful context on that page. To make both entry points redirect back to wherever the admin actually came from, `admin_add_plan`/`admin_edit_plan`/`admin_delete_plan` (`app/routes/admin.py`) gained an optional `return_to: str = Form("")` and a new `_plan_redirect(emp_id, ym, return_to)` helper — same precedent `_complink_redirect()` already set for Overtime Management's own "create a link without leaving this page" card: default (`return_to` blank, Person Detail's own form) redirects to `/admin/person/{emp_id}?ym={ym}` exactly as before; `return_to=assignments` (set by the new hidden field on Assign Work's copy of the form) redirects to `/admin/assignments?employee_id={emp_id}` instead, keeping the same employee selected. `assignments_page()` now also computes `plan_project_items`/`plan_task_items` (via the existing `_plan_combo_items()`) and `assigned_plans` (still-`PLAN_PLANNED` rows for the `selected` employee) whenever an employee is picked — mirrors what `person()` already computes for its own copy of this card. No `app/engine.py`/`app/validation.py` changes (the one `task_allowed_for_project()` call inside `admin_add_plan`/`admin_edit_plan` is unchanged, still a read not a new rule), so the pytest+verify_strikes hard rule doesn't gate this — verified via `py_compile`, a Jinja `Environment(...).parse()` check across `admin/assignments.html`/`base.html`/`admin/person.html` (with the app's real `mdy`/`hm`/`tojson`/etc. filter functions registered on a bare `Environment`, since this sandbox has neither `fastapi` nor `sqlalchemy` to import the real `app/templating.py` module directly), and a full fake-data `Environment(...).render()` of `admin/assignments.html` in both its no-employee-selected and employee-selected states, confirming the Assign-a-task form, the assigned-tasks table (with its edit/delete forms carrying `return_to=assignments`), and the checkbox card all render with the expected content. **Same-day follow-up: the employee picker became a Department → Employee tree** (Ganesh: "instead it should show departments once admin clicked on department it should show employee ... even we can have search option directly search for employees and departments as well") — replaced the flat `<select>` with the exact same `.tree`/`.tree-dept`/`.tree-car`/`.tree-body` classes the Projects & Tasks tree already established, one level deep since an employee is a leaf (clicking one just navigates to `?employee_id=...`, which is what "expands" the cards below on reload — no new JS needed for that part). `assignments_page()` groups the already scope-filtered `emps` list into `emp_dept_groups` (`{"name": department_or_em_dash, "employees": [...]}`, sorted by department name); the template auto-`open`s whichever department the `selected` employee is in and adds a `.sel` highlight to their row. New `empTreeFilter()` JS (same hand-rolled convention as the Lists page's own `treeFilter()`) does the two-way search: typing a department name keeps every employee row in that department visible, typing an employee name narrows to just that row. No Python changes beyond the grouping dict in `assignments_page()` — verified the same way as the rest of this bullet (`py_compile`, CSS brace-balance check, Jinja render test asserting the tree markup/auto-open/highlight/search script all appear, and a `node --check` pass on both of the page's inline `<script>` blocks).

- **Task Logs report gained two Excel downloads — by employee and by project (Ganesh, 2026-08-29)** — the report itself was already employee-wise on screen; `reports.task_log_export_rows()` is a new flat, ungrouped list of dicts (one per `TaskEntry` in the current Department/Employee/date-range filter, same "unplanned" rule `daily_task_log_report()` already uses) that both new `.xlsx` routes in `app/routes/reports.py` — `GET /admin/reports/tasklogs_employee.xlsx` and `GET /admin/reports/tasklogs_project.xlsx` — sort differently rather than running two independent queries that could quietly disagree. The by-project download also gets a second "Project totals" sheet (per-project sum of minutes, busiest first) mirroring the "By Project" breakdown Time by Project/Task already has, so the two "by project" views in the app feel consistent. Both links sit next to the existing date-range line on `admin/reports_tasklogs.html`, carrying the same `dept`/`emp`/`range`/`start`/`end` query params the on-screen filter bar uses, same convention every other report's own "Download Excel" link already follows. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile`, a Jinja parse check, and a standalone Python check of the sort/aggregation logic against a small hand-built row set (confirms employee-wise sorts by name then date, project-wise sorts by project then employee then date, and the project-totals aggregation sums and orders correctly).

- **Task Logs summary: LLM path removed, replaced with a deterministic rule-based one (Ganesh, 2026-08-29: "without LLM cant we generate summary" → "Replace LLM with rule-based")** — `reports.rule_based_day_summary(rows)` is a pure function with no network call: one bullet per project worked that day, naming the project, its total time, and its distinct task names, e.g. `"Bluepeak Consulting Inc. (2:30): Check credential and post job order, Get AD quotes"` — satisfies the original ask ("4-5 bullets highlighting project names") deterministically, instantly, computed fresh on every page view with zero cost or failure mode. Capped at 5 project bullets (a 6th+ collapses into one `"+N more project(s) (Xh Ym)"` line), same top-N-plus-other instinct the "By Project" pie chart already uses. `daily_task_log_report()` now calls this directly per day instead of `get_cached_day_summary()`/an LLM round trip — since it can't fail or go stale, the "stale"/"error"/"Generate summary" states are all gone from `admin/reports_tasklogs.html` along with the `POST /admin/reports/tasklogs/summarize` route, the `TASK_SUMMARY_ENABLED` flag (`app/templating.py`), and `app/llm.py` (deleted — the Anthropic Messages API wrapper, now fully unreferenced). The `TaskDaySummary` cache model (`app/models.py`) was removed the same way — nothing reads or writes it anymore; a `task_day_summaries` table on an already-deployed host is simply orphaned, not a migration risk. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` across every touched file, a Jinja parse + full render test of `admin/reports_tasklogs.html` in both daily and summary mode (confirming the download links still work and every "AI"/"Generate"/stale-badge string is gone), and a standalone execution of the real `rule_based_day_summary()` source against 4 worked cases (no entries, two projects with a repeated task deduped and order preserved, one project with two distinct tasks both listed, and 8 projects correctly capped to 5 + a "+3 more" bullet).

- **Today page: two-column grid left a huge blank gap above Auto time capture (Ganesh, 2026-08-29 bugfix, caught right after the "This Week"/"Reminders" cards landed)** — `.today-grid`'s explicit-`grid-column`-per-wrapper technique (see the reskin bullet below) turned out to have a real bug, not just a theoretical risk: CSS Grid's default "sparse" auto-placement explicitly starts a NEW row whenever a later item's column number is LOWER than the previous item's column number. The right-column wrapper (`grid-column:2`, Plan for the Day/Today's Plan/This Week/Reminders) comes FIRST in the DOM, then the left-column wrapper (`grid-column:1`, Auto time capture/Add row/Task log) comes second — column 2 then column 1 is exactly the "goes backwards" case, so the browser was silently placing the left column in row 2, below the ENTIRE height of row 1 (which had nothing in column 1), rather than beside the right column in row 1 as intended. Fixed with one line — `grid-auto-flow: dense;` on `.today-grid` — which backfills the first open cell each time instead of only ever moving the cursor forward; no HTML/template change needed. This is a correction to the "CSS Grid explicit-column-assignment technique" reasoning in the reskin bullet below: wrapping each column in one div does prevent items interleaving WITHIN a column, but does not by itself prevent the sparse-packing row-skip when the two wrappers' column numbers appear in descending DOM order — `dense` is required whenever that's the case.

- **Today page: two-column grid reverted to a single-column stack (Ganesh, 2026-08-29, same day as the dense-packing bugfix above, live feedback after seeing the fixed layout: "plan for the day should above the auto time capture and todays plan move up")** — the two-column side-by-side layout (Plan for the Day/Today's Plan/This Week/Reminders on the right, Auto time capture/Add row/Task log on the left) technically worked once `grid-auto-flow: dense` fixed the placement bug above, but once Ganesh saw it fixed and side-by-side, he asked for Plan for the Day and Today's Plan to sit ABOVE Auto time capture instead — a single vertical stack, not two side-by-side panels. This was the simplest possible fix: `.today-grid`'s CSS Grid rules (`display:grid`, `grid-template-columns`, `grid-auto-flow:dense`, the 900px media query) were removed outright and replaced with one rule, `.today-col + .today-col { margin-top: 18px; }`, so the two `.today-col` wrapper divs — already in the right DOM order (Plan section first, Auto-capture section second, unchanged from the two-column build) — just stack full-width instead of sitting in a grid; the `style="grid-column:.."` inline attributes on both wrapper divs were removed since they're meaningless once the parent isn't `display:grid`. No template content moved at all, only the wrapping container's own CSS and two now-dead inline style attributes. Verified via the same 7-scenario Jinja render+div-balance harness (all still pass), CSS brace-balance re-confirmed (287 open == 287 close, down from 290 since the removed grid rules were larger than the one replacement rule), and a grep confirming no `grid-column` references remain in `today.html`.

- **Today page: "Today's Plan" swapped into the Task log's old spot, Task log moved to the bottom, Auto time capture shrunk (Ganesh, 2026-08-29, live feedback: "I want Today's Plan should be in place of task logs and task logs section should go down and reduce the auto timer capture box")** — clarified via AskUserQuestion first (the "Plan for the day" add form vs. the "Today's Plan" checklist it feeds are two separate cards): the checklist moved from the right column into the left column's top slot (where Task log used to sit), the small add form stayed in the right column since it doesn't need prime space, and Task log moved to the very bottom of the left column — new left-column order top to bottom: Today's Plan (+ its read-only `past_plans` counterpart on a different day) → Auto time capture → Add row → Task log. Task log is deliberately still OUTSIDE the `{% if not day_locked %}` block that now wraps Auto time capture/Add row — moving it after that block, not inside it, was required specifically so the log itself keeps showing on a locked day (only the entry/timer forms above it hide), same as before this reorder. "Reduce the auto timer capture box": new `.compact-card` class (`app/static/app.css`) tightens this one card's padding and `h2` size below the rest of `.today-page .card`'s already-compact defaults, and its help paragraph was shortened to one line — the dropped rule detail (min 5 chars, no overlaps, Nh cap) didn't disappear, it moved to that paragraph's own `title` attribute (hover to see it), same "shrink the visible text, keep the detail one hover away" pattern the task-log Duration-hover change used earlier the same day. No `app/routes/employee.py` changes (pure template reordering + CSS) — verified via the same 7-scenario Jinja render+div-balance harness (all still pass), CSS brace-balance re-confirmed (299 open == 299 close), a direct position check confirming the new order (Today's Plan h2 < Auto time capture h2 < Add row h2 < the task log's `<thead>`) in the rendered HTML, and confirming a locked day still renders the task log's `<thead>` while correctly hiding both the Auto time capture and Today's Plan sections.

- **Today's Plan / past-plans items: plain-glyph status marker replaced with a real checkbox square (Ganesh, 2026-08-29, from a pasted mockup: "i want check boxes here it should look neat")** — `.plan-check` (`app/static/app.css`) is a small bordered square, same shape a real checkbox reads as; `today.html`'s two identical plan-item blocks (the live "Today's Plan" list and the read-only past-day `past_plans` list) both had their `<strong>{% if p.status == 'done' %}✔{% elif ... %}...{% endif %} {{ p.project.name }}...</strong>` glyph-in-the-heading pattern replaced with a separate `<span class="plan-check {{ p.status }}">...</span>` before the (now glyph-free) `<strong>` heading text — one `replace_all` edit since both blocks were byte-for-byte identical. Color mapping reuses the existing `b-plan-*`/`.plan-item.plan-*` left-border semantics already established (`running`=orange, `paused`=warn/olive, `done`=filled `--ok` green with a white checkmark) with one deliberate exception: `planned` (not-started) gets a plain neutral outline, not the red `--bad` its own badge uses — an empty checkbox reading as an error color would be misleading, even though "not started" is red everywhere else on this card. No `app/routes/employee.py` changes (pure template/CSS) — verified via the same 7-scenario Jinja render+div-balance harness (all still pass), CSS brace-balance re-confirmed (296 open == 296 close), and a direct check of the rendered markup confirming all 4 statuses produce the expected class + glyph (`planned` empty, `running` "▸", `paused` "❚❚", `done` "✓").

- **Today page: task log's Start/End columns collapsed into a hover on Duration (Ganesh, 2026-08-29, "can we remove start and end time? Can we have a hover over the duration with the start and stop" — scoped via AskUserQuestion to Today's task log only, not My Month's own per-day table, which keeps its Start/End columns as-is)** — reports/exports were a separate, explicit ask ("if we want to download the sheet start and end time also should be in reports") and are untouched: `admin/reports_tasklogs.html`'s on-screen table and both Task Logs Excel downloads (`reports.task_log_export_rows()`) still show Start/End as their own columns, since this change only ever touched `today.html`. The table header went from 7 columns (Start, End, Duration, Project/Employer, Task, Details, blank action column) to 5 (Duration, Project/Employer, Task, Details, blank) — both `colspan` values that depend on column count were updated to match: the gap-row's "Unlogged gap" message stays `colspan="3"` (still just Project+Task+Details, unchanged), and "No rows yet" went from `colspan="7"` to `colspan="5"`. The Duration cell itself (a plain `<span class="dur-hint">`/`<strong class="dur-hint">` depending on gap-row vs. real-row) carries a `title="{{ start|clock }} – {{ end|clock }}"` attribute for the browser's native hover tooltip — the gap-before `⚠` warning (`flags.get(e.id)`), previously attached to the now-removed Start cell, moved to sit right after the Duration value instead. New `.dur-hint` CSS (`app/static/app.css`) is purely a `cursor:help`/dotted-underline hover affordance — a class rather than a bare `[title]` attribute selector, since a real row's tooltip lives on the nested `<strong>` while a gap row's lives on the `<span>` wrapping the value, not on a shared parent element an attribute selector could target cleanly. No `app/routes/employee.py`/`app/engine.py`/`app/validation.py` changes (purely a display change over the same `e.start_minute`/`e.end_minute`/`e.duration_minutes` values every other column already read) — verified via the same 7-scenario Jinja render+div-balance harness (all still pass), CSS brace-balance re-confirmed (292 open == 292 close), and a direct check of the rendered header cell count (5) and the exact tooltip string format (e.g. `title="09:00 – 10:00"`) against the real template output.

- **Today page: "This Week" card replaced with "My Month" (Ganesh, 2026-08-29, from a pasted mockup: "instead of this week, i want my month like in second screenshot")** — the mockup's "My Month" card (Logged, Effective target, Running balance, Missing days, Partial days, Strikes, Compliance) is a month-level compliance snapshot, not a week-level logged/target rollup — so unlike `_week_summary()` (a pure live read that deliberately avoids `DayStatus`, since a week's not-yet-submitted days have no guaranteed fresh row), the new `_month_summary(db, emp, cfg, today)` helper in `app/routes/employee.py` calls `engine.recompute_employee(db, emp, first, min(last, today), cfg)` first — the exact same call `my_month()`'s own route already makes before reading `DayStatus` — because these ARE the same Logged/Effective-target/running-balance/strike figures the real `/my-month` page already computes, not a new parallel measurement, so it has to stay in sync with them rather than reinvent a second definition. `missing_days`/`partial_days` both exclude `strike_exempt` rows (same filter `engine.strikes_in()` itself applies), so they always sum to `strikes` — matching the mockup's own implied "Strikes 3 (missing + partial)" relationship, rendered as a small parenthetical next to the Strikes number rather than a separate computed field. "Running balance" reuses the `hm` filter's existing negative-sign handling directly (`hm(-131)` already renders `"-2:11"`) plus an " ahead"/" short" suffix and `--ok`/`--bad` color, matching the mockup's "-2:11 short" text exactly. "Compliance" reuses the exact `.badge`/`.b-complete`/`.b-missing` classes and `strikes >= threshold` comparison `my_month.html`'s own KPI row already uses (`b-complete` green "Compliant" vs. `b-missing` red "At/over threshold") — no new badge colors invented. Card header adds a small "all computed" muted label on the right, matching the mockup; the rest of the card reuses the same `.weekrow` label/value CSS class the old "This Week" card (and the still-present "Reminders" card next to it) already used, so no new row-layout CSS was needed. This is the one card on this page that now has a real DB write side effect (`recompute_employee()` persists `DayStatus` rows) on every `/today` load for a "today" view — an intentional, bounded (current month only) tradeoff to keep these figures accurate, consistent with `my_month()`'s own already-established behavior, not a new pattern. No `app/engine.py`/`app/validation.py` changes, only new calls into existing `engine.recompute_employee`/`month_range`/`running_ledger`/`strikes_in`/`cfg_int` — verified via `py_compile`, the same 7-scenario Jinja render+div-balance harness (extended with a populated `month_summary` matching the mockup's own numbers, confirming every label/value renders and "This Week" no longer appears anywhere on the page), CSS brace-balance re-confirmed (299 open == 299 close, unchanged from before since no new CSS was needed), and a second render pass confirming the Compliant/green badge path (`strikes < threshold`) and the "ahead" (non-negative balance) wording both render correctly, not just the At/over-threshold/short path.

- **Employee-facing nav condensed to 5 items, Leave/Overtime folded into a "My requests" dropdown (Ganesh, 2026-08-29: "Today My hours My requests Support Profile ... under my requests we should have leave request and Overtime Request dropdown")** — `app/templates/base.html`'s two employee nav branches (plain employee, and an admin who clicked "Employee login") both went from 6-8 flat links to: Today, My hours (renamed from "My Month" — same `/my-month` route and `active_nav == 'month'`, label only, confirmed via AskUserQuestion), a "My requests ▾" dropdown, Support, Profile. The dropdown reuses the exact `.navdrop`/`.navdrop-menu` `<details>/<summary>` component the admin nav's "Projects & Task Management"/"Analytics and Reports" menus already use one section down in the same file — no new CSS or JS, just the same existing generic (not admin-scoped) component applied here for the first time. Holidays (when `HOLIDAY_MANAGEMENT_ENABLED`) and the Developer Usage Report (`user.is_developer`, plain-employee branch only, unchanged gating) — both previously their own top-level links, not mentioned in Ganesh's target 5-item list — were folded into the same dropdown rather than dropped from the nav entirely (AskUserQuestion, Recommended): "My requests" now holds Leave Request, Overtime Request, Holidays, and (developers only) Usage Report, in that order. The dropdown's own `<summary>` gets the `on` highlight class whenever `active_nav` is any of `leave`/`overtime`/`holidays`/`reports` (the last one only relevant on the plain-employee branch, since Usage Report never appears on the admin-as-employee branch) — same "highlight the parent while on any child page" convention the admin dropdowns already use, verified by rendering the real `base.html` (via a minimal child template extending it) for each of those `active_nav` values and confirming the summary carries `class="on"` for all four and not for unrelated pages like `today`. `employee_zone` (the list gating the profile-completion popup) was left unchanged — every route these links point to is unchanged, only how they're grouped in the nav changed, so nothing in that list needed to change. Verified via a full Jinja render of `base.html` for a plain employee, a developer-employee (confirms Usage Report appears), an admin-acting-as-employee, and a logged-out request — all render without error, "My hours"/"My requests"/"Leave Request"/"Overtime Request" all present, and the old flat "Leave"/"Overtime" link labels are gone.

- **Today page: back to two columns, Task log moved to the top of its column, bottom breathing room added (Ganesh, 2026-08-29, same day as the single-column revert above — "no i want them in two coloumns only" followed immediately by "revert back to this but i want task logs at top above auto time capture, and lets have some breathing space on the end")** — the single-column stack above was itself reverted within minutes: `.today-grid` went back to `display: grid` with the two columns (Plan for the Day/Today's Plan/This Week/Reminders in the narrower right column, grid-column:2; Task log/Auto time capture/Add row in the wider left column, grid-column:1) and `grid-auto-flow: dense` (still required — see the standalone bugfix bullet above; this wasn't optional to drop just because the layout got reverted once). Within the left column, the Task log card (table + progress bar) was physically moved in the template to sit BEFORE Auto time capture/Add row instead of after — a pure reordering of the same three cards' Jinja/HTML, nothing inside any of them changed. `.today-page` gained `padding-bottom: 32px` for breathing room at the bottom of the page, since the page previously ended right at the last card's bottom edge with no margin. No `app/routes/employee.py` changes at all for this pass (template + CSS only) — verified via the same 7-scenario Jinja render+div-balance harness (all still pass), CSS brace-balance re-confirmed (291 open == 291 close), and a direct position check on the rendered HTML confirming the Task log's `<thead>` now appears before Auto time capture's `<h2>`, which appears before Add row's `id="addrowcard"`, in that exact order.

- **Today page: "This Week" + "Reminders" cards fill the right column's blank space (Ganesh, 2026-08-29, same mockup as the reskin below, follow-up ask: "i want exactly like this, when they added a task in plan for the day the this i want in that balan space")** — the reskin below deliberately left this space empty since the mockup's own numbers ("Submit reminder 5:30 PM", "Yesterday locks at 10:00 AM") aren't real policies this app enforces (Submit Day has no deadline; locking is admin-triggered, not time-triggered) — flagged then as a follow-up, built now with two scoping calls (AskUserQuestion): "Leave remaining" shows Planned Time's remaining balance (Recommended — the one leave type with a real accrual/cap, vs. an ambiguous pick across all 5 V2 types), and the two fabricated clock-times are dropped entirely (Recommended) rather than inventing new Config settings for them — Reminders now shows only genuinely real data. New `_week_summary(db, emp, cfg, today)` helper in `app/routes/employee.py` (not `engine.py` — a presentational rollup over existing engine functions, same "route-level helper, not new business math" precedent `_day_context()`/`_client_required_error()` already set) computes Monday-of-this-week through today LIVE from TaskEntry/LeaveRecord/Holiday data — deliberately NOT from `DayStatus`, since `recompute_employee()` only runs on Submit Day / My Month view / admin actions, not on every `/today` load, so an unlocked "today" (or any not-yet-submitted earlier day this week) has no guaranteed fresh `DayStatus` row. "Overtime this week" sums each day's own positive variance (logged − target) — a shortfall day never offsets a surplus day, the identical "positive variance only, per day" definition `reports.task_log_overtime_report()` already established for Overtime Management's "Who worked overtime" table, so this app now has one consistent meaning of "overtime" across both places. The mockup's "Mon-Thu" day-badge row is real too, not decorative filler: one badge per WORKING day strictly before today this week (skips weekends/holidays and today's own still-in-progress day, matching why the mockup only showed 4 letters on a Friday), colored done/not-done by whether that day has any logged minutes at all. `pending_ot_days` sums the day-span of every still-`requested` `OvertimeApproval` row for this employee — 0 hides the entire Reminders card rather than showing an empty line, same "nothing to say, don't render" convention every other conditional banner on this page already follows; `planned_remaining_days` is `None` (hides its row) when `LEAVE_MANAGEMENT_V2_ENABLED` is off, since Planned Time is a V2-only concept. Both new cards in `today.html` are gated `{% if day == today %}` only — not `not day_locked` — since they're pure information, not an editing form, and specifically because a *locked* today previously had nothing at all in that column (Plan for the Day hides while locked, past_plans only shows for `day != today`) — that dead space is exactly what these cards now fill even after Submit Day. New CSS: `.weekrow`/`.week-badges`/`.week-badge`/`.week-badge.done` in `app/static/app.css`. No `app/engine.py`/`app/validation.py` changes (only new calls into existing `engine.holidays_set`/`is_working_day`/`leave_minutes_on`/`leave_balance_v2`, no new business rules) — verified via `py_compile`, the same 7-scenario Jinja render+div-balance harness the reskin below used (extended with populated/empty `week_summary`/`pending_ot_days`/`planned_remaining_days` cases, confirming the Reminders card and Planned Time row each correctly appear/disappear), CSS brace-balance re-confirmed (290 open == 290 close), and a standalone execution of the real `_week_summary` pure-math shape against 3 hand-worked scenarios (a mixed over/under week matching the mockup's magnitude — one day well over target, one well under, confirming the shortfall day never cancels the overtime day's surplus; a day with zero logged minutes reading as a not-done badge; a half-day-leave day correctly halving just that one day's target while weekend days stay at 0 regardless).

- **Today page visual reskin, no feature/route/behavior changes (Ganesh, 2026-08-29, from a pasted mockup: "i want like this simpel UI for today page and no existing feature breaks , keep existing current features as it is but just chnage ui like this")** — scope was narrowed via AskUserQuestion before writing anything, since `today.html` is the most feature-dense page in the app: (1) the app's global top nav (the mockup showed a different one entirely — Today/Approvals/People/Insights/Setup) stays exactly as-is, only Today's own content changed; (2) this is a **visual reskin only**, not the mockup's literal structure — Plan for the Day, Today's Plan, Auto time capture, Add Row, and the task log table remain the exact same separate features/forms/routes/conditionals they always were, just laid out differently. Zero Python changes (`app/routes/employee.py` untouched) — everything is `app/templates/today.html` + `app/static/app.css`. Three changes: (1) the page header was restructured into `.today-header` (title/subtitle on the left, `.today-header-actions` on the right holding the existing Punch/Break `.quickbar` plus two new items — a plain `<a href="#addrowcard">` anchor link to the pre-existing Add Row card, and the Submit Day `<form>` itself, moved here byte-for-byte from the bottom of the task log card, same route/hidden-input/confirm dialog, just relocated); (2) a `.progress-wrap`/`.progress-bar`/`.progress-fill`/`.progress-labels` block was added right under the existing total/target text in the task log card, computed from the same already-adjusted `total`/`target` integers (clamped 0-100%), purely additive — the text line above it is unchanged; (3) the page body was wrapped in `.today-grid` (CSS grid, two columns, `@media (max-width: 900px)` collapses to one) by inserting exactly two `<div class="today-col" style="grid-column:N">` wrapper tags around the EXISTING block boundaries — Plan for the Day/Today's Plan/past_plans on column 2 (right), Auto time capture/Add row/task log on column 1 (left) — deliberately explicit `grid-column` per wrapper rather than relying on CSS Grid's sparse auto-placement (which would misplace things given the two sections' DOM order doesn't match their visual left/right order); the Suggest-a-new-project card and the trailing `<script>` block (all the `initProjectTaskCombo()` wiring) stay full-width below both columns, untouched. Plan-item status markers switched from a plain `{{ p.status }}` label to a leading glyph (✔/▸/❚❚/☐ for done/running/paused/planned) matching this file's own pre-existing plain-glyph convention (⚠, ✔, ⏳, ✎, ✕) — not colorful emoji, which was tried and self-reverted before landing. Deliberately **not** built: the mockup's "This Week" (weekly logged/target/overtime totals) and "Reminders" (leave balance, lock-time nudges, pending-overtime count) side panels — those need new backend aggregation this app doesn't compute anywhere yet, which is a new-feature ask, not a UI change of something that already exists; flagged here as a follow-up Ganesh can request separately if wanted, not silently dropped. No `app/engine.py`/`app/validation.py`/`app/routes/*.py` changes at all, so the pytest+verify_strikes hard rule doesn't gate this — verified via: a full HTML div-balance check (Python's `html.parser`, not just eyeballing) across 7 hand-built fake-context scenarios (empty today, a fully-loaded today with an ad-hoc active timer/active punch over target/active break/all 4 plan statuses including one assigned-by-another-employee/a gap row/an overtime row/both notice types/the overtime prompt/break-excess/gap-prefill all firing at once, a plan-LINKED active timer, a locked day with a pending unlock request and the punch-out reminder, a resubmit-banner day, a read-only past day with `past_plans`, and a with-no-entries day) — all balanced, zero unclosed/mismatched tags; every `getElementById`/`querySelector` target the inline JS references (`punchtimer`, `breaktimer`, `overtimeBanner`, `tasktimer`, `addrowcard`, `start_time`, `end_time`, `durpreview`, the three combo pairs, `suggest-kind`/`suggest-form`, `addrow`) confirmed present when its guarding condition is true and absent when it's false (e.g. a locked day correctly has no `addrowcard`/combo ids at all); `app/static/app.css`'s brace count re-confirmed balanced (284 open == 284 close) after the reskin's CSS additions; and a `node --check` pass on all 5 of the page's inline `<script>` blocks (none were touched structurally except cosmetic surrounding-markup changes — script contents are byte-for-byte unchanged).

- **My Month: Compensation section removed, Hours ledger became a week→day→entries tree with Overtime status + triangle balance (Ganesh, 2026-08-29, from two screenshots: "1.Remove Compensation (from Punch In/Out) section from MY Hours / 2.for Hours ledger... Status must be partial, complete, leave, overtime / balance must be a small up or down triangle... make it week wise click on that week then that particular week days will expand then wehn click on entries logs will be expand / gap between each column is too big")** — three AskUserQuestion calls settled scope before writing anything, since "Status must be partial, complete, leave, overtime" on its face would drop Missing from view, and Missing is exactly what drives strikes. (1) **Status**, answered "Keep all, just add Leave/Overtime": Weekend/Holiday/Missing/Partial/Complete/Leave (the last already a real `DayStatus`/`effective_status()` value, `m.LEAVE` — nothing new needed there) are untouched; new `_ledger_display_status(r, comp_erases)` in `app/routes/employee.py` is a display-only label that layers "Overtime" on top of an already-Complete day when `variance_minutes > 0` (logged more than target) — a Leave day keeps reading "Leave" even with positive variance (a reduced/zeroed target from a partial-day leave), Leave wins over Overtime. This never touches `DayStatus.status`/`effective_status()`/strikes — `"overtime"` was added to `STATUS_NAMES` in `app/util.py` purely as a label lookup, and `.b-overtime` (new `--overtime`/`--overtime-bg` CSS vars, a blue distinct from the existing ok/warn/bad/leave palette) in `app/static/app.css`. (2) **Balance**, answered "Triangle next to the number": new `.tri.up`/`.tri.down` CSS-triangle spans (green up / red bad-red down, pure CSS borders, no image/icon font) sit before the existing bold `hm_signed` balance text in both the week-total row and every day row — the exact figure is never dropped, just given a quick visual cue. (3) **Week-row content**, answered "Week totals (Logged/Target/Variance/end-of-week Balance)": new `_weekly_ledger(ledger, comp_erases)` in `app/routes/employee.py` groups `engine.running_ledger()`'s already-computed chronological rows into Monday-Sunday weeks — Logged/Target/Variance are plain per-week sums, but a week's "Balance" is deliberately the LAST day's already-cumulative `running_ledger` balance, not a sum (summing running balances would double-count every prior day into every later week). `my_month()` passes both `ledger` (unchanged, still used by the KPI tile) and the new `weekly_ledger` (weeks and, within each, days, both already in descending/most-recent-first order — no `|reverse` needed in the template, replacing the old `ledger|reverse` convention) to `my_month.html`. The Compensation removal is scoped literally to the "Compensation (from Punch In/Out)" **card/table** the screenshot showed (`comp.days` loop, now deleted from the template) — the KPI tile above it (`comp.balance`, with its own explanatory hover tooltip) was NOT named in the request and stays, so `compensation.monthly_summary()` is still called and still used; only the table went away. New table markup uses plain `<tr class="week-row" data-week="N">`/`<tr class="week-day-row" data-week="N">` (initially `display:none`) rather than nested `<details>` (a `<details>` can't wrap multiple `<tr>` siblings) — same hand-rolled show/hide JS convention this app already uses everywhere else (`combo.js`, the entries-expand toggle this table already had). Clicking a week row toggles its day rows via a `data-week` match; clicking a day's existing "View (N)" `<details>` still toggles its own `entries-detail-row` exactly as before, but the week-toggle handler now also explicitly re-hides any `entries-detail-row` whenever its week collapses (checking the sibling day-row's `<details>.open` state on re-expand) so a previously-opened entries table can never leak into view while its week is collapsed. Column-spacing ask ("gap between each column is too big") became a new `.ledger-tree` class: `table-layout:fixed` with explicit per-column `width%` (28/15/11/11/11/14/10) plus tighter `padding:5px 6px`/`font-size:13px`, scoped to this one table only — the default `th,td{padding:6px 8px}` every other table in the app uses is untouched. No `app/engine.py`/`app/validation.py` changes (both new helpers are route-level display/grouping logic over already-computed `DayStatus`/`running_ledger` rows, same "route-level helper, not new business math" precedent `_month_summary()`/`_week_summary()` already set), so the pytest+verify_strikes hard rule doesn't gate this — verified via `py_compile` on `app/routes/employee.py`/`app/util.py`, CSS brace-balance re-confirmed (314 open == 314 close), `node --check` on the ledger table's inline `<script>`, a new standalone Jinja render harness (`render_my_month.py`, mirroring `render_today.py`'s stand-in-filter/fake-ORM approach) built specifically for `my_month.html` with a full month of hand-placed Weekend/Missing/Partial/Leave/Overtime/compensated-Complete/plain-Complete days and two days with entries, and an HTML div-balance check against the rendered output — all confirming: the Compensation section string is entirely absent, Missing/Leave/Overtime/compensated-reads-Complete all render with the correct badge class, week-row count matches `weekly_ledger` length, every week/day row has all 7 `<td>`s aligned under the right header, and the structure is well-formed with zero unclosed tags.

- **My Month "Hours ledger": Entries dropdown fixed (real bug, not the display-only kind), Balance column removed, column gaps reduced (Ganesh, 2026-08-29, same day as the week→day→entries build above)** — three follow-ups from live testing. (1) **Bug**: "View (N)" never expanded. Root cause: the employee nav's "My requests ▾" dropdown (`<details class="navdrop">`, added earlier this same session to `base.html`) sits in `<header>`, ahead of `<main>` in the DOM, and has no ancestor `<tr>`. `my_month.html`'s own script ran `document.querySelectorAll('details').forEach(function(d){ var row = d.closest('tr').nextElementSibling; ... })` — for the nav's dropdown, `d.closest('tr')` returns `null`, so `.nextElementSibling` threw a `TypeError`, which aborted the whole `forEach` before it ever reached the ledger table's own `<details>` elements (`forEach` doesn't catch/skip a thrown exception — it stops there). Every "View (N)" summary still native-toggled open (that's the browser's own behavior, unrelated to this script), but nothing ever ran to reveal the sibling `entries-detail-row`, so it looked like clicking did nothing. This bug was a latent regression introduced by this session's own nav change — a plain employee's `/my-month` had zero `<details>` elements outside the ledger table before the nav dropdown existed. Fixed by null-guarding: `var tr = d.closest('tr'); var row = tr ? tr.nextElementSibling : null;`. (2) **Balance column removed** (Ganesh: "please remove balance coloumn we dont need that") — dropped entirely from both the week-total row and every day row (the `.tri` triangle CSS was only ever used here, so it was deleted too, not left as dead code); the ledger-tree table is now 6 columns (Week/Date, Status, Actual, Target, Variance, Entries) instead of 7, and `entries-detail-row`'s `colspan` was updated from 7 to 6 to match. The month-level "running hours balance" KPI tile above the calendar is untouched — it wasn't named in this request and isn't part of the confusing per-row repetition Ganesh was reacting to. (3) **Column gaps** — the previous fix (percentage-based `width` on a `table-layout:fixed` table) was the wrong lever: a percentage of a wide card's full width still stretches a short value like "8:00" across a proportionally large, visually empty column, which is exactly what "gap between columns is too big" was describing. Switched to fixed `px` widths on the narrow columns (Status 100px, Actual/Target 70px, Variance 80px, Entries 90px) with Week/Date deliberately left unset so it alone absorbs whatever width is left over — the standard `table-layout:fixed` technique for "one flexible column, the rest content-sized." No `app/routes/employee.py`/`app/engine.py`/`app/validation.py` changes for any of the three (template + CSS only) — verified via CSS brace-balance (311 open == 311 close), `node --check` on both of the page's inline `<script>` blocks (this page's own ledger script plus `base.html`'s), and the same `render_my_month.py` Jinja harness re-run with an added regression assertion that specifically extracts the script body and confirms the `tr ? tr.nextElementSibling : null` guard is present — confirming via a real render of the actual `base.html` nav (not a stand-in) that `.navdrop` really is present in the output alongside the ledger table, which is what makes this assertion meaningful rather than tautological.

- **Hours ledger: column gaps reduced again — table itself was still stretching full-width (Ganesh, 2026-08-29, third pass: "now the gap is too much please, reduce the gap")** — the previous fix gave every column except Week/Date a small fixed px width but left Week/Date unset so it would "absorb the rest"; on a wide card that just relocated the same full-bleed gap into one giant blank Week/Date column instead of removing it. Fixed by giving Week/Date its own fixed width (210px) too and setting `.ledger-tree { width: auto; }`, overriding the app-wide `table { width: 100% }` rule — with every column a fixed px and the table itself no longer forced to 100%, the table is now only ~600px wide total (210+90+70+70+80+80) and stops stretching to fill the card. Template unchanged, CSS-only — verified via CSS brace-balance (313 open == 313 close) and the same `render_my_month.py` harness re-run (still passes, including the Balance-removed and null-guard regression assertions from the prior two passes).

- **Hours ledger: card shrunk to fit the compact table instead of leaving dead space on the right (Ganesh, 2026-08-29, fourth pass: "now right side gap is looking odd, please adjust accordingly")** — the previous fix made the table itself compact (~600px, no longer stretched to the card's full width) but left it sitting inside the same full-page-width card as every other card on this page, so the empty space just moved from between columns to the right of the whole table. Asked which way to close it (AskUserQuestion) — "Shrink the card to fit the table" over "stretch the table back out" — so a new `.ledger-card { max-width: 640px; }` (table's ~600px plus the card's own 18px×2 padding) is applied only to this one card (`class="card ledger-card"` in `my_month.html`), leaving every other card on the page at its normal full width. Verified via CSS brace-balance (314 open == 314 close) and the same `render_my_month.py` harness re-run, extended to assert the `ledger-card` class is present on the rendered card.

- **Hours ledger: card back to full width, table centered inside it instead of shrinking the card (Ganesh, 2026-08-29, fifth pass: "still i am not satisfied, please adjust the hours ledger according to the screen")** — the previous fix (shrinking the card to a 640px `max-width`) traded one problem for another: it fixed the internal gap but left a small box floating on the left of the page with a lot of dead space beside it, since every other card here (calendar, KPIs) spans the full page width. Confirmed via AskUserQuestion — "Full-width card, table centered inside it" over stretching the table's columns back out or placing another card alongside it. `.ledger-card { max-width: 640px; }` was removed and the `ledger-card` class taken back off the `<div class="card">` in `my_month.html`, so this card is full-width again like every other one on the page; `.ledger-tree` (still `table-layout: fixed; width: auto;` with the same fixed per-column px widths from the last two passes) gained `margin: 0 auto` so the now-compact ~600px table sits centered within that full-width card rather than pinned to the left edge or stretched to fill it. Verified via CSS brace-balance (313 open == 313 close) and the same `render_my_month.py` harness re-run (the now-obsolete `ledger-card` class assertion was removed from the harness rather than left to fail).

- **Hours ledger: column gap reverted to plain default table styling (Ganesh, 2026-08-29, sixth pass, pasting a screenshot of the table from before any of the gap-tightening rounds: "please revert back to this table columns gap")** — after percentage widths, then fixed px widths, then a shrunk card, then a centered compact table each got tried and rejected in turn, Ganesh's clearest signal was to just go back to how the columns looked before any of this: plain default table styling, no per-column sizing at all. The `class="ledger-tree"` was removed from the table in `my_month.html`, and its entire CSS block (`table-layout:fixed`, the six `nth-child` width rules, the reduced padding/font-size, the `margin:0 auto` centering) was deleted from `app/static/app.css` — this table now inherits the exact same `table { width:100% }` / `th,td { padding:6px 8px }` rules every other plain table on the site already uses. Everything else from the earlier passes is untouched: the week→day→entries collapsible grouping, the Overtime status label, and the removed Balance column are separate asks and stayed exactly as they were — only the column spacing/sizing reverted. Verified via CSS brace-balance (306 open == 306 close, down from 313 since a whole rule block was deleted) and the same `render_my_month.py` harness (its `ledger-tree`/Balance-column assertions were updated to check the class is now ABSENT rather than present, and a new assertion confirms it).

- **Hours ledger: header labels and cell values centered under each column (Ganesh, 2026-08-29, seventh pass: "make the data under each column organizdad centalized")** — new `.ledger-centered` class (`class="ledger-centered"` on the table in `my_month.html`) sets `text-align: center` on every `th`/`td` in this one table, both the header row and every week/day row — text-align only, no width/table-layout/padding change, so this doesn't reopen the column-gap question the sixth pass just settled (plain default table styling). Verified via CSS brace-balance (307 open == 307 close) and the same `render_my_month.py` harness, extended to assert the `ledger-centered` class is present.

- **Hours ledger: Week/Date column kept left-aligned while every other column stays centered (Ganesh, 2026-08-29, eighth pass: "Week/Date column should be at left end only")** — one added rule, `.ledger-centered th:first-child, .ledger-centered td:first-child { text-align: left; }`, right after the centering rule from the previous pass — the date range/day label reads more naturally left-aligned as a row label, while Status/Actual/Target/Variance/Entries stay centered. Verified via CSS brace-balance (308 open == 308 close) and the same `render_my_month.py` harness re-run (still passes unchanged, since this is a pure CSS tweak with no markup/context changes to assert on).

- **Hours ledger: clicking anywhere on a day row toggles its entries, not just the "View (N)" text (Ganesh, 2026-08-29, ninth pass: "even if we clicked on that particular day row the entries should be open not only when we click on view entrie")** — new click listener on each `.week-day-row` (only attached when that row actually has a `<details>`, i.e. has entries — a row showing "—" gets no handler and no pointer cursor). Clicking the row sets `det.open = !det.open` and directly toggles the sibling `entries-detail-row`'s display itself, rather than relying on the `toggle` event firing from a programmatic `open` change (browser support for that varies) — so this doesn't depend on the existing `details`/`toggle` listener from the earlier bugfix pass at all, it's a second, independent way to reach the same visible state. A click that lands on the `<summary>` itself is deliberately ignored here (`ev.target.closest('summary')`) so the native summary click and this row click don't both fire and cancel each other out (double-toggle). Verified via `node --check` on both of the page's inline `<script>` blocks and the same `render_my_month.py` harness re-run.

- **My Month calendar view improved (Ganesh, 2026-08-29, "can we improvise this calendar view" — asked which improvements he wanted via AskUserQuestion, answered "No preference," so this is a design judgment call rather than a specific ask)** — two changes, both purely visual/read-only, no new interactions: (1) each day cell now tints its whole background by that day's status (green/yellow/red/tan using the same `--ok-bg`/`--warn-bg`/`--bad-bg`/`--leave-bg`/`--overtime-bg` vars the badges already use) instead of only the small badge being colored — much faster to scan a month for problem days than reading every badge individually; Weekend/Holiday cells deliberately keep their existing muted `.off` look rather than also being tinted, since they're not a compliance signal. (2) Today's cell gets a `2px` accent outline (`.cal td.today`) plus a small "· today" label next to the day number, so the current date is easy to spot in a full month grid. Both reuse `_ledger_display_status(r, comp_erases)` (the same Complete→Overtime layering the Hours ledger already applies) for the calendar's own status badge/tint, computed once in `my_month()`'s existing weeks-grid-building loop and stored as `cell["display_status"]` — this was previously computed inline in the template via `r.effective_status(comp_erases)`, so the calendar and the Hours ledger below it were technically capable of reading a Complete-with-positive-variance day differently; now both read it as "Overtime" consistently. No `app/engine.py`/`app/validation.py` changes (still the same underlying `DayStatus`/`effective_status()` data, just relabeled the same way the ledger already does, and a new CSS-only tint/outline) — verified via `py_compile`, CSS brace-balance (320 open == 320 close), the `render_my_month.py` harness extended with `display_status` on its calendar-grid cells (matching the real route's new field), and a direct check of the rendered HTML confirming all 5 tint classes (`cal-complete`/`cal-partial`/`cal-missing`/`cal-leave`/`cal-overtime`) appear and that today's cell carries the `today` class and "· today" label.

- **Today page "My Month" card gained a mini working-days calendar (Ganesh, 2026-08-29: "bring that calendar into my month section in today page, no need to display weekend days... just display how many hours worked... +1hr / -2hrs")** — added below the existing Logged/Effective target/Running balance/Missing/Partial/Strikes/Compliance rows in the same card, not a replacement. `_month_summary()` (`app/routes/employee.py`) now also returns `calendar_headers` (this employee's actual work weekdays, e.g. `["Mon",...,"Fri"]`) and `calendar_weeks` — built by reusing `engine.is_working_day()`'s own definition of a working day (`emp.work_day_set` minus the shared `engine.holidays_set(db)`) rather than hardcoding Mon-Fri, so an employee on a non-Mon-Fri schedule still gets the right columns. Column count is fixed per employee and rendered for every week, even a week containing a holiday on an otherwise-normal work weekday (that day gets its own "Hol" cell instead of being dropped) — dropping it instead would shift every later cell in that row into the wrong weekday column. Per cell: just the day number and that day's `variance_minutes|hm_signed`, colored ahead/behind (`--ok`/`--bad`) — no status badge, no actual/target text, deliberately more compact than My Month's own full calendar since this sits in Today's narrower right column. A day beyond `effective_end` (hasn't happened yet) or with no computed row shows a muted "—"; a full-day Leave shows "Leave" instead of a (usually zero, uninformative) variance number. New `.mini-cal`/`.mini-cal-row`/`.mini-cal-cell` CSS (`app/static/app.css`) uses `flex`, not a fixed-column CSS grid, specifically because the column count varies by employee. No `app/engine.py`/`app/validation.py` changes (only new calls into existing `engine.is_working_day`/`holidays_set`, no new business rules) — verified via `py_compile`, CSS brace-balance (322 open == 322 close), the `render_today.py` harness (both `month_summary` fixtures extended with `calendar_headers`/`calendar_weeks`, including a holiday/leave/logged/future mix in the populated scenario, all 7 scenarios still render and stay HTML-balanced), and a direct check of the rendered output confirming the headers, the colored +1:00/-8:00 variances, and the "Hol" label all appear, and that the whole card (mini-cal included) is correctly absent on a past day.

- **Today page "My Month" card trimmed to Logged/Effective target + calendar; mini calendar now includes Saturday/Sunday (Ganesh, 2026-08-29, two quick follow-ups)** — (1) the Running balance/Missing days/Partial days/Strikes/Compliance rows were removed from this card's display (`today.html`) since the mini calendar added just above already shows each day's own variance directly, and the full strike/compliance breakdown still lives on the real My Month page; `_month_summary()` itself is unchanged and still computes/returns all of these (only the template stopped rendering them), so nothing else that might read this dict later loses data. (2) `calendar_weekdays` in `_month_summary()` changed from `sorted(emp.work_day_set)` to `sorted(set(emp.work_day_set) | {5, 6})` — Saturday and Sunday now always get their own column regardless of the employee's normal schedule, since a weekend day still gets a computed `DayStatus` row (`status='weekend'`, target usually 0) via the same `recompute_employee()` call already made above, so any voluntary weekend hours show up as a normal colored variance the same as any other day, rather than being hidden. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` and the `render_today.py` harness (fixture extended to a full 7-day week including Sat/Sun with logged hours, all 7 scenarios still render and stay balanced; confirmed "Running balance"/"Missing days"/"Partial days" strings are gone from the rendered card and "Sat"/"Sun" now appear in the mini calendar's header row).

- **Today page card headers became their own separated header bar (Ganesh, 2026-08-29, "i want seperate box for header it should not be inside the ssection and please check padding pace, section wise space and all")** — every card on this page whose `<h2>` sat as the plain first line of text inside the same padded body (Plan for the day, My Month, Reminders, Today's Plan, the read-only past-day Plan card, Auto time capture, Add row) now wraps that heading in a `<div class="card-head">`. New CSS (`app/static/app.css`) gives `.card-head` a negative margin that exactly cancels the parent `.card`'s own `8px 14px` padding (`-6px -12px` for the smaller `.compact-card`), then re-applies its own padding plus a `background: var(--pending-bg)` and `border-bottom` — so the header reads as a distinct bar flush with the card's edges (with matching rounded top corners) sitting above the body content, rather than just being styled text one line into the same box. Task log has no `<h2>` at all and wasn't touched — nothing to separate there. The now-dead `.today-page .card > h2:first-child`/`.compact-card > h2:first-child` margin rules (no longer matched by anything, since the `<h2>` is nested one level deeper now) were removed rather than left as unused CSS; `.card-head h2`/`.compact-card .card-head h2` carry the equivalent margin/font-size instead. The "check padding/section-wise space" half of the ask was a sanity pass, not a separate list of fixes — confirmed the header's own padding lines up flush with the body's left edge (same 14px/12px the card already used) and that the 10px/6px gap between header and body reads as intentional spacing, not a stray gap; nothing else stood out as needing its own fix. No `app/routes/employee.py`/`app/engine.py`/`app/validation.py` changes (template + CSS only) — verified via CSS brace-balance (324 open == 324 close), the same 7-scenario `render_today.py` harness (all render and stay HTML-balanced, zero unclosed/mismatched tags), and a direct count confirming exactly 6 `card-head` elements render in the fully-populated scenario (one per card that has a real title, matching the list above).

- **Nav label "Today" renamed to "My Day"; "Add row" renamed to "Add Task"; Task log gained a "Today Tasks" header (Ganesh, 2026-08-29, following a naming brainstorm — "Keep My Day and instead of Add row make it Add Task and for Task logs i want header also 'Today Tasks'")** — three label-only renames, no route/id/active_nav-key changes anywhere (matches the precedent "My Month" → "My hours" already set earlier this session — same route, same `active_nav` value, label text only). `base.html`'s two employee nav branches: `>Today<` → `>My Day<` (both the plain-employee and admin-as-employee copies). `today.html`: the header's quick-jump link (`<a href="#addrowcard">`) and the Add row card's own `<h2>` both changed to "Add Task" (still the same `#addrowcard` anchor/id, same route), and the Auto time capture help paragraph's hover tooltip ("Same rules apply as Add row...") was updated to match so it doesn't reference a name that no longer exists on the page. Task log had no header at all before this — it now gets the same `<div class="card-head"><h2>Today Tasks</h2></div>` treatment every other card on this page just got (see the header-separation bullet above), rather than a bare `<h2>` that would've needed the box treatment redone. The page's own `<h1>` ("Today · {weekday}, {date}") was deliberately left alone — only the nav label and these two card titles were named in the request. No CSS/Python changes — verified via the same 7-scenario `render_today.py` harness (all still render and stay HTML-balanced) and `render_my_month.py` (exercises the same `base.html` nav), plus a direct check of the rendered output confirming "My Day"/"Add Task"/"Today Tasks" all appear and neither the old ">Today<" nav label nor any "Add row" text remains anywhere on the page.

- **My Month page: full month calendar removed (Ganesh, 2026-08-30, pasting a screenshot of it: "Remove this rom My Hours")** — the calendar card (the Mon-Sun grid with tinted status cells and the today-outline, built earlier 2026-08-29 in the "improvise this calendar view" pass) is gone from `my_month.html` entirely — the KPI tiles above it and the Hours ledger below it are unaffected, unchanged. `my_month()` (`app/routes/employee.py`) no longer builds the `weeks` calendar grid (the whole block that grouped `rows` into Mon-first weeks with `display_status` per cell) or passes `weeks` in the render context, since nothing reads it anymore; the now-dead `.cal`/`.cal-*`/`.cal td.today` CSS block (`app/static/app.css`) was deleted too — confirmed via grep that no other template references `class="cal"`, and confirmed `--overtime`/`--overtime-bg` (the CSS vars that block also used) are still needed and kept, since `.b-overtime` on the Hours ledger's Status badge still uses them. Note this doesn't touch the *other* new calendar built the same week — the compact working-days mini-calendar added to the Today page's "My Month" card (`_month_summary()`'s `calendar_weeks`/`calendar_headers`) is a completely separate helper/template block and is untouched by this removal. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile`, CSS brace-balance (313 open == 313 close, down from 322), and the `render_my_month.py` harness (its calendar-grid fixture construction was removed since nothing consumes it anymore, and two new assertions confirm `class="cal"` and the old "today's cell is outlined" legend caption are both gone from the rendered output).

- **Today page: Mon-Sun week-position circles added next to the Working date picker (Ganesh, 2026-08-30, pasting a screenshot: "Here right side i want week days in circle Mon, Tue, Wed, Thu, Fri, Sat, Sunday and for suppose today is Tue then remainig from Wed to Fri circle should be some other color indicates remaining working days in that week , today one should be other color highliting and completed day Mon should be greyed out")** — new `_week_day_circles(today)` in `app/routes/employee.py` is a pure date-position function, no DB/DayStatus/TaskEntry lookup at all: for the real calendar Mon-Sun week containing `today`, each day gets exactly one of three states — `past` (before today), `today`, or `upcoming` (after today, still this week) — deliberately NOT tied to whether hours were actually logged that day, since the ask was about calendar position ("completed day Mon should be greyed out" reads as "already happened", not "already logged"), unlike `_week_summary()`'s pre-existing (and, since the 2026-08-29 "This Week" card removal, no-longer-rendered) `day_badges`, which the ask was never referring to and which stays untouched. `today_page()` passes this as a new `week_circles` context var, computed from the real `today` — never from `day` (the possibly-different date the picker is currently browsing) — so it always reflects the actual current week regardless of which date's task log is on screen; this is confirmed by a dedicated harness assertion rendering the `past_day` scenario (`day` two days before `today`) and checking the circles still mark the real `today`, not `day`. `today.html`'s "Working date" card was restructured into a flex row — the existing date-picker `<form>` on the left, a new `<div class="week-circles">` of 7 `<span class="week-circle {{ c.state }}" title="{{ c.date|mdy }}">{{ c.label }}</span>` on the right (hover shows the full MM/DD/YYYY date) — both inside the same card, no new card added. New CSS (`app/static/app.css`): `.week-circles` (flex row, small gap) and `.week-circle`/`.week-circle.past`/`.week-circle.today`/`.week-circle.upcoming` (small circles, reusing existing tokens — `past` uses the muted `--pending-bg`/`--muted` pair, `today` uses `--accent`/`--accent-ink` matching the active nav-tab highlight, `upcoming` uses `--ok-bg`/`--ok`). No `app/engine.py`/`app/validation.py` changes (pure date arithmetic + template/CSS) — verified via `py_compile app/routes/employee.py`, CSS brace-balance (318 open == 318 close), the `render_today.py` harness extended with a `_week_day_circles` fixture mirror plus assertions across all 7 scenarios confirming exactly 7 circles with all 7 weekday labels render, and — since 08/29/2026 (this harness's fixed `today`) is a Saturday — an exact per-scenario check that Mon-Fri render `past`, Sat renders `today`, and Sun renders `upcoming`, plus the `past_day`-scenario regression check above, and the existing `check_structure.py` HTML div-balance pass (all 7 scenarios still balanced).

- **Nav labels "My hours"/"My requests" capitalized to "My Hours"/"My Requests" (Ganesh, 2026-08-30, from a screenshot: "captilize the H in hours, and r in Requestes")** — label-only fix in both employee nav branches of `base.html` (plain-employee and admin-as-employee), matching the title-case convention every other nav label on this bar already uses ("My Day", "Support", "Profile", "Leave Request", "Overtime Request"). No route/`active_nav`-key/behavior change. Verified via the `render_today.py`/`render_my_month.py` harnesses (both render `base.html`'s real nav) re-run clean, plus a direct rendered-output check confirming `>My Hours<` and `My Requests ▾` appear and the old lowercase `>My hours<`/`My requests` strings are gone from the live UI text (the historical comment quoting the original 2026-08-29 request, which reads "My hours My requests" as a record of what was asked then, was left as-is since it's not rendered UI).

- **Admin nav reordered/relabeled: My Work, Dashboard, Team Requests, Projects & Tasks, Employees, Analytics & Reports, Settings & Configuration, Support (Ganesh, 2026-08-30, pasting two screenshots — the app's own admin dashboard and a different app's simpler nav as naming inspiration: "We should have My Work , Dashboard, Team Requests, Projects & Tasks , Employees, Analytics & Reports , Settings & Configuration, Support / My Work should redirect to employee dashboard / Dashboard should be same as of now / Under team requests i need - Leave Management, Overtime management / employees management should be Employees / Analytics & Reports is same")** — a relabel/reorder of the admin `<nav>` in `base.html` only, deliberately **not** a permissions change: every item keeps exactly the gating it already had. "Employee login" (bottom of the old nav, visible to every admin tier, linking to `/today`) is renamed **My Work** and moved to the very first position — same route, same "redirect to employee dashboard" behavior, just renamed/repositioned. **Dashboard** (`/admin`) is untouched, second position as before. **Team Requests** is a new `.navdrop` wrapping the two previously-standalone top-level links **Leave Management** (`/admin/leave`) and **Overtime Management** (`/admin/overtime`) — still `{% if user.is_super_admin %}`-gated exactly as those two links always were (see the 2026-08-28 "Department-scoped admin narrowed to 5 capabilities" bullet above; this change does not reopen that). Its summary shows a **combined** badge (`(nav_badges.pending_leave or 0) + (nav_badges.pending_overtime or 0)`) alongside each submenu item still showing its own individual count — `_admin_nav_badges()` (`app/templating.py`) was not touched, both counts already existed and are simply summed in the template. **Projects & Tasks** is the old "Projects & Task Management" dropdown, label only — its own submenu already had a same-named "Projects & Tasks" link (`/admin/lists`) before this change and still does; that inner link and the outer dropdown label happening to match is fine, they're at different nesting levels. **Employees** is `/admin/roster`, renamed from "Employee Management," still super-admin-only. **Analytics & Reports** is the old "Analytics and Reports" dropdown — Ganesh said "is same" so only the `and`→`&` label text changed, contents/gating (visible to every admin tier, Audit Logs sub-item still super-admin-only) untouched. **Settings & Configuration** (singular, was "Settings & Configurations") and **Support** (was "Contact Support") are unchanged otherwise, still super-admin-only. Because the request's order interleaves two super-admin-only items (Team Requests, Employees) around two all-admin-tier items (Projects & Tasks, Analytics & Reports), the previous single contiguous `{% if user.is_super_admin %}` block wrapping everything from Leave Management through Contact Support had to become three separate guards instead of one, to physically reorder without changing who sees what — verified explicitly for both tiers (see below). `README.md` and a stray comment in `base.html`'s own employee-zone branch were updated to stop naming the old "Employee login"/"Analytics and Reports" labels. No `app/engine.py`/`app/validation.py`/route changes at all (template-only) — verified via a new `render_admin_nav.py` harness (a minimal `test_admin_nav.html` extending the real `base.html`, rendered once as a super admin and once as a department-scoped admin): confirms the super admin's 8 items appear in exactly the requested left-to-right order, every stale label ("Employee Management," "Contact Support," "Settings & Configurations," "Analytics and Reports," "Projects & Task Management," "Employee login") is completely gone, the Team Requests summary shows the correct combined badge (2+5=7 in the fixture), and the department-scoped admin's nav correctly still has no Team Requests/Employees/Settings & Configuration/Support at all (Projects & Tasks and Analytics & Reports still present, per their existing all-admin-tier gating) — plus an HTML div-balance check on both renders (both balanced) and the pre-existing `render_today.py`/`render_my_month.py` harnesses re-run clean to confirm this `base.html` change didn't regress the employee-zone nav branch.

- **Projects & Tasks: "All departments" gained a Category -> Task grouping with an hours rollup (Ganesh, 2026-08-30, pasting a mockup screenshot: "these are common for all departments")** — four AskUserQuestion calls settled scope before touching anything, since this introduces a concept (task category) and a metric (hours worked) that didn't exist anywhere in the app: (1) scope — "Only 'All departments'" (Recommended), not every department group, so every department-specific tree node keeps its existing Project -> Task structure untouched; (2) the existing shared-projects list (with its own Case Type/Manage departments/narrowed-task-list edit controls) — "Keep both", so the new Category -> Task view sits ABOVE that list inside the same "All departments" node rather than replacing it; (3) where "category" comes from — "New freeform field, admin-set" over a fixed preset list, so General/Meetings/Printing are just examples, not a hardcoded enum; (4) what the "hrs" figure means — "All-time total" (sum of every `TaskEntry` ever logged against that task, org-wide, no date filter), not a monthly/yearly window. New `TaskType.category` column (`app/models.py`, `String(60), default="General"`) — admin-editable freeform text via a new dedicated route, `POST /admin/lists/task/{id}/category` (`lists_task_category`, super-admin-only, same tier as Rename/Deactivate/Manage-projects) — kept separate from the existing `lists_rename()` route rather than folded into it, since that route early-returns when the name is unchanged and a category-only edit with no name change would silently no-op if it shared that early-return; blank input resets to "General" rather than leaving the column empty. Same SQLite-ADD-COLUMN-never-backfills gap as every other additive column this project has hit — `ensure_task_category_backfill()` (`app/util.py`, wired into `main.py`'s startup sequence) backfills every pre-existing NULL row to "General", mirroring `ensure_list_status_backfill`'s exact pattern; unlike most of this session's other backfills, this one HAS a real pytest class (`TestEnsureTaskCategoryBackfill` in `tests/test_util.py`, mirroring `TestEnsureListStatusBackfill` byte-for-byte in structure) since `tests/test_util.py` already tests this exact family of functions and the pattern was trivial to extend. `lists_page()` (`app/routes/admin.py`) computes `task_hours` (all-time hours per task, org-wide — `TaskEntry` grouped/summed by `task_type_id`, floor-divided into whole hours, never a float per CLAUDE.md's integer-minutes hard rule) and `shared_task_categories` (only the truly unrestricted tasks — `task_project_ids` empty, the same set `unrestricted_task_count` above it already counts — grouped by `category or "General"`, alphabetical by category name, each category showing its task count and summed hours). A project-scoped task (narrowed to one or more projects via `ProjectTask`) is deliberately excluded from this grouping even if it has a category set, since it already has a home under its own project node elsewhere in the tree — confirmed by a dedicated test case, not just an assumption. `admin/lists.html`'s "All departments" `<summary>` line changed to "shared firm-wide · N project(s) · M task(s)" (was just "N project(s)"), and a new `.tree-cat-list` block renders each category as its own nested `<details class="tree-cat">` (reusing `.tree-proj`'s exact box/summary/car-rotate CSS by extending those selectors, not duplicating rules) with a `.tree-hrs` span pushed to the row's far right via `margin-left:auto` (same trick `.tree-row-acts` already uses) for both the category-level and per-task hours figures. The "All Tasks" master table gained a Category column with the same inline `<details class="details-edit inline">` rename-style edit affordance the Task column already had, since that table is still the one place every task appears regardless of project linkage (same reasoning CLAUDE.md already gives for Rename/Deactivate living there). Deliberately not wired into `treeFilter()`'s search/expand-all — the category rows always stay visible regardless of the search box, a scoped-down choice (not asked for) rather than teaching the existing project-only filter a second, differently-shaped tree to search. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` across every touched `.py` file, CSS brace-balance (320 open == 320 close), a `render_lists.py` Jinja render+assert harness (mockup's own numbers reproduced exactly — General 102 hrs/4 tasks, Meetings 72 hrs/3 tasks, Printing 9 hrs/2 tasks — plus confirming a project-scoped task never leaks into the shared grouping even with its own sky-high fake hours total), an HTML div-balance check on the rendered output, a standalone simulation extracting `lists_page()`'s real grouping/hours-aggregation source and executing it directly (not hand-copied) against 4 cases (the mockup numbers, floor-division rounding on an odd minute total, a task with zero logged minutes defaulting cleanly to 0 hrs, and a None category falling back to "General"), a pure-logic check of `lists_task_category`'s strip/default/no-op-when-unchanged rule against 5 cases, and the new `TestEnsureTaskCategoryBackfill` pytest class (still needs a real `pytest` run to confirm, not just `py_compile` — same missing-legacy-bundle/broken-.venv gap as every other test added this session).

- **Projects & Tasks: "All Tasks" master table removed entirely (Ganesh, 2026-08-30, right after the Category feature above shipped, reacting to a screenshot of its new Category column: "complete remove the All tasks section")** — two AskUserQuestion rounds settled scope, since deleting this card is a real capability loss, not a cosmetic tweak: the first round narrowed "remove this" from "just the Category column" to "the whole All Tasks table"; the second round spelled out exactly what goes with it — the table was the ONLY place to add a brand-new task not already scoped to a project, and the only rename/Deactivate/Reactivate/"manage projects"/Category-edit controls for any task NOT already nested under a specific project's tree node (the large majority — 33 of 38 tasks, per the tree build's own numbers). Ganesh chose "Drop it entirely" over keeping a minimal Add-task form, so `admin/lists.html` lost: the "All Tasks (N active)" card heading, its "Add task type" + Project-combo form, its task search box, and the whole `#tasks-table` (Task/Category/Projects/Deactivate columns, one row per task org-wide). The stray `{% if unrestricted_task_count %}` pointer paragraph directly above it ("...manage them in the All Tasks list below") was removed too rather than left dangling; `unrestricted_task_count` itself is untouched since the "All departments" header meta line (from the Category feature above) still reads it. `combo.js`'s page-specific `<script src>` and its one `initCombo(...)` call were also removed — that combo picker only ever fed the now-gone Add-task form, so nothing on this page needs `combo.js` anymore (`msfilter.js`, used only by the removed table's own "manage projects" checkbox search, needed no page-specific removal since `base.html` already loads it globally for every admin page). `active_projects_json` was dropped from `lists_page()`'s render context (`app/routes/admin.py`) for the same reason — it only ever fed that same combo's `tojson` script tag. Two now-stale text references were updated rather than left dangling: the per-project "+ Task" quick-add form's comment (still the ONLY way left to add a task, now that the master Add form is gone) and the "no tasks narrowed to this project" empty-state message, which pointed at "All Tasks below" and now points at the Category groupings under "All departments" instead. **Explicitly NOT removed** — `TaskType.category` (the column), `ensure_task_category_backfill()`, the `lists_task_category` route, and the "All departments" Category -> Task grouping with its hours rollup all stay exactly as shipped in the bullet above; only their one UI entry point for *editing* a category went away. This is a known, accepted gap from here on — a task's category can now only ever be set at creation-time-implicitly-"General" or via direct DB access, never edited through the app — flagged here rather than silently left for someone to discover later. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` on `app/routes/admin.py`, CSS brace-balance unchanged (no CSS was touched — every class the removed markup used, `.searchbar`/`#tasks-table`/`.ms-box`/`.ms-opt`/`.ms-search`/`.details-edit`, is a generic component also used by half a dozen other admin templates, confirmed via grep, so none of it was dead CSS to clean up), the same `render_lists.py` harness re-run with its category-edit-form assertions inverted to confirm absence (the All Tasks heading, `#tasks-table`, the Add-task form, the search box, the category route path, and the combo picker's id/init call are all now confirmed GONE) while confirming the "All departments" Category grouping still renders correctly, and an HTML div-balance check on the result (still balanced).

- **Overtime Management: duplicate "Pending overtime requests" section removed, "Compensation links" renamed, its employee picker narrowed, and Surplus days switched from checkboxes to a multi-select (Ganesh, 2026-08-30, from two screenshots)** — four related asks in one message. (1) *"why two time we had pending overtime requests, remove one section"*: the page had the real actionable card near the top (Approve/Reject buttons, gated `{% if pending %}`) and a second, purely read-only card further down whose own copy said "same list as 'Pending overtime requests' above, searchable here... Use the Approve/Reject buttons above to act on one" — i.e. it was a self-documented duplicate added later just to add a search box. Removed that second card (`admin/overtime.html`) entirely rather than the actionable one; no Python change, `pending` was already computed once and the first card is unaffected. (2) *"we have comentation links right change it to Missed Hours and Compensation"*: the `<summary><h2>Compensation links</h2></summary>` card title on THIS page (Overtime Management only) is now "Missed Hours and Compensation" — a later paragraph on the same page ("the same 'surplus' figure the Compensation Links picker above sums per day") was updated to match. Person Detail's own separate "Compensation links" card (`admin/person.html`) is untouched — the screenshots were specific to Overtime Management, and CLAUDE.md's own history treats these as two intentionally-separate views onto the same `CompensationLink` rows. (3) *"employee field it should show list of only who are missed the hours and have compensated hours"*: the "Link a shortfall to make-up day(s)" employee dropdown previously listed every employee in the admin's scope (`emps`) regardless of whether picking them led anywhere; it now only lists employees who currently have BOTH an unlinked shortfall day AND an available surplus day for the month being viewed (`ym`) — a new `link_eligible_emps` list computed in `overtime_page()` (`app/routes/admin.py`). Built for cost, not per-employee queries in a loop: reuses `engine.recompute_all(db, first, min(last, today))`, the exact same bulk-refresh call Dashboard already makes ("live months stay fresh on load (cheap at this scale)"), then `engine.statuses_for_month()` for one batched org-wide DayStatus query instead of one per employee; the two remaining per-employee queries (`shortfall_allocated_minutes_by_date`/`surplus_minutes_used_by_date`) only run for employees that already passed a free in-memory "has any shortfall row and any surplus row this month" check, not all ~45. `link_target` itself (the currently-selected employee, from the URL's `employee_id`) is still looked up against the full unfiltered `emps`, not `link_eligible_emps` — so a link already in progress doesn't break if that employee's eligibility changes between page loads; only the dropdown's own OPTIONS are narrowed. "Grant overtime directly"'s and "Who worked overtime"'s own employee lists are both untouched (still the full `emps`) — the narrowing is scoped to this one picker only, confirmed by a dedicated test assertion. (4) *"surplus days should also like shortfall day dropdown from there we can select multiple surplus days"*: the Surplus day(s) field was a row of checkboxes; it's now a `<select name="surplus_dates" multiple>` styled like the Shortfall day dropdown next to it, `size` set to show every option at once (capped at 6) since a multi-select collapsed to one visible row hides the fact that more options exist below. No backend change needed — a `<select multiple>` submits every chosen option's value under the same repeated `surplus_dates` field name a checkbox list already did, and `add_complink()`/`request_compensation_match()` both already take `surplus_dates: list = Form([])`. A fully-used surplus day still renders as a disabled option (not silently dropped), same as before. No `app/engine.py`/`app/validation.py` changes (only new calls into existing `engine.recompute_all`/`statuses_for_month`/`shortfall_allocated_minutes_by_date`/`surplus_minutes_used_by_date`, no new business rules) — verified via `py_compile` on `app/routes/admin.py`, a new `render_overtime.py` Jinja render+assert harness (confirms exactly one "Pending overtime requests" section renders and the removed card's own table id/copy are gone, the section title reads "Missed Hours and Compensation" with the old title fully absent, the Link-picker's employee dropdown contains only the one fixture employee with both a shortfall and a surplus while "Grant overtime directly"'s own dropdown still lists everyone, the Surplus day(s) field is a real `<select multiple>` with no leftover checkboxes, and a fully-used surplus day still renders as a disabled `<option>` rather than vanishing), and an HTML div-balance check on the rendered output (balanced).

- **Overtime Management: Surplus day(s) reverted from a `<select multiple>` to a closed dropdown with a click-to-toggle checklist (Ganesh, 2026-08-30, same-day follow-up: "surplus days field looking bad i want like shortfall day filed only but once we click it should open drop down and we can select multiple surplus days by mouse click")** — the previous pass's `<select multiple size="N">` technically worked but rendered as a permanently-open multi-row listbox with no closed state, and selecting more than one option needs a Ctrl/Cmd-click on most browsers/OSes — neither matches "like shortfall day field" (a normal one-line closed box) or "select multiple... by mouse click" (plain clicks, no modifier key). Replaced with a new small hand-rolled widget: `.msdrop` (`app/static/app.css`) is a toggle `<button>` styled identically to a plain `<select>` (same border/radius/padding the global `input, select, textarea` rule already sets, so it visually matches the Shortfall day field beside it) that reveals a `.msdrop-menu` panel — `position:absolute`, so opening it floats over the page instead of pushing the Note/Link button sideways. The panel's own box/row look reuses the EXISTING `.ms-box`/`.ms-opt` classes (the same "manage projects"/"manage departments" checklist styling already used elsewhere in admin templates) rather than inventing new box CSS — only the floating positioning and the disabled/strikethrough treatment (`.msdrop-opt.disabled`) are new. Crucially this went BACK to plain `<input type="checkbox" name="surplus_dates">` rows inside the panel (same as the very first, pre-multi-select-dropdown version) — not a new form control — so `add_complink()`/`request_compensation_match()` (`surplus_dates: list = Form([])`) needed no backend change for the second time in the same day; only the checkbox list's container changed from "always visible" to "hidden until the toggle button is clicked." Three small JS functions in a new page-specific `<script>` block (`admin/overtime.html`, same "small hand-rolled JS, no framework" convention as `combo.js`/`msfilter.js`/`tablefilter.js`): `toggleMsDrop()` opens one panel and closes any other `.msdrop-menu` on the page first (only one open at a time, though only one exists on this page today); a document-level click listener closes an open panel on any click outside `.msdrop`; `updateMsDropLabel()` rewrites the toggle button's own text to reflect the current selection ("Choose surplus day(s)" / the one selected day's own label text / "N surplus day(s) selected") so the closed state still shows what's picked, same as a native `<select>` would. A fully-used surplus day still renders as a disabled, struck-through checkbox row (never silently removed) — same guarantee the `<select multiple>` version and the original checkbox-row version before it both made. No `app/engine.py`/`app/validation.py`/`app/routes/admin.py` changes at all this pass (template + CSS + inline JS only) — verified via CSS brace-balance (327 open == 327 close), the `render_overtime.py` harness (its own assertions from the `<select multiple>` pass were inverted/rewritten to confirm that markup is GONE and the new `.msdrop`/checkbox/JS-function markup is present, including the fully-used-checkbox-disabled check), an HTML div-balance check on the rendered output (balanced), and `node --check` on both of the page's inline `<script>` blocks (the new one and `base.html`'s nav script, both syntactically valid).

- **Time by Project/Task: Employees/Projects/Tasks filters became closed dropdowns, "By Project" became a nested bar chart; `.msdrop`'s JS promoted to a shared file (Ganesh, 2026-08-30, from two mockup screenshots: "Here alos i want like department dropdown kind field then it will open we can make selection\nand i want project tracking like this bar")** — two related asks on this one page, plus one refactor triggered by the first. (1) *"department dropdown kind field... it will open we can make selection"*: read as "make Employees/Projects/Tasks behave like the Department field next to them" — Department was already a plain closed `<select>`, while Employees/Projects/Tasks were always-open `.ms-box` checkbox lists (the same `msfilter.js`-driven searchable picker built 2026-08-24). All three now use the exact `.msdrop` closed-dropdown-with-checklist widget Overtime Management's Surplus day(s) field introduced earlier the same day — click the toggle to reveal a panel holding the (unchanged) `.ms-search` input + `.ms-box` checkbox list, click elsewhere to close. Scoped to this one report only, per the literal "Here" — Attendance/Strikes/Task Logs keep their existing always-open filter boxes; not migrated, not asked for. (2) *"project tracking like this bar"*: the "By Project" card's plain `<table>` (Project/Task/Time columns, shipped 2026-08-24) is replaced with a nested horizontal bar list — `.proj-bars`/`.proj-bar-row`/`.proj-bar-sub` (`app/static/app.css`) — matching the pasted "Where my teams' hours went" mockup: a bold bar+total per project, indented smaller bars for its tasks underneath. No backend change — `reports.time_by_project_report()`'s existing `projects` list (already sorted busiest-first, `{"project", "total", "tasks": [{"task","minutes"}, ...]}`) is rendered as bars instead of table rows, purely in the template. Every bar's width — project row AND every nested task row, regardless of which project it's under — is a percentage of the SAME reference value (`project_result.projects[0].total`, the single busiest project, since the list is already sorted), not re-scaled per project, so any two bars anywhere in the list stay visually comparable — confirmed against the mockup's own numbers (Case Processing 494:50 renders as the 100% reference bar, Tech Team 396:20 renders at ~80%, exactly matching the mockup's proportions). The Pie chart toggle button/SVG is completely untouched — it's an alternate view of the same data, unaffected by switching the default table into bars. (3) *Refactor, not asked for but required to avoid a second copy-paste*: the `.msdrop` open/close + toggle-label JS existed only as three functions hand-copied inside `admin/overtime.html`'s own `<script>` block (built earlier 2026-08-30) — with a second page now needing the identical behavior, that code moved to a new shared `app/static/msdrop.js` (loaded globally via `base.html`, same as `msfilter.js`/`tablefilter.js`), matching this app's own "once a second page needs it, it becomes a shared file" precedent (`combo.js`). The generic version reads two data attributes (`data-empty-label`, `data-noun-plural`) so each `.msdrop` instance can show its own "nothing picked" text ("All Employees" / "All Projects" / "All Tasks" / "Choose surplus day(s)") and its own plural noun for the "N ... selected" count — `admin/overtime.html`'s markup was updated to carry those two attributes and its inline `onclick`/`onchange`/function-definition `<script>` block was deleted outright now that the shared file auto-wires every `.msdrop` on the page by event delegation (no per-element attribute needed for wiring itself, only for the label text). New `.msdrop-panel` CSS class carries the background/border/padding this page's panel needs (since it wraps a search input above the checkbox box, unlike Overtime's checkbox-only panel, which still combines `class="msdrop-menu ms-box"` on one element exactly as before) — added ALONGSIDE the existing `.msdrop-menu`/`.ms-box` rules, never modifying their own properties, specifically so Overtime's already-shipped surplus-day dropdown renders pixel-identical to before this change; `.msdrop-menu`'s `max-height` was bumped 220px → 260px to fit the added search box comfortably, which only ever makes the cap looser (harmless for Overtime's shorter, search-less content). No `app/engine.py`/`app/validation.py`/`app/routes/*.py` changes anywhere in this pass (template + CSS + JS-file-move only) — verified via a standalone Jinja2 render harness (fastapi/sqlalchemy aren't importable in this sandbox, so `app/templating.py`'s real filters/globals were mirrored by hand, same precedent as every other template-only change this session) rendering both `admin/reports_time.html` and `admin/overtime.html` with fixture data matching the mockup's own numbers: confirms all three new `.msdrop` filters render with the correct empty-labels, the old `#project-table` id and old checkbox-only filter markup are gone, the bar chart renders the expected row/percentage structure (100%/80%/40.1%/24.8%/53.4% against the fixture, matching hand-calculated ratios), Overtime's page no longer contains the duplicate `toggleMsDrop`/`updateMsDropLabel` function definitions or old inline `onclick`/`onchange` attributes, `base.html` loads `msdrop.js` exactly once, and an HTML div-balance check on both rendered pages (both balanced) — plus `node --check` on the new `msdrop.js` file.

- **Dashboard gained a "Compliance Trend" card — weekly % compliant, dashed target line, and a real root-cause note (Ganesh, 2026-08-30, from the same mockup pair as the bar chart above: "i want his also to be added")** — this is the "8-week trend sparkline... needs a week-over-week historical rollup nothing in this app computes yet" flagged as a follow-up when that mockup pair first came up; this bullet is that rollup. Three scoping questions were asked (AskUserQuestion) before writing anything, since each one changes the implementation materially: (1) placement — **Admin Dashboard** (`/admin`), not Strikes report or next to the Time by Project/Task bar chart; (2) the "Two people in Case Processing account for the whole drop" line — **real root-cause detection**, not a placeholder caption; (3) what "compliance %" means — **% of employees under the existing strike threshold** (same compliant/at-risk definition My Month's own badge already uses), not a new "% of workdays fully logged" measure. New `reports.compliance_trend_report(db, emps, weeks, threshold, comp_erases, today, target_pct)` computes, for each of the last 8 Monday-Sunday weeks (the most recent one Monday-through-today, i.e. possibly partial), what fraction of `emps` had `engine.strikes_in(that week's DayStatus rows, comp_erases) < threshold` — reusing the existing `strike_threshold` Config value rather than inventing a second weekly one. Since that threshold (default 5) was sized for a whole month, a single bad week rarely crosses it on its own, so this trend will often sit near 100% — an accepted consequence of reusing the existing config (confirmed with Ganesh) rather than a bug; documented in the function's own docstring so a future reader doesn't "fix" it into something it was asked not to be. New Config default `compliance_target_pct` (`app/models.py` `CONFIG_DEFAULTS`, `"90"`) backs the dashed "Target N%" line — there was no existing notion of a compliance *rate* target anywhere (`strike_threshold` is a per-person strike COUNT, not an org-wide percentage), so per the hard rule above ("never hardcode thresholds") this is a new Config key, DB-default-only for now and not exposed on `/admin/config`, same precedent as `probation_days_default`/`planned_days_year_*`/`unplanned_hours_year_cap` before it. `emps` is whatever Dashboard's own `admin_department_scope()` already narrowed `all_emps` to — org-wide for a Super Admin, one department for a Team Lead — so "your teams" in the mockup's subtitle is accurate either way without the report function needing its own scoping logic. **Root cause**, computed for real: whoever was compliant last week but isn't this week is the "newly non-compliant" set; grouped by department, if ALL of them are in one department the note names it and the exact count verbatim like the mockup ("N people in {dept} account for the whole drop."); if one department has a clear majority (>50%) but not all, it says so without overclaiming; otherwise it says the drop is spread across N departments rather than pinning it on a department that wouldn't actually explain most of it. No note at all when the most recent week isn't a drop (held or improved) — same "only speak up when something needs attention" instinct as the Needs Attention card next to it. Known, accepted gap (documented in the function's own docstring): an employee with zero DayStatus rows in a given week (before they were hired, or a week `recompute_all()` hasn't reached yet) reads as compliant for that week — the same untouched "vacuous truth" every other `strikes_in()` caller already lives with, not something newly introduced here. `dashboard()` (`app/routes/admin.py`) recomputes its OWN 8-week window (`engine.recompute_all(db, trend_start, today)`) independently of the `first`/`last` recompute already done for whichever `ym` is being browsed — deliberately not tied to `ym`, since this card is always "the real last 8 calendar weeks up to today" regardless of which month's grid is on screen; only computed `if not show_grid`, same landing-view-only gating as Needs Attention/Recent activity. Rendering is a hand-drawn inline SVG line chart in `admin/dashboard.html` — same "no charting library" convention as the Time by Project/Task pie chart — fed by a `chart_data` list (plain `{"label", "pct", "compliant", "total"}` dicts, JSON-safe for `|tojson`) returned alongside the real-`dt.date`-bearing `points` list, the same dual-shape pattern `time_by_project_report()`'s own `projects`/`chart_data` pair already established. Runs immediately on page load, not behind a toggle, since unlike the pie chart's "optional alternate view" role, this card IS the reason to look at the Dashboard. The current week's point is drawn in `--bad` red with its own "N% this week" label (matching the mockup); every other point is `--brand-dark`; axis labels thin out to every other point once there are more than 6 (always still including the last) so they don't crowd a ~320px-wide card. No `app/engine.py`/`app/validation.py` changes (only new calls into the existing `engine.strikes_in`/`recompute_all`, no new business rules) — verified via `py_compile` across every touched `.py` file, a standalone execution of the REAL `compliance_trend_report()` source (extracted from `app/reports.py`, not hand-copied) against 5 worked scenarios with fake Employee/DayStatus stand-ins (a whole-department drop, a majority-but-not-all drop, a spread-across-departments drop, a flat/no-drop week producing no note, and the empty-`emps` early return) — all 5 matched by hand-calculated expected numbers and exact drop-note wording — plus the usual Jinja render+HTML-balance harness for `admin/dashboard.html` in three states (trend populated, no data yet, and `show_grid=True` correctly omitting the whole card) and `node --check` on the extracted inline script.

- **Task Logs' daily summary got an LLM back — Gemini this time — as an opt-in layer over the unchanged rule-based bullets; free-tier data-use risk surfaced and knowingly accepted (Ganesh, 2026-08-31)** — this directly reverses the 2026-08-29 decision two bullets up ("without LLM cant we generate summary" → "Replace LLM with rule-based"), and was flagged as such before writing any code (AskUserQuestion) rather than silently re-adding what was just removed. Spec came from Ganesh pre-written (file list, DaySubmission column, fallback behavior) — implemented largely as given, with review/hardening on the risky parts. **The real issue, checked before building anything**: Google's current [Gemini API Additional Terms of Service](https://ai.google.dev/gemini-api/terms) say a key with no Cloud Billing account runs on the "Unpaid Services" tier, where Google *does* use submitted prompts/responses to improve its products, and the terms themselves say "do not submit sensitive, confidential, or personal information" to that tier — this app's task `details`/Case Type client names are exactly that, for a law firm. The paid tier (any billing account attached, even at $0 spend) excludes training use. Raised via AskUserQuestion with three options (require paid/warn, build as-is and let Ganesh own the risk, or hold off) — Ganesh's actual answer was a fourth: proceed on the free tier now, on purpose, with an existing key, planning to attach billing later. Built exactly that — no tier detection/gating in code (the same API key works unchanged under either tier; billing is a Google Cloud project property, not a key property, so upgrading later is a console change, not a code change) — but the risk and the decision are written down in three places so nobody re-discovers this cold: `app/llm_summary.py`'s own module docstring, `README.md`'s new "AI day summaries" section (with the exact ToS quote and a link), and this bullet. New nullable `DaySubmission.summary_text`/`summary_error`/`summary_generated_at` columns (`app/models.py`) — no backfill needed (unlike `TaskType.category` or other additive columns this project has added), since NULL is a permanently correct answer for any day submitted before this feature existed, not a gap to fill. New `app/llm_summary.py` (`summarize_day(entries) -> (text, error)`, plain `httpx` REST call to `generativelanguage.googleapis.com` — `httpx` was already in `requirements.txt`, no new dependency) never raises: missing key, timeout, network error, non-200, or a malformed response all come back as `(None, "<reason>")`, mirroring the exact resilience contract the old (deleted) `app/llm.py` had for the Anthropic version. Deliberately does NOT echo a non-200 response body into the stored error — only a short `"Gemini returned HTTP {code}"` string — since a 4xx error page can reflect request content back, and this feature is already sensitive about not over-storing what it sends. Model is `gemini-2.5-flash` (checked current as of 2026-08-31, not a guess), overridable via a plain `GEMINI_MODEL` env var (not a `Config` row — this is a deploy-time technical knob a host might need to bump if Google deprecates the model, not a business threshold) so a future deprecation needs an env var change, not a redeploy. `_generate_day_summary()` (`app/routes/employee.py`) is called from `submit_day()` right after that day's `DaySubmission`/`DayStatus` are already committed — synchronous and in-request (this app still has no background job queue), bounded by `llm_summary.py`'s own 10s timeout specifically so a slow Gemini call can't turn into a hung Submit Day click; a failure here only ever means a missing/errored summary on the admin side, never a failed submission, since every guard that can reject a submission has already run by this point. `reports.daily_task_log_report()`'s per-day `summary` dict gained a `"source"` key (`"ai"` when `DaySubmission.summary_text` is set for that date, else `"rule"` with `rule_based_day_summary()`'s output unchanged) — one query for the whole visible date range, not one per day. `admin/reports_tasklogs.html` labels the AI path "Summary (AI)" in prose (`white-space:pre-wrap`) vs. the plain "Summary" bullet list for the rule-based path, so an admin can always tell which one they're looking at. Employee-facing Today/My Month are completely untouched — this is admin-report-only, matching Ganesh's own spec. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` across every touched `.py` file, a real import of `app.llm_summary` (with a minimal fake `httpx` module injected into `sys.modules`, since httpx itself isn't installed in this sandbox — no pip network access) exercising all 10 branches (empty entries, no key, timeout, network error, non-200, malformed shape, empty text, success, prompt-content/key-as-query-param checks, and the `MAX_ENTRIES_IN_PROMPT` truncation cap), and the usual Jinja render+HTML-balance harness for `admin/reports_tasklogs.html` confirming an AI-summary day and a rule-based-fallback day render with visibly distinct labels on the same page, plus summary mode (no employee picked) staying completely unaffected.

- **Bug fix: no way to tell WHY a submitted day still showed the rule-based summary instead of AI (Ganesh, 2026-08-31, same day, live testing screenshot: "i could not find any summary here")** — the day in the screenshot (Alok Gupta, 08/28/2026) was correctly falling back to `rule_based_day_summary()`, but nothing on the page said whether that was because `GEMINI_API_KEY` wasn't actually set yet, that day predated the feature/key, or the Gemini call genuinely failed — three very different fixes, indistinguishable from the UI. `daily_task_log_report()`'s per-day `summary` dict gained an `"error"` key: `sub.summary_error` (already stored by `_generate_day_summary()` for exactly this reason, just never surfaced anywhere) when a real `DaySubmission` row exists but has no `summary_text`, `None` when there's no `DaySubmission` row at all (a day from before this feature existed, or seeded demo data that never went through `submit_day()` — a meaningfully different case from "generation was attempted and failed", so kept as a separate `None` rather than collapsed into the same falsy value). `admin/reports_tasklogs.html` shows "AI summary unavailable: {reason}" under the rule-based bullets whenever `error` is set — e.g. "AI summary unavailable: GEMINI_API_KEY not set" is now the direct, in-app answer to "why don't I see AI summaries yet", no log-digging required. Gated `user.is_super_admin` only, same as the rest of this report's more infrastructural details — a department-scoped Team Lead has no route to act on a missing API key. No `app/engine.py`/`app/validation.py`/`app/routes/employee.py` changes (the error was already being captured, just not read back) — verified via `py_compile` on `app/reports.py` and the same Jinja render+HTML-balance harness, extended with a day carrying a real error (asserts the exact message renders, exactly once, not duplicated onto the day with no error) and a second render as a department-scoped admin confirming the error note is correctly hidden for that tier.

- **Audit Logs moved from Analytics & Reports to Settings & Configuration (Ganesh, 2026-08-31: "audit logs should move to settings & configuration")** — `base.html`'s admin nav only: the `<a href="/admin/audit">Audit Logs</a>` link (already Super-Admin-only, unchanged) moved out of the Analytics & Reports dropdown's `.navdrop-menu` into Settings & Configuration's, dropping the now-redundant inner `{% if user.is_super_admin %}` guard around it (Settings & Configuration's whole dropdown is already wrapped in that same check one level up, same as Holiday Management's link right above it). Analytics & Reports' `<summary>` highlight condition dropped `'audit'` (now just `active_nav == 'reports'`, no longer `in ('reports', 'audit')`); Settings & Configuration's gained it (`in ('config', 'admin_holidays', 'audit')`) — so visiting `/admin/audit` (`active_nav = 'audit'`, set in `admin/audit.html`, unchanged) now correctly highlights Settings & Configuration instead of Analytics & Reports. No route/permission change — `/admin/audit` itself, its `require_super_admin` gating, and every other link in both dropdowns are untouched. No `app/engine.py`/`app/validation.py`/`app/routes/*.py` changes (template-only) — verified via a minimal child-template render of the real `base.html` (three passes: `active_nav` = `reports`/`audit`/`config`) confirming the Audit Logs link is present in Settings & Configuration's menu and absent from Analytics & Reports' in all three, and that the `on` class lands on the correct dropdown's `<summary>` in each case; plus the existing `render_dashboard.py`/`render_tasklogs.py` harnesses (both extend `base.html`) re-run clean to confirm this didn't regress either page's nav rendering or HTML balance.

- **Department-scoped admins gained VIEW-ONLY access to Leave Management and Overtime Management (Ganesh, 2026-08-30: "as of now admins dont have access for leave approvals and overtimes right, and only super admin having all\nso please add leaves and overtime viewable access to admins" — confirmed via AskUserQuestion: "View only (matches \"viewable\")")** — both pages were made fully Super-Admin-only on 2026-08-28 (see that bullet above); this reopens read access only, it does NOT restore the ability to act, which is a deliberate narrower choice than what was there before 2026-08-28. Investigation before writing anything found the department/team scoping logic these two pages had *before* 2026-08-28 was never removed, only gated off — `leave_page()`'s `admin_department_scope(admin)` filter (by department string, since `LeaveRecord` has no department column of its own) and `overtime_page()`'s `led_by(admin, db)` filter (by direct-report `employee_id` set, a Team-Lead-is-per-person axis independent of department — see `led_by()`'s own docstring) both still fully work, unexercised since 2026-08-28 simply because `require_super_admin` blocked every non-super-admin from ever reaching the route body that used them. So the actual change is narrow: `leave_page`/`overtime_page` (`app/routes/admin.py`) switched from `Depends(require_super_admin)` to `Depends(require_admin)` — nothing else in either function changed. Every *mutating* route underneath them stays exactly `require_super_admin`, unchanged: `leave_approve`/`leave_reject`/`leave_add`/`leave_delete`, all four `leave_bulk_upload_*` routes, `overtime_approve`/`overtime_reject`/`overtime_grant`/`overtime_delete`, and `approve_complink`/`reject_complink` (the Overtime ↔ Missed Hours match-request decision routes) — so even if a department-scoped admin crafted one of those POSTs directly, they still get `Forbidden()`/403 exactly as before this change; the template guards below are a UX cleanliness layer on top of that server-side gate, not a replacement for it. `require_super_admin()`'s own docstring (`app/auth.py`) was updated to state this carve-out explicitly rather than leaving the pre-2026-08-28 boundary list looking stale. Template-side, `admin/leave.html` and `admin/overtime.html` both wrap every write-capable form/button in `{% if user.is_super_admin %}` so a department-scoped admin gets a clean read-only view instead of buttons that would 403 on click: the "Record leave" add form, the Pending-requests Approve/Reject forms (replaced with a plain "Awaiting Super Admin" label for them), the Approved-leaves Delete column, and the "Grant Special Paid Time"/"Bulk assign leaves" links in `leave.html`; the "Grant overtime directly" card, the "Link a shortfall to make-up day(s)" create-link form (the read-only `comp_links` table above it stays visible either way — it was already read-only for every tier), the Pending-overtime-requests Approve/Reject forms (same "Awaiting Super Admin" replacement), and the Approved-overtime Delete column in `overtime.html`. The "Overtime ↔ Missed Hours match requests" card (`pending_matches`) needed no template guard at all — it was already computed only `if ... scope is None` at the Python level in `overtime_page()`, i.e. only for a Super Admin, so it's naturally empty and renders nothing for a department-scoped admin; left as-is rather than adding a redundant template check. `base.html`'s "Team Requests" navdrop (Leave Management + Overtime Management links) dropped its `{% if user.is_super_admin %}` wrapper entirely so a department-scoped admin can actually reach these pages — Employees/Settings & Configuration/Support stay exactly as super-admin-only as before, only Team Requests (and the pre-existing Projects & Tasks/Analytics & Reports) are now visible to both tiers. `_admin_nav_badges()` (`app/templating.py`) previously had a dedicated early-return for a department-scoped admin that omitted `pending_leave`/`pending_overtime` entirely, reasoning that "those counts would have nowhere to display and no action they could take" — no longer true now that Team Requests is a real, reachable page for them, so a permanent "0 pending" badge next to it would misinform rather than simply being absent. That branch now also returns a scoped `pending_leave` (a new inline `LeaveRecord` JOIN `Employee` count filtered to the admin's own department, mirroring `leave_page()`'s own filter) and `pending_overtime` (`_pending_overtime_count(led_by(user, db))`, reusing the existing helper with the admin's own direct-report set) — deliberately still WITHOUT `_pending_complink_count()` folded in, since that count is specifically about the super-admin-only match-request decision card and stays folded into the super-admin badge only, unchanged. `led_by` is now imported into `app/templating.py` from `app/auth.py` (confirmed no circular import — `app/auth.py` only imports `app.models`/`app.db`/`app.security`, none of which import `app.templating`). No `app/engine.py`/`app/validation.py`/`legacy/import_legacy.py` changes anywhere in this pass, so the pytest+verify_strikes hard rule doesn't gate it — verified via `py_compile` on `app/routes/admin.py`/`app/auth.py`/`app/templating.py`, and a dedicated Jinja render+HTML-balance harness rendering `admin/leave.html`/`admin/overtime.html`/a minimal `base.html`-extending test template as both a Super Admin and a department-scoped admin: confirms the Super Admin still sees every write form/button (Record leave, Approve/Reject, Delete, Grant overtime, the shortfall-link form) and the department-scoped admin sees none of them (replaced by "Awaiting Super Admin" on the two pending-decision tables) while still seeing the real read-only rows (employee names, approved history, the comp-links table), that Team Requests now renders for the department-scoped admin's nav while Employees/Settings & Configuration stay absent, and that every rendered variant is HTML-balanced with zero unclosed/mismatched tags.

- **Card headers became their own separated box, app-wide (Ganesh, 2026-08-31: "Like this for each header of all pages in both admin and employee dashboards i want seperage box for headers")** — generalizes the 2026-08-29 Today-page-only treatment (`.today-page .card-head`) to every other admin and employee page, confirmed in scope via AskUserQuestion ("Every admin page and every employee page" over "just the two dashboard pages"). New general `.card-head` rule in `app/static/app.css` (right after the base `.card` rule) uses the *default* `.card`'s own 16px/18px padding for its cancel-out negative margin — the more specific `.today-page .card-head`/`.today-page .compact-card .card-head` rules further down the file still win on the Today page itself unchanged, since its own tighter card padding needs different numbers; nothing there had to change. Applied by wrapping each card's real `<h1>`/`<h2>`/`<h3>` title in `<div class="card-head">...</div>` across every template in `app/templates/` and `app/templates/admin/` except the login/signup/error pages (not a "dashboard" in any sense) and Today (already done). Mechanically done via a scripted pass (three successive regex sweeps, widening each time: bare `<div class="card">` immediately followed by a heading; the same but allowing extra attributes like `style="max-width:..."` on the div, which the first pass's exact-string match missed; then hand-fixed the handful of headers sitting inside a flex row alongside a button/badge, like Overtime Management's "Missed Hours and Compensation" `<summary>` line and Time by Project/Task's "By Project" + Pie-chart-toggle row) rather than one giant hand-edit, specifically so a scripted `py_compile`/Jinja-parse/render-balance check could confirm the same transformation was applied identically everywhere instead of trusting ~90 individual edits by eye. Two deliberate exceptions, not oversights: (1) Overtime Management's "Missed Hours and Compensation" card title lives inside a `<details>/<summary>` disclosure toggle (`<summary><h2 style="display:inline">...`) — left unwrapped since forcing the same boxed-background treatment onto a `<summary>` risks fighting the browser's own disclosure-triangle/`list-item` display semantics for a case nobody asked to be fixed; (2) Task Logs' `{{ result.employee.name }}` heading on `admin/reports_tasklogs.html` sits directly in the page body, not inside any `.card` at all, so it was correctly left as a plain page-section heading. Two profile forms (`profile_personal_details.html`'s "Personal information", `profile_employment_details.html`'s "Bank account") had their `<h3>` moved from being the first child *inside* the `<form>` to being a sibling `<div class="card-head">` directly under `.card` (a purely structural move, same visible text, since a heading is not itself a form control) so the same negative-margin-to-card-edge trick applies identically to every other card. `profile.html`'s two whole-card links (`<a class="card" href="/profile/personal-details">...`/`.../employment-details`) also got the treatment — `.card-head`'s CSS is tag-agnostic (targets the class, not `div` specifically) so it works the same way inside an `<a class="card">` as inside a `<div class="card">`. No `app/engine.py`/`app/validation.py`/route changes anywhere in this pass (template + CSS only) — verified via a from-scratch depth-aware Python scanner (not a flat regex — it tracks `<div>`/`<a>` open/close depth to find each card's true inner content, so it can't be fooled by a nested `.card` or by `card-head` itself matching a naive `class="card` prefix check) confirming zero unwrapped card headings remain outside the two documented exceptions, across every one of the ~40 templates in `app/templates/`; `Environment().parse()` (Jinja syntax check, no render context needed) on all 42 templates, 0 errors; a CSS open/close brace-count balance check (343/343); and the full existing render-harness suite (Today, My Month, Leave/Overtime, Lists, Task Logs, admin nav, Dashboard) re-run clean with zero HTML-balance regressions.

- **The standalone "Profile" nav link was removed; the userchip's own name/photo now opens it instead (Ganesh, 2026-08-31: "We have profile button in employee dahsboard right i want to remove that i want that link to redirect when the employees click on thier name then the profile page will open")** — `base.html`'s two employee-facing nav branches (plain employee, and an admin who clicked "My Work") each lost their trailing `<a href="/profile">Profile</a>` link; the pure admin-zone branch never had one to begin with. The userchip block (top-right, name/photo/role/Sign out — rendered once, shared by every tier) now wraps the name+photo+role text in `<a href="/profile" class="userchip-name">` instead of a bare `<span>`, applying to every signed-in tier (not just plain employees) since `/profile` itself was never role-restricted — a Super Admin now also gets a way to reach their own profile from the chip, which the removed nav link never offered them anyway (it only ever appeared in the two employee-facing branches). New `.userchip-name` CSS (`app/static/app.css`, right after `.userchip`) deliberately keeps the link visually identical to the plain text it replaced (`color: inherit`, no underline) so it reads as "the same chip, now clickable" rather than a generic blue hyperlink — an underline on `:hover` is the only new affordance, matching the `.linklike` Sign-out button's own restrained styling one element over. `employee_zone`'s `'profile'` entry and every page's own `active_nav = 'profile'` assignment are untouched (still needed for `show_profile_reminder`'s gating and other unrelated logic) even though there's no nav link left to visually highlight with it — harmless, not dead code to clean up. No Python changes at all (template + CSS only) — verified via a dedicated Jinja render test rendering `base.html` as a plain employee, an admin-as-employee, and a Super Admin: confirms `>Profile<` is absent from all three nav renders and `class="userchip-name"`/`href="/profile"` is present in all three, plus the existing Today/My Month/Leave-Overtime/admin-nav harnesses (all of which extend `base.html`) re-run clean.

- **Admin Dashboard: Compliance Trend swapped for a new "Projects Progression" bar card, Recent activity removed and replaced by an improved Compliance Trend (Ganesh, 2026-08-31, from a pasted "Where my teams' hours went"-style mockup)** — two related asks in one message, both confirmed via AskUserQuestion before writing anything: "remove that compliance trend from admin dashboard page and instead of that please add projects progression... dont mention timmings just mention bar and how much percentage is completed, and remove the recent activity from the dashboard also" plus a follow-up "Instead of recent activity week wise complaince trend but it should be look nice and understandable". Net result, same 3-card `.cols` landing-view row: Needs attention (unchanged) → **Projects Progression** (new, in Compliance Trend's old slot) → **Compliance Trend** (moved into Recent activity's old slot, bug-fixed). "Percentage completed" has no literal meaning for a Project here — they're open-ended client work buckets, not tasks with a defined scope/target (confirmed via AskUserQuestion: "Relative share of hours vs. the busiest project (Recommended)" over inventing a new admin-settable budget-hours field) — so `projects_progression` in `admin.dashboard()` (`app/routes/admin.py`) reuses the exact same normalization the Time by Project/Task report's own "By Project" bars already established (busiest project this month = 100%, everything else scaled against it), just calling `reports.time_by_project_report()` with this page's own already-department-scoped `all_emps` roster and the currently-browsed month's `first`/`last` window (not a new date-range picker — the landing view never lets `ym` change anyway) and rendering percentages only, no `hm`-formatted durations anywhere, per the explicit "dont mention timmings" instruction. Capped to the top 6 projects by minutes with a "+N more projects this month" note for the rest, same top-N-plus-remainder instinct the "By Project" pie chart already uses at a different N. Reuses the exact `.proj-bars`/`.proj-bar-row`/`.proj-bar-label`/`.proj-bar-track`/`.proj-bar-fill`/`.proj-bar-value` CSS classes that report's bars already established — no new CSS needed, `.proj-bar-value` just holds a `%` string instead of an `hm`-formatted duration this time. "Recent activity" (the `recent_audit` query and its table) is gone from `admin.dashboard()`/`admin/dashboard.html` entirely, not just hidden — the full audit trail is still one click away at Admin → Settings & Configuration → Audit Logs for anyone who needs it, so nothing was lost, only a duplicate preview widget. Compliance Trend itself got a real bug fix, not just a change of address: the screenshot Ganesh shared showed its "Target 90%" label and the current week's "100% this week" label overlapping into unreadable garbled text whenever the latest point sat at or above the target line, because both labels used to anchor near the same top-right corner of the chart — the target label now anchors top-**left** instead (`text-anchor="start"`, `x = padL + 4`) so the two can never land in the same corner regardless of where the last point falls, and the "N% this week" label now flips to sit *above* its point instead of below whenever below would run off the bottom of the plot area (a near-0% latest week), so it's never clipped against the day-label axis underneath either. A small text legend line was added under the SVG (a filled dot for "% of your team under the strike threshold, per week" and a dashed-line glyph for "target N%") so the chart doesn't rely on hover tooltips alone to be understood at a glance — the literal ask behind "nice and understandable". No `app/engine.py`/`app/validation.py` changes (only a new call into the existing `reports.time_by_project_report()`, no new business rules) — verified via `py_compile` on `app/routes/admin.py`, a dedicated Jinja render+HTML-balance harness covering three states (fully populated with both cards' data matching a worked example, both cards' empty-data fallback text, and `show_grid=True` correctly omitting the whole landing-view row) that also asserts the card order (Needs attention → Projects Progression → Compliance Trend), confirms every one of the four titled cards in this row carries a `.card-head` wrapper, confirms "Recent activity" is completely absent from the rendered output in every state, and confirms the target-label's new left-anchor fix is present in the extracted inline `<script>` — plus the pre-existing render-harness suite re-run clean to confirm no regression elsewhere on the page.

- **Roster: "Add person" moved above the employee table; the table itself is now grouped by department (Ganesh, 2026-08-31: "this add person section should be top at employees page" / "If we click all its showing all employees list right but it should show like department wise like in last screenshot")** — two related asks on the Roster page. (1) Pure reorder in `admin/roster.html`: the "Add person" `<div class="card">` (form + its help paragraph) moved to sit directly under the Active/Ex-Employees toggle, above the department-pills/search/table card — no field, route, or behavior inside that form changed, only its position on the page. (2) The table itself was flat (one department *column*, no grouping) whenever "All" was selected; `admin.roster()` (`app/routes/admin.py`) now builds `dept_groups` (a `{department_name: [Employee, ...]}` dict, single pass over the already `department, name`-ordered `emps` list — no second query, no re-sort) and the template renders one `tr.depthead` header row per department (reusing the Dashboard compliance grid's own `table.grid tr.depthead td` look, extended with a matching `#roster-table tr.depthead td` rule in `app/static/app.css` rather than inventing a second header style) followed by that department's rows, instead of one flat `{% for e in emps %}` loop. The pasted mockup screenshot showed a narrower column set (Name/Designation/Target per day/This month/Strikes/Leave remaining/View) — deliberately NOT copied: that's a compliance-summary view, while Roster's actual job is employee management, so ID/Country/Reports to/Work days/Start date/Flags/Edit all stay exactly as they were; only the "group by department" idea was carried over from the screenshot, not its specific fields. The department-pill filter's existing JS (`roster.html`'s own `<script>`) already toggled row visibility by `data-department` — extended by three lines to also toggle each group's `tr.depthead` the same way, so picking one department pill still shows just that one group (not every header), and "All" shows every group with its header. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` on `app/routes/admin.py`, a dedicated Jinja render+HTML-balance harness (4 fixture employees across 3 departments, confirming "Add person" renders before the table, exactly 3 `tr.depthead` rows render in the correct department order, and every employee's own `data-department` attribute is still present for the pill-filter JS to key off), and a CSS brace-balance check (343/343).

- **Projects & Tasks tree: row-level "+ Task"/"Edit"/"Deactivate" actions are now always visible, not hover-only (Ganesh, 2026-08-31: "when mouse pointer is on project name then only +task, edit optiosn are apperaing but those should be always appear not like hover, always be appear")** — `.tree-row-acts` (`app/static/app.css`, built 2026-08-29 as a deliberate hover-reveal to keep a collapsed project row visually quiet until the admin's mouse was actually over it) had its `opacity: 0` default and `.tree-proj > summary:hover .tree-row-acts { opacity: 1; }` rule replaced with a flat `opacity: 1` — the controls now render at full visibility from page load, matching `.tree-task-acts`'s own "always visible" precedent one section down (that class never needed hover-hiding since a task row is only ever shown once its parent project is already expanded). Pure CSS, no markup/route change — every button/form these controls wire up to (`/admin/lists/add` quick-add, the inline rename toggle, `/admin/lists/{kind}/{id}/toggle`) is completely unchanged, only their default visibility. Verified via a CSS brace-balance check (343/343, unchanged count since this only edited existing rules) and a grep confirming no other selector still references `.tree-row-acts` at `opacity: 0` or a `:hover` guard.

- **Production incident, 2026-08-31 — bug fix: `ActiveTaskTimer.client`/`.details` being NULL crashed the Today page with a 500 (`AttributeError: 'NoneType' object has no attribute 'strip'`), locking employees out of `/today` entirely** — surfaced live via Cloud Run error logs (`gcloud logging read`) after an employee who'd left Auto time capture running for 2+ days reported the generic "Something went wrong" page on every visit. Root cause: `_log_timer_as_entry()` (`app/routes/employee.py`, the function `_auto_split_timer_if_over_cap()` calls on every `/today` GET via `_day_context()` to close out cap-length chunks of an over-long timer — see the 2026-08-28 "Auto time capture: max-row auto-split" bullet) called `timer.client.strip()` and `timer.details.strip()` unconditionally; `_client_required_error()` (same file) likewise called `client.strip()` unconditionally. Case Type/Client (2026-08-28) added `ActiveTaskTimer.client` as `Text, default=""` — but per this file's own established "SQLite/Postgres ADD COLUMN never backfills" pattern (see `ensure_task_category_backfill`, `ensure_list_status_backfill`, etc.), that ORM-level default only ever applies to a fresh INSERT through SQLAlchemy; a timer row that predates the column, or that otherwise ended up with a raw NULL, keeps NULL forever without an explicit backfill — and because `_auto_split_timer_if_over_cap()` runs on every single page load for as long as that timer stays open, this wasn't a one-time failure, it was a permanent lockout for that employee until fixed. Two-part fix, no `app/engine.py`/`app/validation.py` changes (both files untouched): (1) every unconditional `.strip()` on a timer's `client`/`details` now goes through `(x or "").strip()` — `_client_required_error()`'s truthiness check, and both fields inside `_log_timer_as_entry()`'s `TaskEntry` construction — treating a NULL exactly the same as the empty string it was always supposed to be, with no behavior change for the already-correct non-NULL case; (2) new `ensure_client_text_backfill()` (`app/util.py`, wired into `main.py`'s startup sequence alongside the other `ensure_*` calls) sweeps `ActiveTaskTimer`/`TaskEntry`/`PlannedTask` for any row with `client IS NULL` and sets it to `""`, closing the data-layer gap so no other currently-NULL row is waiting to hit this or a similar unguarded `.strip()` elsewhere — a no-op once every row has a string, safe on every startup, same pattern as every other `ensure_*_backfill`. Verified via `py_compile` on `app/routes/employee.py`/`app/util.py`/`app/main.py`, and a standalone dry-run of the exact fixed lines (extracted verbatim, not hand-copied) against 4 scenarios: the precise crash shape (NULL client, non-case-type project — must produce `""`, not raise), NULL client on a Case-Type project (must still correctly return the "enter the Client" validation message, not crash inside the check itself), NULL details together with NULL client (both guarded independently), and normal already-non-NULL values (byte-for-byte unchanged output, confirming no regression) — all 4 passed. The Today-page render harness (7 scenarios) was re-run clean afterward with no changes needed, since this fix touches request-handling logic the harness's stand-in `Obj` fixtures don't exercise (they always carry real strings), not template structure.

- **Today page: header's "Add Task" quick-jump link removed (Ganesh, 2026-08-31: "why "add tas button is here" please remove")** — this only removes the small `<a href="#addrowcard" class="btn secondary small">Add Task</a>` convenience shortcut added to the header actions row during the 2026-08-29 reskin; the actual Add Task card itself (`id="addrowcard"`, the real form) is untouched and still sits in its usual place further down the left column — employees can still scroll to it manually, they just lost the one-click jump button next to Punch/Break/Submit Day in the header. No Python changes (template-only) — verified via the existing 7-scenario `render_today.py` harness (all still render and stay HTML-balanced) plus a direct check confirming `href="#addrowcard"` is gone from every rendered scenario while `<h2>Add Task</h2>` (the card's own heading, correctly unrelated to the removed link) still renders wherever the card itself is expected to show (i.e. every scenario except the two where the whole Add Task section doesn't render at all — a locked today, and any past day).

- **Plan-ahead: employees can now plan a future day, not just today; PlannedTask gained an optional estimated-time field (Ganesh, 2026-08-31: "the employees should be able to plan their day the previous day itself and add an estimated time for each planned task")** — scoped via two AskUserQuestion rounds before writing anything: (1) "plan the day before" means widening the existing Working-date picker so any future day (not just tomorrow) gets its own full Plan-for-the-day section, not a separate "plan tomorrow" mini-feature; (2) estimated time is a single whole-minutes field (matching the app's own integer-minutes hard rule, no separate hour/minute inputs), shown on both the employee's Today's Plan list and the admin Assign Work / Person Detail "Assigned tasks" views.

  New `PlannedTask.estimated_minutes` (`app/models.py`, nullable `Integer`, additive column — no backfill needed, NULL means "no estimate given," same as every other optional column in this app) is purely informational: nothing in `app/engine.py`/`app/validation.py` reads it, it plays no part in compliance math, strikes, or the 4h/day cap. New module constant `PLAN_AHEAD_DAYS = 30` (`app/routes/employee.py`, plain Python constant, not a `Config` row — this bounds a date-picker dropdown, not a business threshold) extends `_allowed_dates()`'s loop past `today` up to that horizon; the date picker itself needed no other change since `today_page()`'s own `date` query-param parsing was already unbounded (`allowed_dates` only ever fed the dropdown's options, never gated the route).

  `_day_context()`'s plans/past_plans split widened from `date == today` / `else` to `date >= today` (→ `plans`, writable) / `date < today` (→ `past_plans`, read-only, unchanged) — a future day's `PlannedTask` rows now come back through the same `plans` list today's own rows already use, so the moment that date actually becomes today, the exact same query naturally picks them up as ordinary, fully-interactive rows with zero day-rollover bookkeeping needed anywhere.

  `add_plan()` now accepts `date` and `estimated_minutes` form fields: a plan can be added for today or up to `PLAN_AHEAD_DAYS` out, never a past day ("Can't plan a day that's already passed"/"Can't plan more than N days ahead"). The day's-first-plan auto-Punch-In (2026-08-22) and the locked-day check are both now scoped to `plan_date == today` only — punching someone in, or blocking on a lock, for a day that hasn't happened yet makes no sense. `edit_plan()` gained the same `estimated_minutes` field (deliberately allowed to be edited, unlike Project/Task/Client which stay delete-and-re-add-only — a wrong estimate has zero downstream effect on anything, so there's no reason to withhold editing it). `start_plan()` gained a hard guard — `plan.date != today_local()` flashes "Can't start a plan before its day arrives" and redirects back — since the live timer it opens always runs against `now_local()`, which can never be inside a day that hasn't started; today.html already hides the Start/Resume button for a future-day item, this is the server-side backstop matching every other button-guard in this file. `edit_plan()`/`delete_plan()`/`start_plan()` redirects were all changed from a bare `/today` to `/today?date=<plan's own date>` when that date isn't today, so acting on a future day's plan doesn't silently bounce the employee back to today's view.

  `admin_add_plan()`/`admin_edit_plan()` (`app/routes/admin.py`) both gained the same `estimated_minutes` field, reusing the identical `_parse_estimated_minutes()` validator imported from `app/routes/employee.py` (same "one validator, not two that can drift apart" precedent `_client_required_error()` already set for this file) — admin-side date range was already unrestricted before this change (an admin could already assign to any date), so no new window-check was needed there.

  Template changes, all in `today.html`: both the "Plan for the day" add-form guard and the "Today's Plan" checklist guard widened from `day == today` to `day >= today and not day_locked`; each now shows a day-aware heading ("Plan for the day"/"Today's Plan" vs "Plan for {weekday}, {date}"/"{date}'s Plan") and a day-aware help line. The add form gained a hidden `date` field (posts whichever day is currently selected) and an "Est. minutes" number input; each plan item shows a `~H:MM` badge via the existing `hm` filter when `estimated_minutes` is set. Start/Pause/Resume/Stop stayed gated to `day == today` specifically (added as an extra condition on the Start/Resume button only — Pause/Stop are already only ever reachable from a `running`/`paused` status a future day's row can never be in). The `past_plans` block's own guard was corrected from `day != today` to `day < today` — required once `plans` also covers future days, otherwise the two blocks would have overlapped and both fired for a future date. A real bug caught during this build: the page's own inline JS that wires up the Plan-for-the-Day combo picker (`initProjectTaskCombo` for `combo-plan-project`/`combo-plan-task`) still guarded on `{% if day == today %}` — left unchanged, the combo's hidden `project_id`/`task_type_id` inputs would never get wired up on a future day even though the form itself now renders, silently breaking Add on any future date. Fixed by widening that script guard to match the form's own `day >= today and not day_locked` exactly.

  No `app/engine.py`/`app/validation.py` changes anywhere in this feature — verified via `py_compile` across every touched `.py` file, a Jinja `Environment(...).parse()` check on `today.html`/`admin/person.html`/`admin/assignments.html`, a standalone execution of the exact extracted date-window/estimated-minutes validation logic (not hand-copied) against 10 boundary cases (yesterday blocked, today allowed, tomorrow allowed, exactly-`PLAN_AHEAD_DAYS`-out allowed, one day past that blocked, blank/garbage date defaulting to today, `start_plan` blocked on a future date and allowed on today, and the full `_parse_estimated_minutes` range including blank/zero/1440/1441/negative/non-numeric inputs) — all passed — and an extended 8th scenario added to the `render_today.py` harness (a day two days out, two plans — one with a 90-minute estimate, one without — confirming: the heading shows the future date not "Today", the add-form's heading/hidden date field are correct, the `~1:30` estimate badge renders for the estimated plan, no Start button appears for either plan, Edit/Delete forms are still present, no `past_plans` card leaks in, and the page stays HTML-balanced) alongside the pre-existing 7 scenarios re-run clean (today's own plans still show Start/Pause/Stop exactly as before — no regression).

- **Plan-ahead window narrowed from a flat 30-day cap to a week-scoped rule, same day (Ganesh, 2026-08-31: "we made for 30 days but it should be like in current week for suppose today is monday so i can plan my days till friday like that for suppose today is friday then i can only able to add a plan for next monday only for monday, once that monday came then i can plan for that whole week days")** — replaces the flat `PLAN_AHEAD_DAYS = 30` constant (same feature, same day, before it ever shipped to production) with `_plan_ahead_max_date(today, work_days)` in `app/routes/employee.py`: the furthest plannable date is the last configured work day of the CURRENT calendar week (Monday-Sunday) — but the instant `today` itself IS that last work day (or later, e.g. a weekend), the window jumps to just the FIRST configured work day of NEXT week instead of opening the whole week early. This exactly reproduces all three of Ganesh's worked examples: Monday through Thursday all resolve to "through this Friday"; Friday (and, by the same logic, Saturday/Sunday) resolves to "next Monday only"; and the moment that next Monday actually arrives and becomes `today`, the same function is simply called fresh and returns THAT week's own Friday — the "whole week opens up" behavior falls out of the rule automatically, no day-of-week special-casing or rollover bookkeeping needed. Generalizes beyond a hardcoded Mon-Fri assumption by reading `Employee.work_day_set` (falling back to `{0,1,2,3,4}` if empty, same default Roster's own Add-person form uses) — `max(work_days)`/`min(work_days)` give the last/first configured work day regardless of which 5 (or other count of) weekdays an employee actually works, verified against a Tue-Sat schedule as well as the standard Mon-Fri one. Both `_allowed_dates()` (the date-picker dropdown's upper bound) and `add_plan()`'s own server-side check were switched from the flat day-count arithmetic to this function; the flash message on rejection changed from "Can't plan more than N days ahead" to "Can't plan past {date} yet — more days open up once your next work week starts," naming the actual boundary date (via `fmt_date`, per the MM/DD/YYYY hard rule) rather than a day count that no longer means anything constant. `today.html`'s date-picker helper text and the "Plan for the day" card's own doc-comment were updated to reference `plan_ahead_max_date` (a real date passed in context, formatted via `|mdy`) instead of the retired `plan_ahead_days` count. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile`, the `render_today.py` harness re-run clean with its future-day fixture's context var renamed to match, and a standalone execution of the real extracted `_plan_ahead_max_date()` source against 8 worked cases: Monday/Tuesday/Thursday all resolving to that week's Friday, Friday/Saturday/Sunday all resolving to the following Monday, the following Monday itself resolving to ITS OWN week's Friday (confirming the reset), and a non-Mon-Fri (Tue-Sat) schedule resolving correctly off `min`/`max` rather than hardcoded weekday numbers — all 8 matched exactly by hand-calculated expected dates.

- **Plan-ahead, follow-up bug: viewing a future day rendered the fully-interactive Auto time capture (including a currently-running live timer leaked from today), Add Task, and Today Tasks — none of which should exist for a day that hasn't happened yet (Ganesh, 2026-08-31, from a screenshot of `/today?date=` two days out showing an active "Northwind Consulting Inc. · Reply to email — started 1:33 PM" timer and a fully rendered Add Task form: "the auto time should not work for next days , or future days only plan for the day can be accessible")** — root cause: `ActiveTaskTimer` is one row per EMPLOYEE, not per date (see its own model docstring), and nothing in `today.html` ever actually gated Auto time capture / Add Task / the task log table to `day == today` — a stale assumption, not a real guard — so once the Plan-ahead feature above made browsing a future day's `/today?date=` a normal thing to do, whatever timer happened to be running against *today* rendered on every OTHER date's view too, alongside a fully live Add Task form and task log for a day with no actual elapsed time yet. Fixed with one new outer guard in `today.html`: `{% if day <= today %}...{% endif %}` now wraps the entire Auto time capture card, the pre-existing `{% if not day_locked %}` block around it, the Add Task card, and the Today Tasks table+progress-bar card — collectively "the whole day-logging apparatus" — leaving Plan for the Day / Today's Plan (already correctly `day >= today`-gated by the earlier Plan-ahead bullet) and the read-only `past_plans` card as the only things a future day's view ever shows. Two more latent bugs surfaced by adding this guard, both fixed the same pass since the newly-conditional elements they reference would otherwise throw on a future day: the JS `initProjectTaskCombo(...)` wiring for both combo pairs (`combo-project`/`combo-task` on Add Task, `combo-timer-project`/`combo-timer-task` on Auto time capture) was unconditional and now shares the same `{% if day <= today %}` guard; the duration-preview IIFE (`document.getElementById('addrow')` immediately followed by `.querySelector(...)` with no null check) was also completely unconditional and got the identical wrap — both would have thrown `TypeError: Cannot read properties of null` on a future day's page load once `#addrow`/the combo elements stopped existing in that day's DOM. `fillGapRow()` needed no change — it's only ever invoked from a Fill button that itself only renders for `day_unlocked_today` (`day == today` already), so it was never reachable on a future day to begin with. No `app/routes/employee.py`/`app/engine.py`/`app/validation.py` changes at all (template-only) — verified via Jinja `Environment(...).parse()`, extending the `render_today.py` harness's `future_day_plan` scenario with a live `active_timer` object reproducing the exact screenshot bug, and precise substring assertions on the re-rendered output confirming `<h2>Auto time capture</h2>`, `id="autocapture"`, `id="tasktimer"`, `id="addrowcard"`, `<h2>Add Task</h2>`, `Today Tasks`, `id="combo-project"`, and `id="combo-timer-project"` are all absent (output shrank from 20508 to 14242 characters) while the Plan section, its estimate badge, and HTML balance all remained intact — plus the pre-existing 7 same-day/past-day scenarios re-run clean, confirming Auto time capture/Add Task both still render exactly as before for today and any already-submitted day.

- **Multilevel approval for Leave, Overtime, and Overtime ↔ Missed Hours match requests (Ganesh, 2026-09-01: "i want multilevel approval for leave and overtime management the flow is 1.once employee send request then first team lead i mean admin should get request then admin can verify and they can deny or accept with reason then once they denied or accepted with reason then it should go to super admin then super admin can verify it and can approve or reject")** — scoped via two AskUserQuestion rounds before writing anything: which request types get the new stage (all three, all "Recommended" — Leave, Overtime, and the Overtime ↔ Missed Hours match-request flow, since all three already shared the same `LEAVE_REQUESTED`/`LEAVE_APPROVED`/`LEAVE_REJECTED`-style status reuse); who the "Team Lead" reviewer is scoped by ("By department, for both" — the same axis Leave Management already used, and what Overtime Management's own scoping was switched to as a result, see below); what happens to requests already pending when this ships ("Grandfather them" — the old single-step flow, not retroactively routed through a Team Lead they never had); and whether this ships behind a flag ("Yes"). New `MULTILEVEL_APPROVAL_ENABLED` env var (`app/templating.py`, default **on**, same on/off-switch precedent as `LEAVE_MANAGEMENT_V2_ENABLED`/`HOLIDAY_MANAGEMENT_ENABLED`), plus `m.LEAD_ACCEPTED`/`m.LEAD_DENIED`/`m.LEAD_DECISIONS` (`app/models.py`, reusing this app's own "generic status strings, not a new enum per table" convention).

  Architecture: the new stage is five additive columns on each of `LeaveRecord`, `OvertimeApproval`, and `CompensationLink` (`requires_lead_review: bool`, `lead_decision: Optional[str]`, `lead_reason: str`, `lead_reviewed_by: str`, `lead_reviewed_at: Optional[datetime]`) that a Team Lead's decision writes to — and that ONLY those columns write to. The pre-existing `status` field (`LEAVE_REQUESTED`/`LEAVE_APPROVED`/`LEAVE_REJECTED`, reused across all three tables) still only ever flips via the existing Super-Admin-only `leave_approve`/`leave_reject`/`overtime_approve`/`overtime_reject`/`approve_complink`/`reject_complink` routes, completely unchanged. Since `app/engine.py`'s `leave_minutes_on()`/`leave_balance_v2()` and every strike/compliance calculation only ever read `status`, never the new lead-review columns, this feature touches neither `app/engine.py` nor `app/validation.py` — so the pytest+verify_strikes hard rule doesn't gate it (same missing-legacy-bundle/no-pytest gap as every other feature this session — see the sandbox notes). New `ensure_lead_review_backfill()` (`app/util.py`, wired into `main.py`'s startup sequence) is the grandfathering mechanism itself, not just a display-gap backfill like most of this file's other `ensure_*` calls: every pre-existing row (any status) gets `requires_lead_review = False` explicitly, so anything still `requested` when this ships goes straight to a Super Admin exactly as before; only a request submitted AFTER the backfill runs keeps the ORM's own `default=True` and gets routed through a Team Lead first.

  Three new POST routes, one per table, all `require_admin` (so a Super Admin can also record a recommendation, e.g. for a department with no Team Lead assigned) and all requiring both a `decision` (`accepted`/`denied`) and a non-blank `reason` — `POST /admin/leave/{id}/lead-review` (`leave_lead_review`), `POST /admin/overtime/{id}/lead-review` (`overtime_lead_review`, delegating to the existing `_ot_forbidden_unless_scoped()`), and `POST /admin/complink/{id}/lead-review` (`complink_lead_review`, scoped by the REQUESTING employee's department — there's no other "team" concept for a `CompensationLink`). Each 404s outright when `MULTILEVEL_APPROVAL_ENABLED` is off (`complink_lead_review` also requires `LEAVE_MANAGEMENT_V2_ENABLED`, same as the rest of that flow). **Soft gate, not a hard block** (my own interpretation call, stated to Ganesh before implementing): none of the three existing Super-Admin approve/reject routes are blocked from acting even if a Team Lead never reviewed the request — because department-based scoping means a department with no admin assigned would otherwise permanently strand every request from that team; a Super Admin can always act directly, with or without a Team Lead's input, same as before this feature.

  Side effect, explicitly called out to Ganesh before building: Overtime Management's reviewer scoping switched from `led_by()` (the old per-person `reports_to_id` axis) to `admin_department_scope()` (department string match) — the same function Leave Management already used — so both pages now use one consistent "Team Lead" definition instead of two different axes. `overtime_page()`, `_ot_forbidden_unless_scoped()`, and `overtime_grant()` were all switched; `led_by()` itself is untouched in `app/auth.py` in case a future feature wants per-person scoping again, it's simply no longer called from `app/routes/admin.py`/`app/templating.py`. `reports.py`'s `task_log_overtime_report()` docstring (describing how its caller scopes employee_ids) was updated to match, since it previously named `led_by()` by name.

  `leave_page()`/`overtime_page()` each now compute `pending_lead` (still awaiting a Team Lead's accept/deny — `requires_lead_review and lead_decision is None`) and `pending_super` (everything else — already lead-reviewed, or never needed review) from the same `pending` query, and pass both to their templates; with the flag off, `pending_lead` is always empty and `pending_super` is exactly the old single `pending` list, so the UI collapses back to today's single-step behavior byte-for-byte. `admin/leave.html` and `admin/overtime.html` each split their old single "Pending requests"/"Pending overtime requests" card into an "Awaiting Team Lead review" card (Accept/Deny + required-reason form, shown regardless of admin tier) and a "Pending requests" card (the existing Super-Admin-only Approve/Reject form, now also showing the recorded Team Lead decision/reason/reviewer inline when one exists, or a "⚠ No Team Lead review yet" note when it doesn't). `overtime_page()`'s `pending_matches` (the CompensationLink match-request card) took a different shape than the other two — rather than two separate lists, it's ONE list whose meaning depends on the viewer's tier: for a department-scoped admin it's already narrowed to their department AND to rows still awaiting their lead review (already-reviewed ones are a Super-Admin-only concern from here); for a Super Admin it's every still-`requested` row org-wide regardless of lead status, since only they make the final call. `admin/overtime.html`'s single "Overtime ↔ Missed Hours match requests" card branches its heading/action column on `user.is_super_admin` to match this dual meaning, rather than rendering two cards that would double-count the same rows for a Super Admin. The read-only "Missed Hours and Compensation" summary table (visible to every admin tier) also gained a small "(Team Lead accepted/denied)"/"(awaiting Team Lead)" note next to its existing "pending approval" badge.

  Verified via `py_compile` across every touched `.py` file, a Jinja `Environment(...).parse()` check on both `admin/leave.html`/`admin/overtime.html`, and a from-scratch Jinja render harness (fake `Employee`/`LeaveRecord`/`OvertimeApproval`/`CompensationLink` stand-ins, since this sandbox still has neither `fastapi` nor `sqlalchemy` — see the sandbox-no-pytest memory) covering four states: a Super Admin's view (both cards render, the lead's recorded decision/reason surfaces correctly on `pending_super` rows), a department-scoped admin's view (the lead-review Accept/Deny form renders and posts to the right URL, the final-decision card correctly shows "Awaiting Super Admin" with no Approve/Reject buttons), and the flag turned off for both tiers (the "Awaiting Team Lead review" card never renders, `pending_super` correctly holds every pending row, matching the pre-feature single-step table exactly). Confirmed via grep that all three new route paths (`/admin/leave/{id}/lead-review`, `/admin/overtime/{id}/lead-review`, `/admin/complink/{id}/lead-review`) match their template forms exactly, and that no template references a nonexistent context variable. Drive-by bug fix while already touching `approve_complink()`/`reject_complink()`: four stale `RedirectResponse("/admin/leave", ...)` calls (left over from the 2026-08-22 move of this UI to Overtime Management) were corrected to `/admin/overtime`.

- **Multilevel approval got a visual step-progression tracker (Ganesh, 2026-09-02, two follow-ups on the feature above)** — first, a question about why an already-Team-Lead-accepted Leave request still showed the Super Admin's Approve form ("employee submited request, then admin approved then why its showoing target box in super admin") — answered directly, no code change: a Team Lead's Accept/Deny was always a *recommendation*, not a final decision, exactly matching Ganesh's own original spec ("...then it should go to super admin then super admin can verify it and can approve or reject"), so the Approve form (blank = approve as requested, or partially approve with a note — the same Leave V2 form that predates this feature) correctly stays until the Super Admin actually acts. Then, from a pasted mockup of a horizontal step tracker (Raised → Team Lead → Operations Lead → Done): "it shwould show like this progression of leave approval process." Scoped via one AskUserQuestion round (3 questions) before writing anything: placement — "Both employee + admin views"; which request types — "All three" (Leave, Overtime, and Overtime ↔ Missed Hours match requests, since all three already share the identical `status`/`requires_lead_review`/`lead_decision`/`lead_reviewed_by`/`reviewed_by`/`review_note` field set); label style — "Simple version" (this app's own terms, who acted at each completed step, no elapsed-day counter or named reviewer at the still-in-progress step, unlike the mockup's fancier styling).

  New `util.approval_progress_steps(record, multilevel_enabled)` is one pure function, not three — it works generically off those shared field names, since they're deliberately identical across `LeaveRecord`/`OvertimeApproval`/`CompensationLink` (see each model's own multilevel-approval column comment) and every status string (`LEAVE_REQUESTED`/`OT_REQUESTED`/etc.) already resolves to the same literal `"requested"`/`"approved"`/`"rejected"`. Returns a 2-step list (Submitted → Super Admin Review/Approved/Rejected) when the Team Lead stage never applied to this row at all — `multilevel_enabled` is False, or `requires_lead_review` is False (admin-direct entry via `leave_add`/`overtime_grant`/`add_complink`, or a grandfathered pre-feature row) — or a 3-step list with "Team Lead Review" inserted when it did. Each step carries a `tone` (`ok`/`bad`/`accent`/`neutral`) as a separate axis from `state` (`done`/`current`/`skipped`) purely for CSS coloring — a "done" step can mean accepted/approved (green) or denied/rejected (red), and the soft gate this feature deliberately allows (a Super Admin acting before any Team Lead review happened — see `leave_lead_review()`'s own docstring) shows that skipped stage as `state: "skipped", tone: "neutral"` rather than stuck on `"current"` forever. Registered as a plain Jinja global (`templates.env.globals["approval_progress_steps"]`, `app/templating.py`) rather than threaded through every route's own `render()` context — it's a pure function of `(record, MULTILEVEL_APPROVAL_ENABLED)`, and every template that loops over one of these three tables already has both the row and that flag in scope, so no `leave_page()`/`overtime_page()`/`request_leave()`/`request_overtime()` changes were needed at all for this pass. New `app/templates/_macros.html` (this app's first shared-macro file — previously the one existing macro, in `admin/lists.html`, was self-contained and recursive) holds `approval_stepper(record)`, pure markup calling that global and looping the returned steps into `.approval-steps`/`.approval-step.tone-*` divs — one macro, `{% import "_macros.html" as macros %}`ed into all four touched templates, so the exact same look is guaranteed everywhere rather than hand-copied per table.

  Wired into a "Progress" column (replacing the old plain Status badge column) on: `leave.html`'s and `overtime.html`'s (both top-level, employee-facing) own "Your requests" tables, `overtime.html`'s self-service Overtime ↔ Missed Hours match-request table, and — added as a new column rather than a replacement, since these admin tables' existing "Decision" column holds the actionable form — `admin/leave.html`'s "Awaiting Team Lead review" and "Pending requests" cards, `admin/overtime.html`'s equivalent two OvertimeApproval cards, and its dual-meaning "Overtime ↔ Missed Hours match requests" card. Adding the stepper made two inline notes redundant and they were removed: the "Team Lead accepted/denied — reason (name)" / "⚠ No Team Lead review yet" div that used to sit inside `pending_super`'s Decision cell (now conveyed by the stepper's own "Team Lead Review" step), and `leave.html`'s "Admin: {{ review_note }}" line in the Note column (now conveyed by the stepper's "final" step detail). Deliberately NOT added to the already-decided "Approved leaves"/"Approved overtime" tables or the read-only "Missed Hours and Compensation" summary/"Pending leaves" searchable-duplicate tables — those already show a settled outcome via badge/note, and a 2-3-checkmark stepper for a fully-closed row adds less value than for one still in flight; flagged here as a scope line, not an oversight, in case it's wanted later.

  No `app/engine.py`/`app/validation.py` changes — this is a pure display helper over already-computed fields, verified via: `py_compile` across every touched `.py` file; a Jinja `Environment(...).parse()` check on all four templates plus `_macros.html`; the real `approval_progress_steps()` source extracted from `app/util.py` and executed directly (not hand-copied) against 7 worked cases matching Ganesh's own screenshots exactly (freshly requested; Team-Lead-accepted-awaiting-Super-Admin, reproducing the screenshot that prompted the first question; Team-Lead-denied-then-Super-Admin-approved-anyway; fully approved with a partial-hours note; an admin-direct entry with `requires_lead_review=False`; the flag turned off entirely; and the soft-gate case — Super Admin rejecting with no Team Lead review at all, correctly rendering "skipped" not stuck "current") — all 7 matched by hand-calculated expected step lists; and a from-scratch Jinja render pass across `leave.html`/`overtime.html`/`admin/leave.html`/`admin/overtime.html` in both `MULTILEVEL_APPROVAL_ENABLED` states and both empty/populated row states, followed by an HTML div-balance check (Python's `html.parser`, not just eyeballing) on every rendered output — all balanced, zero unclosed/mismatched tags, and the old inline Team-Lead-note strings confirmed absent from the re-rendered `pending_super`/`pending_matches` output.

- **Task Logs' AI day summary backend swapped a second time — Gemini out, a self-hosted Ollama server in (Ganesh, 2026-09-02: "can we use local llm like ollama, omniroute to summarize it, we already have a prompt to ai how to generate 3-5 points from task logs")** — this is the third backend this one feature has had (Anthropic → rule-based-only → Gemini → now Ollama; see the 2026-08-29/08-31 bullets above for the earlier two swaps), and the fourth time overall the underlying `_build_prompt()` text has survived a swap completely unchanged, exactly matching Ganesh's own framing that the *prompt* was never the problem. Two independent reasons drove this swap, both raised and confirmed before writing any code (AskUserQuestion, 3 questions — hosting location, what "omniroute" meant, which model): (1) Gemini's `generateContent` endpoint was hitting a real, currently-open Google-side 404 bug for `gemini-2.5-flash` (confirmed via WebSearch against Google's own developer forum plus Ganesh's own direct `curl` test outside this app returning the identical 404 — never a bug in this app's code, see that day's troubleshooting); (2) even if that resolved, the free "Unpaid Services" tier Ganesh was intentionally running on (a known, accepted risk at the time) still meant Google's own terms allowed using submitted prompts/responses — including this app's client-linked task notes — to improve their products, a real compliance exposure for a law firm that a self-hosted model sidesteps entirely by never sending anything to a third party at all. "Omniroute" turned out not to be a specific tool Ganesh was already running — confirmed via AskUserQuestion, he wants a plain build against Ollama's own API, which is what most local-LLM setups mean by this anyway.

  `app/llm_summary.py` was rewritten in place (same `summarize_day(entries) -> (text, error)` contract, same "never raises" guarantee, same unchanged `_build_prompt()` — only the HTTP call and its surrounding config changed) to call Ollama's OpenAI-compatible `/v1/chat/completions` endpoint instead of Gemini's `generateContent`. New env vars replace the Gemini ones one-for-one: `OLLAMA_BASE_URL` (required, no default — the network address of Ganesh's own Ollama host, e.g. `http://10.0.0.5:11434`; a blank value short-circuits to `(None, "OLLAMA_BASE_URL not set")` before any network call, same "safe to deploy before the host exists" property `GEMINI_API_KEY`'s absence had), `OLLAMA_MODEL` (default `llama3.2:3b`, Ganesh's own pick via AskUserQuestion — small/fast enough for CPU-only hosting on a short summarization task), and two new ones Gemini never needed: `OLLAMA_API_KEY` (optional, only relevant if Ganesh's host sits behind a reverse proxy requiring a bearer token — sent as `Authorization: Bearer <key>` when set, omitted otherwise, since plain Ollama itself has no built-in auth) and `OLLAMA_TIMEOUT_SECONDS` (default 20, up from Gemini's hardcoded 10 — self-hosted CPU inference is reasonably slower than a hosted "flash"-tier API call, and this still sits synchronously inside Submit Day's request/response cycle since this app has no background job queue, so the timeout still has to be generous but not unbounded). Error-string shape is unchanged (`"Ollama returned HTTP {code}"`, `"Ollama request timed out"`, `"Ollama response had an unexpected shape"`, etc.) — never echoes the raw response body, same reasoning as the Gemini version (a 4xx can reflect request content, i.e. the employee's own task details, back in its body). `DaySubmission.summary_text`/`summary_error`/`summary_generated_at` (`app/models.py`) needed no schema change at all — those columns were always backend-agnostic, only their docstring comment was updated to note the swap.

  Every other Gemini-era comment/docstring that named the provider specifically was updated for accuracy rather than left stale (`app/reports.py`'s "why there's no AI summary" comment, `admin/reports_tasklogs.html`'s summary-block comment, `app/routes/employee.py`'s `_generate_day_summary()` docstring) — none of these needed a logic change, `summarize_day()`'s call site and return-value handling are byte-for-byte identical, since the whole point of this module's isolation (no DB/ORM coupling, plain dict in / plain tuple out) was so a future provider swap would be exactly this contained. README.md's "AI day summaries" section was rewritten with the full three-backend history and dropped the now-inapplicable Gemini free-tier ToS risk paragraph, replacing it with a much shorter "you're responsible for securing your own Ollama host" note — the risk category changed from "a vendor's ToS lets them train on this" to "don't expose the host without auth," which is a normal infra concern, not a per-request legal tradeoff to re-confirm on every touch the way the Gemini section demanded.

  No `app/engine.py`/`app/validation.py` changes — this module has zero DB/model coupling by design, so the pytest+verify_strikes hard rule doesn't gate it (same missing-legacy-bundle/no-pytest sandbox gap as every other feature this session). Verified via `py_compile` across every touched `.py` file; a Jinja parse check on the one touched template; and a real import of `app.llm_summary` with a minimal fake `httpx` module injected into `sys.modules` (this sandbox has no `httpx`/network access to pip-install it — same precedent the original Gemini-version verification already established for this exact file), exercising the real `summarize_day()` source directly against 11 cases: `OLLAMA_BASE_URL` unset, empty entries, a clean success (asserting the exact request URL/model/empty-headers shape), a timeout, a network error, HTTP 500, **HTTP 404 specifically** (the exact failure mode Gemini hit in production, confirming it's now just an ordinary handled case rather than a special one), an unexpected response shape, an empty-string response, the `OLLAMA_API_KEY` bearer-header wiring, prompt-content correctness (confirming `_build_prompt()`'s own text is unchanged from the Gemini version), and the `MAX_ENTRIES_IN_PROMPT` truncation cap — all 11 passed.

- **Bug fix, 2026-09-02 — department-scoped Admins were being silently promoted to Super Admin on every app restart (Ganesh: "why super admin tag showing for admins, it should show admin")** — root cause was in `ensure_super_admin_backfill()` (`app/util.py`), the startup step that exists to backfill `Employee.is_super_admin` for rows that predate that column (2026-07-31, see the column's own docstring). Its query filtered on `is_super_admin.isnot(True)`, which in SQL's three-valued logic matches **both** a genuinely un-migrated NULL row (the case this function exists for) **and** a row explicitly set to `False` — which is exactly what every normal department-scoped Admin has, since `Employee.is_super_admin`'s ORM default of `False` is a real `False` on every fresh INSERT, not NULL (only a pre-existing row from before the column existed gets NULL, via SQLite's own bare `ALTER TABLE ADD COLUMN`). Because this function runs on **every** app startup — not once — any department-scoped Admin got silently re-promoted to Super Admin the next time the app restarted (a Cloud Run redeploy, a cold start, anything), every single time, since the day this column shipped. In other words: this "one-time backfill" was never actually a no-op for a real department-scoped Admin, contrary to its own docstring's claim — it just looked like one until the next restart. Fixed by narrowing the filter to `is_super_admin.is_(None)`, which only ever matches the genuine legacy-NULL case; a deliberately-`False` department-scoped Admin now correctly stays `False` across restarts. **This fix does not retroactively correct anyone already wrongly promoted** by the old query before this shipped — check Roster for any Admin who should be department-scoped but currently shows Super Admin, and reset their Role via Roster → Edit → Role. No `app/engine.py`/`app/validation.py` changes, so the pytest+verify_strikes hard rule doesn't gate this — verified via `py_compile`, a standalone simulation of SQL's `IS NOT TRUE` vs `IS NULL` semantics against NULL/False/True confirming the old query wrongly matched the False case and the new one doesn't, and a new `tests/test_util.py` `TestEnsureSuperAdminBackfill` class (4 tests: a NULL legacy row still gets promoted, an explicit-False department-scoped Admin is left alone, a non-admin is never touched regardless of its own is_super_admin value, and calling the function twice in a row is confirmed still a true no-op) — no test existed for this function at all before this fix, which is also why the bug shipped unnoticed for over a month. Still needs a real `pytest` run to confirm, not just `py_compile` (same missing-legacy-bundle/no-pytest sandbox gap as every other change this session).

- **"Add a project" gained a departments dropdown at creation time, instead of always defaulting to every department (Ganesh, 2026-09-02: "as of now if we adding project its directly going to all departments but i need one more field where we can have a dropdown of departments list and we can select the multiple departments as well")** — before this, every new `Project` row started unrestricted (visible to every department, per `ProjectDepartment`'s own "zero links = unrestricted" convention — see the 2026-08-28 "Department-scoped Projects" bullet above) and narrowing it required a second trip to that project's own "Manage departments" panel after creation. `lists_add()` (`app/routes/admin.py`) now also accepts `departments: list = Form([])` and `other_department: str = Form("")` for the `kind == "project"` case only (the `kind == "task"` branch is completely untouched — a task's project-scoping is still only ever set via the separate "Manage projects" panel, never at creation). `db.flush()` was moved to run unconditionally right after `db.add(item)` (previously only flushed inside the `kind == "task"` branch, to get `item.id` for its `ProjectTask` link) so the same freshly-flushed `item.id` is available for creating `ProjectDepartment` rows too. The dedup logic — build a `names` set from the checked `departments` plus a stripped `other_department`, one `ProjectDepartment` row per name, admin-attributed via `created_by=admin.name` — is a direct copy of `lists_project_departments()`'s own replace-all logic, just run once at INSERT time instead of on every UPDATE (there's nothing to delete-and-replace for a project that doesn't exist yet). `audit()`'s payload for `add_project` gained a `department_count` key so the audit trail shows whether a newly-added project started scoped or unrestricted, without having to cross-reference `ProjectDepartment` separately.

  UI reuses the exact `.msdrop` closed-dropdown-with-checklist shared widget (`app/static/msdrop.js`, already loaded globally via `base.html`) in its simple no-search-box form — the same pattern Overtime Management's Surplus day(s) field established 2026-08-30, chosen there specifically because a plain `<select multiple>` stays permanently open and needs a modifier key to multi-select, neither of which matches "a dropdown... we can select multiple departments." The existing project-tree "Manage departments" `.deptpill` checklist (a different, always-visible widget) was left completely untouched — this is a second, independent department-picker on the Add form only, not a replacement. Nothing checked/typed in the new field means exactly what it always has: available to every department, same as before this feature existed. The help paragraph directly below the Add form was reworded from a flat "New projects start available to every department" statement (no longer strictly true) to explain both paths — leave it untouched for unrestricted, or narrow now via the same dropdown — while still pointing at "departments — manage" for changing it later either way.

  No `app/engine.py`/`app/validation.py` changes (only a new `ProjectDepartment` write path, reusing the existing model and the existing enforcement in `validation.project_allowed_for_department()`, which was already unaware of *when* a link was created) — verified via `python3 -m py_compile` on `app/routes/admin.py`, a Jinja `Environment(...).parse()` check on `admin/lists.html`, and a from-scratch Jinja render+assert harness (fake `Project`/`Employee` stand-ins, since this sandbox still has neither `fastapi` nor `sqlalchemy`) confirming the new `.msdrop` field renders with every fixture department as a checkbox plus the free-text fallback, that it's wired with the correct `data-empty-label`/`data-noun-plural` attributes for the shared `msdrop.js` to pick up, and that it coexists cleanly with an existing project's own "Manage departments" panel rendered via `project_node()` elsewhere on the same page (two independent `other_department` inputs, one per form, neither interfering with the other) — plus an HTML div-balance check on the rendered output (balanced).

- **Task Logs' AI day summary backend swapped a fourth time, same day — Ollama out, Groq's hosted API in (Ganesh, 2026-09-02, immediately after the Ollama swap above: "so my requiremtn is just to summarize the task logs i dont to spend any amount on this, i want to it to be free use so please give me best options")** — the Ollama swap earlier the same day genuinely cost nothing for the software itself, but making it *reachable from the deployed Cloud Run app* reliably needs an always-on host somewhere, and that costs real money (confirmed via a WebSearch for current GCP Compute Engine pricing: roughly $25-50/mo for a VM with enough RAM to run even a small model acceptably; GCP's Always-Free e2-micro tier only has ~1GB RAM, too small to run a model well). Running Ollama only on a personal machine avoids that cost but isn't reliable for production (discussed and accepted as a real limitation earlier the same day). Given Ganesh's explicit "I don't want to spend any amount," four options were laid out (AskUserQuestion): drop the LLM entirely and keep only the already-built, already-free `rule_based_day_summary()`; self-host Ollama on GCP's free e2-micro tier with a tiny model (real $0/month, but weak quality/slow on shared 1GB RAM); a free-tier **hosted** API (no VM to run at all); or Ollama on a personal machine used only occasionally. Ganesh picked the hosted-API route.

  Before recommending a specific hosted-API provider, their actual current data-handling policies were checked via WebSearch rather than assumed — the exact kind of "present-day fact that could have changed" this app has already been burned by once with Gemini's ToS. **Groq**: states it does not use customer prompts/responses to train or fine-tune any model, and this policy is account-wide, **not split between free and paid tiers** (unlike Gemini) — default retention is 30 days with a self-serve Zero Data Retention option available. **OpenRouter**: messier by comparison — its free tier is ~25 different models from different upstream providers, and data policy (logging/training) varies PER MODEL depending on which provider actually serves it; opting into OpenRouter's own prompt logging additionally grants OpenRouter an irrevocable commercial-use right over that data. Groq was recommended and confirmed via a second AskUserQuestion (model choice: `llama-3.1-8b-instant`, Ganesh's pick, over the larger/slower `llama-3.3-70b-versatile`) specifically because it matches or exceeds the assurance Gemini's *paid* tier gave, for free, with no VM to run.

  `app/llm_summary.py` was rewritten again (same `summarize_day(entries) -> (text, error)` contract, same never-raises guarantee, same unchanged `_build_prompt()` — the fourth time this exact prompt text has survived a backend swap) to call Groq's OpenAI-compatible `https://api.groq.com/openai/v1/chat/completions` endpoint. Env vars: `GROQ_API_KEY` (required — a blank value short-circuits to `(None, "GROQ_API_KEY not set")` before any network call, same "safe to deploy before the key exists" property every prior backend's missing-credential case had), `GROQ_MODEL` (default `llama-3.1-8b-instant`), `GROQ_TIMEOUT_SECONDS` (default 10 — back down from Ollama's 20, since Groq's hardware is fast like Gemini's hosted "flash" tier was, unlike self-hosted CPU inference). The `OLLAMA_*` env vars are gone; every place that named Ollama specifically in a comment (`app/models.py`, `app/reports.py`, `admin/reports_tasklogs.html`) was updated to reference the full Anthropic → rule-based → Gemini → Ollama → Groq chain rather than being left pointing at a now-retired backend. This module's own docstring is explicit that Groq, unlike the Ollama version it replaces, IS still a third party — task details and any client names in them do leave this app's infrastructure under Groq's no-training guarantee, not under a "nothing ever leaves" guarantee — and names self-hosted Ollama as the fallback path if that distinction ever stops being acceptable. README.md's "AI day summaries" section was rewritten with the fifth backend-history entry and Groq's specific data-policy findings (with the same rigor the original Gemini ToS writeup had, not a shorter version just because this is the "good" option).

  No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` across every touched `.py` file, and the same fake-`httpx`-injection technique used for every prior backend's verification (this sandbox still has no `httpx`/network access), exercising the real `summarize_day()` source directly against 11 cases: `GROQ_API_KEY` unset, empty entries, a clean success (asserting the exact request URL/model/`Authorization: Bearer` header), a timeout, a network error, HTTP 500, HTTP 401 (a realistic bad-key failure mode specific to a hosted API, which self-hosted Ollama never had to handle), an unexpected response shape, an empty-string response, prompt-content correctness (confirming `_build_prompt()` is still byte-for-byte unchanged), the `MAX_ENTRIES_IN_PROMPT` truncation cap, and a check that the stored error message never echoes the raw response body (same "don't re-leak the employee's own task details via an error string" reasoning every prior backend's error handling already had) — all 11 passed.

- **Bug fix, same day — Groq default model (`llama-3.1-8b-instant`) was already dead by the time this got tested end-to-end (Ganesh, 2026-09-02, caught live on a Task Logs report screenshot: "AI summary unavailable: Groq returned HTTP 404")** — the Groq swap above shipped with `llama-3.1-8b-instant` as the default `GROQ_MODEL`, which was Ganesh's own pick and a real, working model ID at the time the code was written earlier the same day. But Groq had already announced its deprecation on 06/17/26 with a shutdown date of 08/16/26 (confirmed via `console.groq.com/docs/deprecations`, checked 2026-09-02) — 17 days before this feature was actually run against the live API for the first time, so every request to it returned HTTP 404, not a code bug. A 404 from Groq's `chat/completions` endpoint specifically means the model ID doesn't exist/is retired — a bad API key returns 401, not 404 — which is what pointed straight at the deprecations page rather than the request/auth code. Fixed by changing `GROQ_MODEL`'s default (`app/llm_summary.py`) to `openai/gpt-oss-20b`, Groq's own documented replacement for `llama-3.1-8b-instant`. No other code changed — `summarize_day()`'s request/response handling, `_build_prompt()`, and every env var name are exactly as shipped in the Groq-swap bullet above; only the one default string changed, still overridable via `GROQ_MODEL` same as before. Caught via the exact mechanism this feature's own error-surfacing bugfix (2026-08-31, "why don't I see AI summaries yet") was built for — the Task Logs report showed the real reason ("Groq returned HTTP 404") instead of a silent fallback to the rule-based bullets, which is what made this fast to diagnose rather than looking like the feature just wasn't working. Flagged for the future: since Groq deprecates specific model IDs on a rolling basis (see their own changelog — this is at least the 15th model they've retired since late 2024), whichever model this app points `GROQ_MODEL` at will eventually need bumping again; the deprecations page is the first thing to check if "Groq returned HTTP 404" reappears later. README's "AI day summaries (Groq)" section got a matching bug-fix note.

- **Bug fix, same day, second one — empty Groq responses ("Groq returned an empty summary") once the 404 above was fixed (Ganesh, 2026-09-02, same Task Logs screenshot session)** — `openai/gpt-oss-20b` (the replacement model from the bugfix above) is a reasoning model, unlike `llama-3.1-8b-instant` before it: it spends part of its token budget on a hidden chain-of-thought, returned separately in Groq's `message.reasoning` field, before writing the actual answer into `message.content` (confirmed against `console.groq.com/docs/reasoning`, checked 2026-09-02). The original request payload's `max_tokens: 220` — a fine budget for a plain non-reasoning chat model — let the model spend its entire allowance on that hidden reasoning step and never reach a final answer, so `content` came back `""`, which `summarize_day()` correctly (if unhelpfully, without this context) reported as "Groq returned an empty summary" rather than crashing. Fixed in `app/llm_summary.py`'s request payload: renamed `max_tokens` to `max_completion_tokens` (the parameter name Groq's own docs use for this family of models) and raised it 220 → 500, and added `"reasoning_effort": "low"` — Groq's own documented knob (only supported by GPT-OSS/Qwen 3.8 reasoning models, harmless to send otherwise) to keep the hidden thinking step short for a low-stakes summarization task, rather than trying to out-budget an unbounded reasoning step. No change to `_build_prompt()`, error handling, or anything else in the module — same "narrow, targeted fix" pattern as the model-ID bugfix immediately above it. Verified via the same fake-`httpx`-injection technique as every other verification of this file (this sandbox still has no real `httpx`) — confirmed the new payload keys/values are sent exactly as intended and that the existing empty-string-response handling path (`if not text: return None, "Groq returned an empty summary"`) is unchanged and still correctly reachable if a response is ever legitimately empty for some other reason.

- **AI summary switched from prose to a bullet list, same day (Ganesh, 2026-09-02, live testing feedback after the two Groq bugfixes above finally produced real output: "But i need it as a point wise just 3-5 points")** — the original prompt (unchanged since the very first Anthropic-backed version of this feature) asked for "3-4 short sentences... plain prose only," rendered as one `<p style="white-space:pre-wrap">` block. `_build_prompt()` (`app/llm_summary.py`) now asks for "3 to 5 short bullet points... each bullet must start with a hyphen (-) on its own line" instead — the one line of prompt text that had survived all 4 prior backend swaps unchanged finally changed, not because of a backend swap this time but because the actual requested output shape changed. New `reports._parse_ai_bullets(text)` (`app/reports.py`) parses the raw stored `DaySubmission.summary_text` into a list at *read* time — split on newlines, strip a leading `-`/`*`/`•` or `1.`/`1)` marker from each line, drop blanks — rather than trusting the model followed the hyphen instruction exactly (a model can still wrap a line or use a different marker); if the model ignores the format entirely and returns one unbroken paragraph, that whole paragraph comes back as a single "bullet" rather than showing nothing. Deliberately parses from the stored raw text on every read rather than re-generating or storing pre-split bullets — a future improvement to `_parse_ai_bullets()` applies retroactively to every already-generated summary for free, no backfill needed. `admin/reports_tasklogs.html`'s two previously-separate rendering branches (`{% if source == 'ai' %}` → one `<p>` block; `{% elif bullets %}` → one `<ul>`) collapsed into one `{% if day.summary.bullets %}` → `<ul>` branch that both the AI and rule-based paths now share — only the small "(AI)" suffix on the "Summary" label still distinguishes them, so an AI summary and a rule-based fallback look identical in structure, matching what Ganesh's own screenshots showed him expecting. No `app/engine.py`/`app/validation.py`/`app/routes/employee.py` changes — `submit_day()`'s call into `summarize_day()` and what gets stored on `DaySubmission.summary_text` are both unchanged, this is purely a prompt-text change plus a read-time parsing/rendering change. Verified via `python3 -m py_compile` on both touched `.py` files, a Jinja `Environment(...).parse()` check on the touched template, and a standalone run of the real extracted `_parse_ai_bullets()` source against 4 cases: mixed markers in one blob (`1.`, `-`, `*`, and a bare unmarked line all in the same text — every marker stripped correctly, unmarked line passed through as-is), a clean hyphen-bulleted response (the intended, prompt-compliant shape), an empty string (returns `[]`, matching `rule_based_day_summary()`'s own "no entries → `[]`" convention so the template's shared `{% if day.summary.bullets %}` guard behaves identically for both sources), and a single-unbroken-paragraph response with no newlines at all (returns one bullet, not nothing).

- **Task Logs AI/rule-based summary now quantifies "where did most of the time go" (Ganesh, 2026-09-02, relaying his manager's question about the Task Logs AI summary: "can we quantify it... like most of the time was spent doing this")** — both summary paths previously listed projects in whatever order they happened to appear (chronological, for the rule-based path) with no signal about which one actually dominated the day. Two matching changes, same day: (1) `reports.rule_based_day_summary()` now sorts its per-project bullets by total minutes descending instead of first-logged order — the first bullet is always the project that took the most time that day, computed the same deterministic way it always was, no LLM involved. (2) `llm_summary._build_prompt()` was rewritten to pre-aggregate entries by project IN PYTHON (sum minutes, collect distinct tasks/details, sort descending) before ever building the prompt text — the model is handed already-correct, already-sorted totals and instructed not to recalculate or reorder them, only to phrase each pre-computed line as a bullet and make the first one clearly read as "most of today went here." This is deliberate: LLMs are unreliable at summing several line items correctly — the arithmetic and ranking are Python's job, the model's only job is wording. Both paths now agree on which project leads, whether or not Groq is configured, so an admin gets the same "most time went to X" answer either way. No `app/engine.py`/`app/validation.py`/`app/routes/employee.py` changes — pure prompt-construction and sort-order changes over data that was already being computed. Verified via: `python3 -m py_compile` on both touched files; a standalone execution of the real extracted `_build_prompt()` against a 3-entry case (Northwind logged first at 60 min, Cedarline logged later across two entries totaling 242 min) confirming Cedarline's aggregated 4:02 total ranks first in the prompt text and is correctly formatted as `H:MM`, not raw minutes; and a standalone execution of the real extracted `rule_based_day_summary()` against the identical scenario (using fake ORM-row stand-ins, same technique as every other reports.py verification this session) confirming its own bullet list leads with `"Cedarline Consulting Inc. (4:02): ..."` despite Northwind being the chronologically-first entry.


- **Departments became a real managed list, not free text on Employee — Bulk upload gained a "Departments" section (Ganesh, 2026-09-02: "under bulk upload i need Departments option where we can add departments then while adding employees it will show the existed departments dropdown, if we want to add new department then we have to click on departs then it will show existed departments and add option will be there to add new departments")** — before this, "department" was never its own thing anywhere in this app, just a free-text string on `Employee.department`; every dropdown that looked like a department picker (Projects & Tasks' "Manage departments" panel, every Reports page's Department filter, Assign Work's department tree, Roster's own department-pill filter) actually just showed whatever distinct strings happened to already exist on employees (`reports.departments_list()`, pre-2026-09-02: `sorted({e.department for e in employees})`). That meant there was no way to add a department before someone was assigned to it, no way to rename one everywhere at once without hand-editing every employee row, and Roster's "Add person"/Edit forms let anyone free-type a near-duplicate ("Ops" vs "Operations"). Scoped via three AskUserQuestion rounds before writing anything, since each answer changes the shape of the feature: (1) where the management UI lives — **"Section on the Bulk Upload page"**, not a new dedicated page like Projects & Tasks and not just an inline "+Add new" on the Add-person form itself; (2) whether Department is a full managed list or a simple add-only list — **"Full managed list (Recommended)"**, mirroring `Project`/`TaskType`'s existing Add/Rename/Deactivate shape rather than inventing a third pattern; (3) what happens to every employee's current free-text department value — **"Auto-import as the starting list (Recommended)"**, so the list starts populated with whatever's already in use rather than empty.

  New `Department` model (`app/models.py`, same simple shape as `Project`/`TaskType`: `id`/`name` unique/`active`/`created_by`/`created_at`) is the new single source of truth. `reports.departments_list(db)` was rewritten to read active `Department` rows instead of scanning `Employee` data — this is what makes the "full managed list" choice actually pay off: every existing caller of that one function (Projects & Tasks' Add-a-project/Manage-departments pickers, every Reports page's Department filter, Assign Work's department tree) picks up Add/Rename/Deactivate for free, with zero template changes needed anywhere else. New `ensure_departments_backfill()` (`app/util.py`, wired into `main.py`'s startup sequence like every other one-time additive backfill in this app) is the auto-import step — on first startup it turns every distinct non-blank, non-"—" `Employee.department` string already in use into a `Department` row (`created_by="system"`), then never touches a `Department` row again on any later startup (an admin's own rename/deactivate, or a name it already imported, is left alone — this only ever adds, never edits, same as it did for its own first run). Three new Super-Admin-gated routes in `app/routes/admin.py` — `POST /admin/departments/add`, `/{id}/rename`, `/{id}/toggle` — mirror `lists_add`/`lists_rename`/`lists_toggle` byte-for-byte in structure (case-insensitive duplicate check via `func.lower`, `audit()` call, flash + redirect back to `/admin/roster/bulk-upload`). New `_all_departments(db)` helper feeds a new "Departments" card on `admin/roster_bulk_upload.html` (add form + a name/inactive-badge/Edit/Deactivate table, same inline-rename-toggle JS pattern the Projects & Tasks tree's task rows already use) — placed on Bulk Upload per Ganesh's own explicit choice, not a new page.

  Roster's "Add person" form and the "Edit" page (`admin/roster.html`/`admin/roster_edit.html`) both had their Department field switched from a plain `<input>` to a `<select>` populated from `reports.departments_list(db)` (`roster()`/`roster_edit_page()` in `app/routes/admin.py`) — a genuinely new department can only enter the system via the Bulk Upload page's own Add form first, never by free-typing it into Add-person again. `roster_edit_page()` deliberately unions in the employee's own current department string if it's since been deactivated or somehow isn't in the active list, so editing someone for an unrelated reason never silently blanks or resets their department — same "don't let an unrelated edit destroy an existing value" instinct the Holiday-Management-off `_emp_from_form` bullet above already established for a similar case. Roster's own department-pill *filter* (`all_depts`, derived from currently-listed employees, used only to build the pill row above the table) is a separate, pre-existing, and deliberately untouched variable — it still reflects who's actually assigned to what, not the managed list, so a `Department` row with nobody in it yet correctly shows no pill.

  **Deliberately NOT touched, a stated scope boundary** — the Roster bulk-upload Excel sheet's own Department column stays free text, exactly as before: it is not validated against the new `Department` table and does not auto-create rows from it. Auto-creating departments from a bulk file risked silently multiplying near-duplicate names from typos with zero admin review; an admin who wants a bulk-uploaded department to actually show up in the dropdown still adds it once via the new Bulk Upload card first, same as any other new department. No `app/engine.py`/`app/validation.py`/`legacy/import_legacy.py` changes anywhere in this feature, so the pytest+verify_strikes hard rule doesn't gate it (same missing-legacy-bundle/no-pytest sandbox gap as every other feature this session) — verified via `py_compile` across every touched `.py` file, a Jinja `Environment(...).parse()` check across `admin/roster.html`/`admin/roster_edit.html`/`admin/roster_bulk_upload.html`/`admin/lists.html` (catching and fixing one real bug along the way: an HTML `<!-- -->` comment above the new Departments card contained the literal prose `{% if user.is_super_admin %}`, which Jinja parsed as a real unmatched tag despite being inside an HTML comment — HTML comments don't shield Jinja delimiters — breaking the whole template's `{% endblock %}` nesting until the comment was reworded to avoid literal Jinja syntax), a from-scratch Jinja render+assert harness covering the Departments card in both populated and empty states and both Roster forms' dropdown (including the stale/deactivated-department-still-selected case), an HTML div-balance check on every rendered output (all balanced), and a new `TestEnsureDepartmentsBackfill` pytest class in `tests/test_util.py` (5 tests: distinct departments import correctly, blank/None/"—" values never become rows, an admin's already-existing Department row is never duplicated or silently reactivated, a no-employees no-op, and idempotency across two calls) — still needs a real `pytest` run to confirm, not just `py_compile`, before calling this fully done.

- **Bulk upload page layout fix, same day (Ganesh, 2026-09-03: "the bulk upload section should be left and departments sections should be right")** — the new Departments card (see the bullet above) had been placed as its own standalone full-width block below the existing Onboard/Update `.cols` row, which left it stacked under "Onboard new employees" with a large empty gap to its right where nothing else sat at that height. Fixed by moving "Upload your sheet" and "Departments" into a second `.cols` row (the same flex two-up row `Onboard new employees`/`Update existing employees` already use above it) — Upload on the left, Departments on the right, in that DOM order. Dropped both cards' one-off inline `max-width` styles (480px/640px) so they size the same flexible way every other `.cols` card on this page already does, rather than fighting the row's own flex-basis. Pure template reorder, no Python/route/model change — verified via the same `render_departments.py` harness (extended with a DOM-order assertion confirming both cards sit inside one `.cols` div, Upload before Departments) and an HTML balance check, both passing. **Follow-up, same day (Ganesh: "update existing section also should be left side")** — the two-row layout above still left "Update existing employees" on the right of row 1, disconnected from "Onboard"/"Upload" which read as one logical left-side flow. Collapsed to a single `.cols` row: a plain (non-`.card`) wrapper div holds Onboard → Update → Upload stacked as three ordinary `.card`s on the left (each keeps its own existing `margin-bottom`, so they space themselves the same as before), and Departments alone occupies the right column at `flex: 1 1 340px`, matching `.cols`' own convention rather than a special case. Re-verified the same way (DOM-order assertion updated to check Onboard→Update→Upload→Departments all inside the one `.cols` div; HTML balance re-confirmed for both populated and empty-departments states).

- **My Hours gained a "Where your hours went this month" bar chart (Ganesh, 2026-09-03: "i want to add bar graph or any chart in employee dashboard in My Hours page which can represents employees their projects working and how much they are spending on each project on each day it should be simple and employees can able to understand")** — scoped via two AskUserQuestion rounds before writing anything, since the literal ask ("each project on each day") could have meant a full day-by-day stacked chart: (1) chart type — **"Simple bar chart: total per project (Recommended)"**, not a stacked-bar-per-day view or a click-to-expand hybrid, specifically because a day-by-day split gets visually busy once someone works several projects in a month and the ask was explicitly "simple ... employees can able to understand"; (2) placement — **"New card between the KPI tiles and the Hours ledger (Recommended)"**, not below the ledger or folded into Today's own mini "My Month" card.

  New `reports.my_month_project_totals(db, employee_id, start, end)` is the single-employee sibling of the existing admin-side `time_by_project_report()` a few functions above it — same "aggregate TaskEntry by project_id, then resolve Project names in one follow-up query" technique, just scoped to exactly one `employee_id` over a plain date range with none of the department/project/task filter knobs the admin report needs (an employee's own page has nothing to filter by). Returns `{"projects": [{"name": str, "minutes": int}, ...]` sorted busiest-first, capped to the top 8 with a rolled-up `"Other"` bucket for the rest — same top-N-plus-remainder convention the admin "By Project" pie chart already established — `"grand_total": minutes}`, or an empty `projects` list when nothing's been logged yet. `my_month()` (`app/routes/employee.py`) calls it once per page load (`first`/`last` — the same month range everything else on the page already uses) and passes it through as `project_totals`; `reports` had to be added to that file's top-level import line since nothing there previously imported the reports module.

  Rendering deliberately needed **zero new CSS** — `my_month.html`'s new card reuses the exact `.proj-bars`/`.proj-bar-row`/`.proj-bar-label`/`.proj-bar-track`/`.proj-bar-fill`/`.proj-bar-value` classes the admin's Time by Project/Task "By Project" bars (2026-08-30) and Dashboard's Projects Progression (2026-08-31) already use — one bar per project, width scaled against the busiest project (`projects[0].minutes`) so any two bars stay visually comparable, value shown via the existing `hm` filter. An empty-state message ("Nothing logged yet this month...") replaces the chart entirely when `project_totals.projects` is empty, rather than showing a blank chart.

  No `app/engine.py`/`app/validation.py` changes (a pure read over already-existing `TaskEntry` rows, no new business rule) — verified via `py_compile` on both touched `.py` files, a Jinja `Environment(...).parse()` check on `my_month.html`, a from-scratch render+assert harness (populated/empty/more-than-8-projects states, confirming bar widths compute correctly and the empty state's message shows instead of a chart) plus an HTML div-balance check on all three renders, a standalone execution of the real extracted `my_month_project_totals()` source (not hand-copied, using fake DB/select/model stand-ins since this sandbox still has no sqlalchemy — same technique as every other reports.py verification this session) against 5 worked cases (busiest-first sort, exactly-8-projects with no "Other" bucket, 10-projects capped to top-8-plus-"Other" with the correct rolled-up total, an empty range, and a deleted/missing Project id falling back to a "—" placeholder name instead of crashing), and a new `TestMyMonthProjectTotals` pytest class in `tests/test_reports.py` (5 tests, mirroring `TestTimeByActivityReport`'s own fixtures/helpers) — still needs a real `pytest` run to confirm, not just `py_compile`, before calling this fully done.

- **Bug fix: an already-running Auto time capture timer became permanently stuck if its Project got restricted out from her department mid-day (Ganesh, 2026-09-03, from a live screenshot: "That Project/Employer isn't available to your department — ask an admin to add it under Lists" — employee could neither Stop/Pause the running timer nor start anything new)** — root cause: `validate_entry()`'s department-scoped-project check (`project_allowed_for_department()`, 2026-08-28) and its project-scoped-task check (`task_allowed_for_project()`, 2026-08-27) both apply on EVERY call, with no distinction between "picking this project/task fresh right now" and "closing out a timer that's already been running against it since before the rule changed." `_finish_task_timer()`/`_log_timer_as_entry()` (the one shared function behind Auto time capture's Stop, every Plan Pause/Stop segment, AND `_stop_current_timer_if_any()` — the auto-close that runs before starting a new timer or a new plan) all route through this same `validate_entry()` call, so once an admin narrowed a project's departments (or unlinked a task from a project) while an employee had a timer open against it, EVERY way of closing that timer failed the same check — including the auto-close that has to succeed before she could start anything else. She was completely stuck: nothing could close the old timer, and nothing else could start until it closed.

  New `closing_existing: bool = False` parameter on `validate_entry()` (`app/validation.py`) — when `True`, skips `project_allowed_for_department()`/`task_allowed_for_project()` for that one call only; every other rule (locked day, overlap, 4h cap, backdate window, details length) still fully applies, since none of those are about "is this a fresh pick," only those two scoping checks are. Deliberately a distinct flag from the existing `acting_admin` bypass (which also skips the backdate window and is meant for a different actor — an admin working on someone else's day — not for an employee closing their own already-started work). `_log_timer_as_entry()` is the ONLY call site that passes `closing_existing=True` — `add_entry()` (a fresh, hand-typed row) correctly keeps the default `False`, since typing in a brand-new row IS picking a project right now and should still be held to whatever scoping is currently in effect. This also means `_auto_split_timer_if_over_cap()`'s earlier full-cap chunks (which call `_log_timer_as_entry()` in a loop) get the same bypass, correctly, since those are closing out segments of the same already-running timer.

  Immediate unblock for the specific employee affected: re-add her department to the project's "Manage departments" panel on the Lists page (or unrestrict it entirely), have her Stop/Pause once to clear the stuck timer, then re-narrow the project's departments again afterward if that was still the intent — narrowing a project's scope while someone has it actively open is now safe going forward, but wasn't at the moment this was hit.

  No `app/engine.py`/`legacy/import_legacy.py` changes (this does touch `app/validation.py`, so the pytest+verify_strikes hard rule applies — same missing-legacy-bundle/no-pytest sandbox gap as every other `validation.py` change this session) — verified via `py_compile` on both touched files, a regex-confirmed extraction of the exact two modified condition lines from the real source (not hand-retyped) exercised against the full truth table of `(acting_admin, closing_existing, allowed)` combinations for the department check and `(project_is_not_none, closing_existing, allowed)` for the task check — confirming both checks are fully suppressed whenever `closing_existing=True` and completely unchanged (byte-for-byte prior behavior) whenever it's `False` — and two new pytest classes in `tests/test_validation.py`: `TestDepartmentScopedProjects` (3 tests — closes a pre-existing gap, since `project_allowed_for_department()` had shipped 2026-08-28 with zero pytest coverage, verified only by hand-trace per that day's own CLAUDE.md bullet) and `TestClosingExistingBypass` (4 tests — a department restriction added after the fact still blocks a fresh pick, `closing_existing=True` bypasses both the department and the task-project restriction, and confirms `closing_existing` does NOT soften an unrelated rule like end-before-start) — still needs a real `pytest` run to confirm, not just `py_compile`, before calling this fully done.

- **Projects & Tasks tree: unapproved suggestions now show a "pending approval" badge (Ganesh, 2026-09-03, from a real incident)** — a department admin ("Norine") reported finding projects/tasks under Front Desk she never created (a "Missed Call Tracker" project, "ParaLeagle case creation"/"Outbound Vitel Calls" tasks); she checked the Audit Log first, was told to check Suggestions instead, and found they were simply employee suggestions nobody had approved or rejected yet. Investigated why this was confusing rather than obvious: `lists_page()` (`app/routes/admin.py`) queries every `Project`/`TaskType` row with **no status filter at all** (`select(m.Project)`/`select(m.TaskType)`, no `.where(status == ...)`), and `admin/lists.html` had **zero visual indicator** distinguishing a still-`pending` suggestion from an already-`approved` item anywhere in the Department → Project → Task tree — confirmed this was the only gap, not a security/data-integrity issue, since `_visible_projects_and_tasks()` (`app/routes/employee.py`) and `validate_entry()`'s own `LIST_PENDING` check both already fully block a pending item from being logged against by anyone, submitter included (unchanged, already covered by `TestSuggestionStatus` in `tests/test_validation.py`) — an admin just had no way to tell *why* something showed up without separately opening Suggestions. Confirmed via AskUserQuestion ("Yes, add the badge (Recommended)") rather than building a second page or a new filter toggle: "A visible badge... on any tree node whose status is still pending — same page, no new page needed." Reuses the exact existing convention `admin/overtime.html` already established for this situation (`<span class="badge b-pending">pending approval</span>` — the muted-gray `.b-pending` class, `app/static/app.css`, already means "awaiting a decision" everywhere else it's used) rather than inventing a new badge style. Added in the one shared `project_node(p)` macro (covers both the `shared_projects` list and every `dept_groups` entry's projects, since both loops call the same macro — one `{% if p.status == 'pending' %}` right after the project name), its nested per-project task-row block (`{% if t.status == 'pending' %}` next to each linked task's name), and the "All departments" Category → Task grouping's own task-row block (same check, `t.status == 'pending'`) — the three places a Project or TaskType name can render anywhere on this page, confirmed via a full read of the template. Matches this codebase's own established convention of comparing `.status` against a literal string (`'pending'`, matching `m.LIST_PENDING`'s value) directly in templates rather than importing the constant, same as `today.html`'s plan-status checks and `admin/overtime.html`'s own link-status checks. No Python/route changes needed at all — `projects`/`tasks`/`tasks_by_id`/`cat.tasks` were already real ORM objects with a `.status` column passed into the template context, so this is a template-only fix. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` (n/a, no `.py` touched), a Jinja `Environment(...).parse()` syntax check on `admin/lists.html`, and an extended `render_lists.py`-style harness (`render_lists_badge.py`) with fixture data reproducing the exact incident (a pending "Missed Call Tracker" project, a pending unrestricted "Outbound Vitel Calls" task in the shared Category grouping, and a pending project-scoped "ParaLeagle case creation" task nested under a project) confirming the badge renders next to all three pending items and is absent from every approved project/task in the same fixture, plus an HTML div-balance check on the rendered output (balanced).

- **Bank & statutory details (Employment Details) hidden by default behind a new Super-Admin-toggleable setting (Ganesh, 2026-09-03)** — "hide employements details section as of now for everyone and just give an option in settings where super admin can enable or disable... make it by default disabled." Unlike every other on/off flag in this app (`TICKETING_ENABLED`, `HOLIDAY_MANAGEMENT_ENABLED`, `LEAVE_MANAGEMENT_V2_ENABLED`, `MULTILEVEL_APPROVAL_ENABLED` — all read from an env var in `app/templating.py` and frozen at process start), this one is deliberately a `Config`-table value (`employment_details_enabled`, new key in `CONFIG_DEFAULTS`, `app/models.py`, default `"0"`), exposed as a real checkbox on `/admin/config` (`config_page`/`config_save` in `app/routes/admin.py`, same `"1"`/`"0"` string-boolean convention `comp_erases_strike` already uses) — Ganesh explicitly asked for a Super Admin to flip it at runtime with no redeploy, which an env var can't do. "For everyone" per his own wording — hidden from BOTH surfaces this data appears on, not just employee self-service: `profile.html`'s "Employment Details" card (`profile_page()` now takes `db` and passes `employment_details_enabled` into context) and `admin/person.html`'s read-only "Bank & statutory details" card (`person()` already computes `cfg`, just added the same boolean to its render context). The two routes that actually read/write `EmployeeBankDetails` — `employment_details_page()` (GET) and `employment_details_save()` (POST), both `app/routes/employee.py` — 404 outright while the flag is off, the exact same convention `HOLIDAY_MANAGEMENT_ENABLED`'s routes already use for a disabled feature (blocks a direct/bookmarked URL hit, not just the now-also-hidden card that used to link there). Existing `EmployeeBankDetails` rows already on file are completely untouched either way — this only ever controls whether the section is *shown*, never deletes or blocks what's already saved, so flipping the setting back on later brings everything right back exactly as it was. **Real regression caught and fixed in the same pass, not left for later**: `_needs_profile_reminder()` (`app/templating.py`) — the "complete your profile" popup — previously nagged an employee whenever `bank_details is None`, which is now true for essentially everyone by default; left unfixed, every employee would get nagged forever with literally no way to satisfy it (no route to reach the form). Fixed by short-circuiting on `employment_details_enabled` before ever checking `bank_details` — reads it via `object_session(user)` (the same live SQLAlchemy session this function's own docstring already relies on for lazy-loading `personal_details`/`bank_details`) rather than threading a new `db` argument through `render()`'s many employee-page call sites, most of which don't currently pass one; falls back to "don't require it" (matching the feature's own default) if no live session is found. `personal_details` completion is completely unaffected — still required unconditionally, exactly as before. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` across every touched `.py` file, a Jinja parse check on all four touched templates (`profile.html`, `profile_employment_details.html`, `admin/config.html`, `admin/person.html`), a render+assert harness confirming the card is present/absent on `profile.html` and `admin/person.html` and the checkbox is checked/unchecked on `admin/config.html` for both flag states, and a standalone execution of the real extracted `_needs_profile_reminder()` source (not hand-copied) against 7 worked scenarios (no user, dismissed, personal details missing regardless of the flag, bank details missing with the flag off, bank details missing with the flag on but no live session, bank details missing with the flag on and a live session — the one case that actually nags, and both cards complete) — all 7 matched.

- **Projects & Tasks: the "All departments" Category → Task hours-rollup grouping removed; a plain "Tasks not linked to any project" list takes its place (Ganesh, 2026-09-03, real bug report)** — Ganesh flagged the 2026-08-30 Category → Task breakdown under "All departments" → "General" as "showing all the tasks of all departments" and asked "why its showing duration this is a serious bug," then described the structure he actually wants: "under all department we have projects and tasks under each project where every one can access." Investigated rather than assumed: the grouping was never leaking cross-department data — it was correctly scoped to only the truly unrestricted tasks (`task_project_ids` empty) — but since only a handful of the ~38 real tasks have ever been explicitly linked to a project (same scale note as the original 2026-08-30 build), the vast majority landed in this one bucket, which read exactly like "every task, everywhere." Confirmed via two AskUserQuestion rounds rather than guessing which part to fix: (1) "under all department we have projects and tasks under each project... why its shwoing time" — remove the grouping (and its all-time hours figure) entirely, so "All departments" reads exactly like every other department group (a plain project list, each project showing only its own explicitly linked tasks) — no separate task-level breakdown or duration figure anywhere on this page; (2) since the old flat "All Tasks" table was already removed 2026-08-30, and a task never linked to any project would otherwise become completely unmanageable from this page (no rename/deactivate access anywhere), Ganesh asked for a lightweight replacement: "Yes, add a simple list back (Recommended)." `lists_page()` (`app/routes/admin.py`) dropped the `task_minutes`/`task_hours`/`shared_task_categories` computation (the `TaskEntry` duration-sum query and the whole category-grouping build) entirely, replacing it with one line — `unrestricted_tasks = sorted((t for t in tasks if not task_project_ids.get(t.id)), key=lambda t: (not t.active, t.name))` — the exact same underlying task set, just no longer bucketed by category or annotated with hours. `admin/lists.html`'s "All departments" node lost its whole `{% if shared_task_categories %}...{% endif %}` block and its meta line went back to a plain `"N project(s)"` (matching every sibling department group's own meta exactly); a new `.card` below the tree, "Tasks not linked to any project," lists each one (name, pending-approval/inactive badges, Super-Admin-only Edit/Deactivate) reusing the tree's own `.tree-task-row`/`.tree-task-acts`/`.tree-task-rename` classes and the existing `treeToggleTaskRename()` JS verbatim — no new CSS/JS needed for the list itself. This also structurally fixes the second bug report in the same message: "under genral its showing 'send client emails' but when i am searching its not showing" — `treeFilter()` only ever matched against `.tree-proj` elements' `data-tasks` attribute, so a task visible ONLY via the old Category grouping (never linked to any project, hence never in any project's `data-tasks` string) could never be found by the search box, and typing its name would in fact hide the whole "All departments" `<details>` (since no `.tree-proj` matched, `anyVisible` stayed false). The new list isn't wrapped in any collapsible/filterable element at all — it's always fully visible — so this class of bug can't recur for an unlinked task; no patch to `treeFilter()` itself was needed or made. Deliberately NOT touched: `TaskType.category` the column itself (still real, still admin-settable in the DB) — only its one display/rollup consumer on this page went away, per Ganesh's own scope answer; a project's own "Manage departments"/"Manage projects" narrowing controls elsewhere in the tree are completely unaffected. Cleaned up now-fully-dead CSS in the same pass (`.tree-hrs`, `.tree-cat-list`, and `.tree-cat` dropped from every shared selector it appeared in — `app/static/app.css`) rather than leaving unused rules behind, confirmed via grep that nothing else referenced them. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` on `app/routes/admin.py`, a Jinja parse check on `admin/lists.html`, CSS brace-balance re-confirmed, and a render+assert harness reproducing the exact bug report (a "Send client emails" unrestricted task, a project-linked task, and a pending suggestion) confirming: no `tree-cat`/`tree-hrs`/hours-figure markup renders anywhere on the page, "All departments" meta is project-count-only, the new list contains "Send client emails" but correctly excludes any project-linked task, and the earlier pending-approval badge fix still works — plus an HTML div-balance check on the rendered output (balanced).

- **"Tasks not linked to any project" gained a "Manage projects" action, same day (Ganesh, 2026-09-03: "can these be allocate to particular projects")** — the new list itself (see the bullet just above) only offered Edit/Deactivate; linking one of these tasks to a specific project required going the OTHER direction (a project's own "+ Task" quick-add, which creates a brand-new task rather than re-scoping an existing one). Turns out `lists_task_projects()` (`POST /admin/lists/task/{id}/projects`, `app/routes/admin.py`) — the real replace-all "link this task to project(s)" route, built 2026-08-27 — was still fully live server-side; it simply lost its only UI panel when the flat "All Tasks" master table was removed 2026-08-30, so there had been no way to reach it since. Restored as a per-row "Manage projects" action on the new list (`admin/lists.html`) — a `treeToggleTaskProjects()` toggle (mirrors the existing `treeToggleTaskRename()` pattern exactly) reveals a form posting to that same route, using the shared `.msdrop` search+checkbox widget (`app/static/msdrop.js`/`msfilter.js`) rather than a flat checkbox list, since there are ~300 real Project/Employer rows and a plain list that size would be unusable — same "~300 possible options" precedent Time by Project/Task's own Projects filter already established for this widget. Every row here is unrestricted by definition (`task_project_ids` empty), so nothing is ever pre-checked; saving with ≥1 box checked moves that task out of this list entirely on the next load, onto its newly-linked project's own tree node (`lists_task_projects()`'s own flash message already confirms the count). New CSS: `.tree-task-projects` (drops to its own full-width line like `.tree-task-rename`, holds the block-level `.msdrop` widget instead of a single inline input) in `app/static/app.css`. No Python/route changes at all — the backend was already correct and untouched; this was purely restoring a missing UI entry point to an existing capability. Verified via a Jinja parse check, CSS brace-balance (359/359), `node --check` on the extracted inline `<script>`, and the same render+assert harness from the bullet above extended to confirm: the "Manage projects" action and its panel render on an unlinked task's row, the form posts to `/admin/lists/task/{id}/projects` with `name="project_ids"` checkboxes for every real project (including a still-pending one — linking doesn't require the project to be approved first), the `treeToggleTaskProjects` function is defined, and the page stays HTML-balanced.

- **Bug fix, same day — "+Task" quick-add rejected an existing unlinked task instead of linking it (Ganesh, 2026-09-03: "when i try to move some of the tasks not linked to a project to the project it says they have already been added and leaves them in the unlinked list")** — the natural, most-discoverable way to "move" a task out of the new "Tasks not linked to any project" list (see the two bullets above) is to type its exact name into the TARGET project's own "+ Task" quick-add form — more prominent than that list's own "Manage projects" panel a level down the page. `lists_add()` (`app/routes/admin.py`) already had a duplicate-name guard for exactly this case (`exists = db.execute(select(model).where(model.name == name)).scalar_one_or_none()`) — its own pre-existing comment even said as much ("To link the same task name to MORE projects later, use that task's 'Manage projects' panel... rather than re-adding it here") — but the `else` branch just flashed `"'{name}' already exists."` and returned, never touching `ProjectTask` at all, so the task stayed exactly where it started: unlinked. Fixed by giving `kind == "task"` its own branch instead of falling into that generic message: if the existing task isn't already linked to the target project, add the link (`ProjectTask(project_id=project.id, task_type_id=exists.id, ...)`) and flash a success message ("'X' already existed — linked it to Y as well.") instead of failing; if it's already linked to that exact project, flash a clear "'X' is already linked to Y." rather than the generic duplicate message. Deliberately additive, not replace-all: if the task was previously unrestricted (zero `ProjectTask` rows), this correctly narrows it to just the picked project — the whole point of "moving" it out of the unlinked list — but if it already had OTHER project links, those are left alone and this new one is added alongside them; only the "Manage projects" panel's own replace-all form (`lists_task_projects()`, unchanged) redefines a task's entire link set at once. `kind == "project"`'s own duplicate-name behavior is completely unchanged — still the plain "already exists" rejection, since a project has no equivalent "which project is it linked to" ambiguity to resolve. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` and a standalone execution of the real extracted `lists_add()` source (not hand-copied) against a small fake-ORM harness (`Project`/`TaskType`/`ProjectTask` stand-ins with a minimal `db.execute(select(...).where(...))`/`db.get()` shim) covering 5 scenarios: a brand-new task still creates+links exactly as before; the exact reported bug (an existing unlinked task typed into a different project's "+ Task" form now gets linked there, no duplicate `TaskType` row, a success flash); the same task typed a second time into the SAME project (now already linked) gets a clear no-op error instead of a duplicate `ProjectTask` row; an existing task already linked to Project A gets an ADDITIVE second link when typed into Project B's form, with the Project A link left untouched; and a duplicate PROJECT name is still rejected exactly as before. `tests/` has no route-level test file for `app/routes/admin.py`'s Lists functions to extend, matching this project's own established precedent for route-level (non-engine/validation) changes elsewhere this session.

- **Projects & Tasks: deactivated projects/tasks pulled out of the tree into their own two lists, with Reactivate + a way to change their department(s)/project links right there (Ganesh, 2026-09-03)** — "if we deactivated those are still showinf under each projects but as greay out and as deactivated, i want it to be like unlinked tasks how we have right now... i want deactived list of projects & Tasks from there we can actiave again and evn we can chnages the to whicuh departmet it should belongs." Before this, deactivating a project or task left it exactly where it always was in the Department → Project → Task tree, just grayed out with an "(inactive)" label — findable only by knowing which department/project to look under, and with no way to change a deactivated project's department scoping without reactivating it first. Confirmed via one 3-question AskUserQuestion round (all "Recommended"): deactivated items are removed from the tree entirely rather than staying inline grayed-out; projects and tasks get two SEPARATE lists rather than one combined one; and a deactivated task's "Manage projects" control is the exact same widget the "Tasks not linked to any project" list (2026-09-03, earlier the same day) already has, not a new one.

  `lists_page()` (`app/routes/admin.py`) now builds `active_projects` (`[p for p in projects if p.active]`) as the one filter every "what's offered/what's in the tree" computation in this function derives from — `shared_projects` and each `dept_groups` entry's `projects` list are now built from `active_projects` instead of the full `projects` list, so an inactive project simply never enters the tree-building loop at all (not a template-side `{% if p.active %}` filter, which would've left `.tree-proj-nm`'s existing gray-out CSS as dead, unreachable code). New `inactive_projects = sorted((p for p in projects if not p.active), key=lambda p: p.name)` is the Deactivated Projects list's own source list. `project_task_ids` (which active project each active task is nested under, in the tree) is now built by first filtering out any task that's inactive OR whose only remaining links point at now-inactive projects — deliberately keyed off `active_project_ids`, not just "does a ProjectTask row exist," since deactivating a project must not orphan a still-active task that happens to only be linked to that one project (a real case worth testing, not a hypothetical — see the harness below). `unrestricted_task_count` and `unrestricted_tasks` (the "Tasks not linked to any project" list, 2026-09-03) got the identical `active_project_ids`-based redefinition — a task linked only to an inactive project now correctly reads as "unrestricted" again for THIS list's purposes, rather than disappearing into neither list. New `inactive_tasks = sorted((t for t in tasks if not t.active), key=lambda t: t.name)` is the Deactivated Tasks list's own source list.

  Both new lists in `admin/lists.html` reuse the exact `.tree-task-row`/`.tree-task-acts`/`.tree-task-rename` markup/CSS the "Tasks not linked to any project" list already established (2026-09-03) — no new row-level CSS needed beyond one new class. Deactivated Projects: each row gets Edit (rename), a new "Manage departments" toggle (`treeToggleProjectDepts(btn)`, new `.tree-proj-depts` CSS class — same drop-to-full-width-line pattern as `.tree-task-projects`) revealing the SAME `.deptpills`/`.deptpill` checkbox-chip form a project's own in-tree edit panel already uses, posting to the existing `/admin/lists/project/{id}/departments` route and pre-checked from `project_department_names.get(p.id, [])`, and a "Reactivate" button posting to the existing `/admin/lists/project/{id}/toggle` route — no new routes for any of this, purely a relocated UI surface over routes that already existed. Deactivated Tasks: each row gets Edit, the identical "Manage projects" `.msdrop` panel the unlinked-tasks list already has (posting to the existing `/admin/lists/task/{id}/projects` route) — except here a checkbox IS pre-checked whenever `p.id in task_project_ids.get(t.id, [])`, since (unlike an unrestricted task, which by definition has zero links) a deactivated task may already have real project links from before it was deactivated, and the panel needs to show them — and a "Reactivate" button posting to `/admin/lists/task/{id}/toggle`. Both Manage-projects/Manage-departments pickers on the new lists deliberately only ever offer `active_projects`/`all_departments` as pickable targets (never `projects`/inactive ones) — linking to a deactivated project was never meaningful, matching the existing unlinked-tasks list's own scoping.

  No `app/engine.py`/`app/validation.py` changes — verified via `py_compile` on `app/routes/admin.py`, a Jinja `Environment(...).parse()` check on `admin/lists.html`, a CSS brace-balance check (360 open == 360 close), `node --check` on the extracted inline `<script>` (confirming the new `treeToggleProjectDepts` function is syntactically valid alongside the pre-existing toggle functions), and an extended Jinja render+assert harness with fixture data specifically covering the orphaning edge case (an inactive task, "Old intake form," whose only `ProjectTask` link points at a still-active project — confirmed it disappears from that project's own tree node, and reappears correctly in Deactivated Tasks with that project's checkbox pre-checked, never silently vanishing from both places) plus a second inactive, never-linked task (confirmed nothing pre-checked), a second inactive, department-scoped, Case-Type project (confirmed its Case Type badge and both its existing department checkboxes render pre-checked, and the third, unlinked department does not) — all assertions passed, plus an HTML div-balance check on the rendered output (balanced).

- **Today page: Mon-Sun week-position circles now color-code weekends/off-days separately (Ganesh, 2026-09-03)** — "i think the weekends should be in other color as they are not working days but some employee will do on that day to compensate." `_week_day_circles(today, work_days)` (`app/routes/employee.py`, built 2026-08-30) now takes a second `work_days` argument (the caller passes `user.work_day_set`, the same source `_month_summary()`'s calendar and `_plan_ahead_max_date()` already read) and each returned circle carries a new `off_day` flag — True whenever that weekday isn't in the employee's own schedule. This generalizes beyond a hardcoded Sat/Sun to any non-Mon-Fri work pattern the model already supports. `today.html`'s circle row adds an `off-day` class and an extended tooltip ("— not a working day, but you can still log hours to compensate") whenever the flag is set — deliberately just a color cue, never disabled or struck through, since the entire point of the ask is that hours CAN and do get logged on an off-day to compensate. New CSS in `app/static/app.css` reuses the existing `--hol-bg`/`--muted` pairing `.b-holiday`/`.b-weekend` and `.c-holiday`/`.c-weekend` already use elsewhere for "non-working day," rather than inventing a new hue — via two-class selectors (`.week-circle.past.off-day`, `.week-circle.upcoming.off-day`) so they win over the single-class `.past`/`.upcoming` rules regardless of source order. `.week-circle.today.off-day` is spelled out explicitly (matching `.today`'s own values) so a day that happens to be both "today" and an off-day is unambiguously still the highlighted "today" color, not the muted off-day one. No `app/engine.py`/`app/validation.py` changes — verified via `py_compile`, a CSS brace-balance check, a standalone execution of the real extracted `_week_day_circles()` source against 3 worked cases (a Thursday "today" on a Mon-Fri schedule — Sat/Sun both flagged off_day, everything else not; a Saturday itself as "today" — confirms `state="today"` and `off_day=True` coexist correctly, the edge case the explicit `.today.off-day` CSS rule exists for; and a non-Mon-Fri Tue-Sat schedule — confirms Monday alone gets flagged off_day, not Sat), and the `render_today.py` harness (all 8 scenarios, whose fixed "today" of 08/29/2026 is itself a Saturday) re-run with updated assertions confirming the off-day class/tooltip render correctly on Sat+Sun only and an HTML balance check across all 8 scenarios.

- **Automatic compensation balance: source switched from Punch In/Out to logged Task Log time (Ganesh, 2026-09-03)** — reacting to a screenshot of My Month's "Compensation owed this month" KPI tile: "this should be come from task logs auto sum time right why its comming from punch in clock." This module (`app/compensation.py`, built 2026-07-31 — see its own module docstring for the full pre-existing design) always compared completed Punch In/Out minutes (netting out BreakEntry time) against each day's `DayStatus.target_minutes` to compute a self-service monthly shortfall/overtime balance, deliberately independent of Task Log time at the time — by 2026-09-03 that was the odd one out, since the 2026-08-28 "Auto-count logged hours" change had already made Task Log time the one authoritative "actual hours" measure everywhere else (DayStatus.actual_minutes, strikes, compliance). Confirmed via AskUserQuestion (3 options: switch to Task Log time / keep Punch-based / remove the KPI entirely) — Ganesh picked the switch.

  `_completed_punch_minutes_by_day()`/`_completed_break_minutes_by_day()` (the Punch/Break netting pair) were replaced with one `_logged_minutes_by_day()` — a grouped `select(TaskEntry.date, func.sum(TaskEntry.end_minute - TaskEntry.start_minute)).group_by(TaskEntry.date)` query, the same shape `engine.recompute_employee()`'s own `task_totals` query already uses — no break-netting step needed anymore since a TaskEntry row never overlaps a break by construction (`validate_entry()` already rejects that), unlike a Punch In/Out session which could span one. `monthly_summary()`'s per-day dict key renamed `"punched"` → `"logged"` throughout (both templates that read it — `my_month.html`'s KPI tooltip, `admin/person.html`'s "Task Log compensation (automatic)" card, renamed from "Punch Clock compensation (automatic)", including its `<th>Logged</th>` column and the `d.logged` value) — updated together, not left mismatched.

  **Known, accepted consequence, documented in the module's own docstring rather than silently changed**: for any day with a positive target, `overtime - shortfall` now equals exactly `actual_minutes - target_minutes` for that day — the same figure `engine.running_ledger()` sums into My Month's "Running hours balance" KPI shown directly above this one. Since this function's own long-standing rule (unchanged by this switch) only ever looks at days with `target_minutes > 0` — skipping every leave/holiday/weekend day entirely — while `running_ledger()` sums every DayStatus row in range including target=0 days (where `variance_minutes = actual_minutes`, so hours logged on an off day to compensate count straight in), the two balances will now usually match but can genuinely diverge whenever hours were logged on an off day: `running_ledger`'s balance picks that up, this one's does not. Narrowing/widening that day-inclusion rule wasn't part of what was asked — only the data source was — so it was deliberately left alone; flagged here in case it's worth revisiting given the same-week the weekend-circle-coloring feature (right above this one) was built specifically because employees do log hours on off days to compensate.

  Both KPI tooltips were rewritten to be honest about this near-overlap rather than implying two unrelated numbers (`my_month.html`: "From your logged task rows, same source as the Running hours balance above... One real difference: hours logged on an off day... count in the balance above but not in this one"). No `app/engine.py`/`app/validation.py` changes (a new query inside `app/compensation.py`, which was always deliberately independent of those two files — see its own docstring) — verified via `py_compile`, a Jinja parse check on both touched templates, a from-scratch extracted-and-exec'd simulation of the real `_logged_minutes_by_day()`/`monthly_summary()` source (not hand-copied) against 11 scenarios — every pre-existing shortfall/overtime/credit/floor/month-isolation/sort-order case reproduced with TaskEntry rows instead of PunchSession/BreakEntry, plus a new case confirming "logged" is computed independently of a deliberately-stale `DayStatus.actual_minutes` value, plus a check that the real source no longer queries `m.PunchSession`/`m.BreakEntry` anywhere — a targeted Jinja-string render of `admin/person.html`'s comp KPI + table block confirming the new tooltip/labels/`d.logged` all render correctly, and the `render_my_month.py` harness re-run clean (HTML-balanced, new tooltip text present, old "From Punch In/Out" text gone). `tests/test_compensation.py` was rewritten in the same pass — every `_punch()`/`_break()` fixture helper call replaced with `_entry()` (seeding `TaskEntry` rows directly, same convention `test_reports.py`'s own `_entry()` helper already uses), the whole `TestBreakTimeExcludedFromPunchedMinutes` class removed (break-netting no longer exists to test) and replaced with a `test_multiple_entries_same_day_are_summed` case plus a new `TestSourceSwitchMatchesDayStatusActual` class — still needs a real `pytest` run to confirm, not just the extracted-source simulation (same missing-legacy-bundle/no-pytest sandbox gap as every other change this session).

- **Bug fix: a running Break and a running Auto time capture timer could overlap live, exactly the case `validate_entry()` already blocks for a manual Add Task row (Ganesh, 2026-09-03, from two screenshots: an Auto time capture timer shown "started 9:35 AM" against Project "Bluepeak Technologies Inc." / Task "Plan for the Day", running at the same time as an already-logged Break (9:35 AM–9:37 AM, Task "Break") — plus the red validation banner that appeared the moment the employee then tried to Add Task into that same window)** — "it should not happen like this right, wile running the break how come auto time capture will start it should not, if breka timer is running the the auto time capture should be restrict or it hsould stop the breka time and log that break and the auto time capture should start in same way if auto time capture is running and if i clicked on break the auto time capture should pause and log that task and break time should start, these are the basic things please fix these." Root cause: `BreakEntry` (Punch/Break) and `ActiveTaskTimer` (Auto time capture/Plan Start-Resume) have each independently enforced "only one of me running at a time" since their own respective builds — `start_task_timer()`/`start_plan()` already call the shared `_stop_current_timer_if_any()` (2026-08-21) to auto-finish whatever OTHER task timer was running before starting a new one, and `start_break()` already blocks a second concurrent Break — but neither code path had ever checked the OTHER type's active state at all, so a Break and a Timer could both be "currently running" for the same employee at the same moment with nothing to reconcile them. `validate_entry()`'s own overlap check (unchanged, already correct) is what produced the red banner — it correctly rejects a manual entry that overlaps an already-logged break — but the two LIVE start buttons had no equivalent live-side guard against each other.

  Fixed by extending the exact same "starting something live auto-finishes whatever else was running" convention `_stop_current_timer_if_any()` already established between two task timers, to now also cover Break, symmetrically in both directions — no new restrict-only option was added (the "restrict OR auto-stop-and-log" framing in the first half of the request was resolved in favor of auto-stop-and-log, matching the unconditional wording used for the reverse direction, and keeping both directions consistent with each other and with the existing timer-vs-timer precedent). New `_end_current_break_if_any(db, user)` (`app/routes/employee.py`, sits right after `end_break()`) is the Break-side mirror of `_stop_current_timer_if_any()` — finds today's still-open `BreakEntry` (`end_minute IS NULL`) if any, closes it using the exact same `clamp_break_end()` logic `end_break()` itself uses, and commits; a plain no-op, no `(ok, error)` return, when no break is running (ending a break can't itself fail validation the way finishing a timer into a `TaskEntry` can). `start_task_timer()` and `start_plan()` both now call `_end_current_break_if_any(db, user)` first — before their existing `_stop_current_timer_if_any(db, user, cfg)` call — so starting either kind of task timer always closes out a running break first, then closes out any other running timer, then starts the new one. `start_break()` now calls the existing `_stop_current_timer_if_any(db, user, cfg)` before creating its new `BreakEntry` — same failure handling every other caller of that helper already has (`ok, error` — if the running timer can't be auto-saved, e.g. a details-length rule, the employee sees the same "Couldn't save the timer already running: {error}" flash and the break doesn't start, rather than silently discarding logged time). No new Config/flag — this is a correctness fix to an existing, always-intended "one live thing at a time" rule, not a new feature to gate.

  No `app/engine.py`/`app/validation.py` changes (`validate_entry()`'s own overlap/cap/backdate rules are completely unchanged — both directions of this fix route through the exact same `_finish_task_timer()`/`_log_timer_as_entry()` → `validate_entry()` chain every other timer-stop already used, and Break has no `validate_entry()` involvement at all) — verified via `py_compile` on `app/routes/employee.py`, and a standalone extraction of the real `_end_current_break_if_any()` source (not hand-copied) executed against a fake DB/BreakEntry harness covering 3 cases (an active break gets closed at exactly "now," an already-ended break is left untouched with no wasted commit, and a DIFFERENT employee's active break is never touched), plus a structural regex check on the real source of all three call sites confirming the correct order in each: `start_break()` calls `_stop_current_timer_if_any` before `db.add(m.BreakEntry(...))`; `start_task_timer()`/`start_plan()` both call `_end_current_break_if_any` before `_stop_current_timer_if_any` before their own `db.add(m.ActiveTaskTimer(...))` — matching this project's own established route-level (non-engine/validation) verification precedent, since `tests/` has no route-level test file for `app/routes/employee.py` to extend. Still needs a real device/staging click-through to confirm the end-to-end UX (flash wording, redirect) once deployed, same as every other route-only fix this session.

- **Bug fix: manually adding a task could overlap a currently-running Auto time capture timer, which then blocked Stopping the timer (Ganesh, 2026-09-03, from a screenshot — same session as the Break/Timer fix above, same day)** — "at the same time i can bale ti add task in add task manually with time frame 11:50 to 12:50 i have aded now if i am trying to sto the auto time capture its showing this error" (the red "Overlaps existing row 11:50 AM–12:20 PM" banner). Root cause: `validate_entry()`'s overlap logic already checked a new row against every other logged `TaskEntry` and every `BreakEntry` for that day, but never against a currently-running `ActiveTaskTimer` — since the timer hasn't become a `TaskEntry` yet, there was nothing for the manual Add Task row to conflict with at the moment it was saved. The conflict only became visible later, when Stop tried to log the timer's own segment across that same now-occupied window and hit the ordinary overlap check — by which point the employee had no way to fix it except Cancel (discarding the timer's real elapsed time).

  Presented as a choice (AskUserQuestion) rather than assumed: fully disable Add Task while any timer is running, or block only a row that actually overlaps the timer's live window. Ganesh picked the latter (Recommended) — narrower, and lets an employee still log something unrelated (e.g. a forgotten entry from earlier this morning) while a timer keeps running elsewhere in the day. Fixed by treating a currently-running `ActiveTaskTimer` exactly like an open/still-running `BreakEntry` already is in the same function — its real end time isn't known yet, so it blocks *from its start to end of day*, not just up to "now" (allowing something between "now" and the eventual real Stop time would just recreate the same failure one step later). New block in `validate_entry()` (`app/validation.py`), placed right after the existing break-overlap check: looks up the employee's `ActiveTaskTimer` for that exact date and, if one exists and its `start_minute < end_minute` of the row being validated, adds "Auto time capture is currently running (started {time}) — pause or stop it before logging a row that overlaps that time." Skipped when `closing_existing=True` — that flag is only ever passed by `_log_timer_as_entry()` finishing THE running timer itself (one timer per employee, DB-enforced), so the timer this new check would find is always the exact entry being validated, not a different one to protect against; without this skip, a timer could never successfully Stop at all, since its own final segment would always "overlap" itself. `suggest_non_overlapping_start()` (the "nudge to a valid start time" helper `add_entry()`'s failure path already calls) got the identical addition, duplicated on purpose per that function's own docstring (it deliberately mirrors `validate_entry()`'s overlap conditions rather than calling it) — a manual entry that gets rejected for overlapping a running timer now also gets bumped to a suggested start of end-of-day (1440), same as it already does for an open break, rather than leaving the employee to guess.

  `today.html`'s Add Task rules line ("Rules: no overlaps · can't log over a logged break · ...") was updated to "· can't log over a logged break or a running timer ·" so the on-screen rule list stays accurate. No `app/engine.py` changes — verified via `py_compile` on `app/validation.py`, a Jinja syntax parse on `today.html`, a truth-table extraction of the real new block from both `validate_entry()` and `suggest_non_overlapping_start()` (not hand-copied) run against 6 and 3 cases respectively — the exact screenshot repro (11:50 AM timer, 11:50–12:30 PM manual add → blocked), starts-before-runs-into, touching-the-timer's-start (allowed), entirely-after-the-timer's-start (still blocked, real end unknown), `closing_existing=True` (never blocked), and no-timer-running (no-op) — all passed. New pytest coverage added to `tests/test_validation.py`: `TestActiveTimerOverlap` (5 tests), plus one new case each in `TestSuggestNonOverlappingStart` and `TestClosingExistingBypass` — still needs a real `pytest` run to confirm, not just `py_compile`/the truth-table check, same missing-legacy-bundle/no-pytest sandbox gap as every other `validation.py` change this session.

- **Per-employee clock timezone for live actions (Ganesh, 2026-09-04)** — "as of now the default time zone is CST right but now new employees joined they will be working in IST hours so the auto time capture and today plan tasks timer should capture the time in IST... it should not convert to CST." This directly touches the "one fixed reference timezone, not per-employee ones" hard rule two sections up (manager request, 2026-08-10) — flagged explicitly before building anything, since the exact same instinct (per-country/per-employee handling) was tried once for Holidays (2026-08-12) and reverted within days. Resolved as a narrow, deliberate **exception**, not a reversal: only which real-world clock a LIVE action (Auto time capture, Plan Start/Pause/Resume/Stop, Break Start/End) reads its start/end minute FROM now follows the employee's own clock — everything else that hard rule was actually protecting (which calendar day something belongs to, so every employee still shares one business day) stays exactly as it was. `today_local()`/`BUSINESS_TZ` are completely unchanged and still drive Submit Day locking, the backdate window, holiday matching, and the strike/compliance day boundary; only the MINUTE captured for these live buttons changes. This means the change touches neither `app/engine.py` nor `app/validation.py`, so the pytest+verify_strikes hard rule doesn't gate it (same missing-legacy-bundle/no-pytest sandbox gap as every other non-engine change this session).

  Reuses the existing `Employee.location` column (`app/models.py`, added 2026-08-12 for now-abandoned per-country Holiday Management, self-service via Profile's Country picker, otherwise inert since holidays stopped reading it 2026-08-14) rather than adding a new `time_zone` field — location already fully answers "which clock should this employee's own buttons read from," and a second field just risks the two silently disagreeing. New `EMPLOYEE_CLOCK_TIMEZONES` dict + `employee_clock_tz(emp)`/`now_for_employee(emp)` (`app/util.py`, right after `today_local()`) map `LOCATION_INDIA` → `ZoneInfo("Asia/Kolkata")`, `LOCATION_US` → `BUSINESS_TZ`, with any blank/unrecognized value falling back to `BUSINESS_TZ` — the same "never guess, fall back to the shared default" instinct `DEFAULT_LOCATION`'s own docstring already uses, so a future third country or an unset value behaves exactly like today, not a crash. All 7 places in `app/routes/employee.py` that previously called `now_local()` to stamp a live action's minute now call `now_for_employee(user)` instead: `start_break()`, `end_break()`, `_end_current_break_if_any()`, `_auto_split_timer_if_over_cap()`'s same-day elapsed-minutes calculation, `_finish_task_timer()`, `start_task_timer()`, `start_plan()`. Deliberately NOT touched: `_auto_split_timer_if_over_cap()`'s cross-midnight rollover (still `dt.datetime.combine(next_day, dt.time(0,0), tzinfo=BUSINESS_TZ)`, unchanged) — a day boundary, not a live-action minute; Punch In/Out (`punch_in()`/`punch_out()`) — `PunchSession` only ever stores pure-elapsed-duration UTC audit timestamps (`dt.datetime.utcnow()`), never a clock-face-of-day value, same as every other audit timestamp in this app; manual Add Task (`add_entry()`) — an employee types explicit HH:MM values with no `now_local()` involved at all, so there was nothing to swap. Confirmed via grep that `app/routes/admin.py` has zero `now_local()` calls, so it needed no changes either.

  `DEFAULT_LOCATION` (`app/models.py`) flipped from `LOCATION_INDIA` to `LOCATION_US` the same day, per Ganesh's own explicit ask ("by default for everyone it should show USA only, if any employees want to work in IST they should change it to india in profile section") — a brand-new employee added with no explicit location now defaults to US/CST going forward, the opposite of the pre-existing default. This only affects a fresh INSERT with no explicit location; it does not touch any existing employee's already-stored `location` value. Caught and fixed before it could ship as a real bug, not after: `ensure_location_backfill()` (`app/util.py`) previously stamped legacy pre-2026-08-12 NULL `Employee.location`/`Holiday.location` rows with `m.DEFAULT_LOCATION` — since this backfill represents a historical fact ("these rows predate the column and were originally all India-based," per the module docstring at the top of this file), leaving that reference unchanged would have meant flipping `DEFAULT_LOCATION` also silently changed what any future run of this backfill stamps genuinely-India-based legacy employees as. Fixed by hardcoding both assignments to `m.LOCATION_INDIA` directly, decoupling "what a brand-new employee defaults to today" from "what a pre-existing legacy row's location historically was" — the two are conceptually different things that happened to share one symbol before this change. `tests/test_roster_location_flag.py`'s own docstring and its one default-value test were updated to reference `m.DEFAULT_LOCATION`/`m.LOCATION_*` symbols rather than hardcoded "India"/"US" strings, so they keep passing correctly through a future default change like this one without needing an edit.

  **Known, accepted limitation, documented in `now_for_employee()`'s own module comment rather than solved here**: because "today" stays `BUSINESS_TZ`-based while the minute now follows the employee's own clock, the two agree correctly for any shift falling between `BUSINESS_TZ`'s midnight and the following midnight AS SEEN IN THAT EMPLOYEE'S OWN ZONE — for IST (11.5h ahead of CST/10.5h ahead of CDT) that's roughly 10:30/11:30 AM IST through the same time the next day, which covers a normal afternoon/evening IST shift (confirmed against Ganesh's own worked 1 PM–9 PM IST example: both ends land on the same `BUSINESS_TZ` calendar date). An employee working a very-early-morning IST shift (midnight–8 AM IST) could have that work land on the calendar day `BUSINESS_TZ` still calls "yesterday" at that moment, even though it's their own today — flagged to Ganesh as worth confirming no current India hire is actually on a shift like that, not solved in this pass.

  Verified via `py_compile` across every touched `.py` file, and a standalone extraction of the real `employee_clock_tz()`/`now_for_employee()` source (not hand-copied, via line-slicing the actual file between its `BUSINESS_TZ = ` and `def fmt_hm(` markers) exec'd against fake `Employee`/`m` stand-ins — 10 checks: India→Asia/Kolkata, US→BUSINESS_TZ, unrecognized-location→BUSINESS_TZ, None-location→BUSINESS_TZ, empty-string-location→BUSINESS_TZ, a fixed-UTC-instant sanity check confirming IST reads ≥10h25m ahead of Chicago's reading of the same instant, a structural check that `now_for_employee()`'s real body calls `dt.datetime.now(dt.timezone.utc)` and routes through `employee_clock_tz(emp)` rather than duplicating the lookup, the 1 PM–9 PM IST worked example staying on one `BUSINESS_TZ` calendar date, the known early-morning-IST edge case reproduced exactly as documented (not silently "fixed" by the test), and the literal "2:48 PM IST reads as 14:48 in Asia/Kolkata, no CST conversion" case from Ganesh's own screenshot — all 10 passed. Also confirmed via grep that zero `now_local()` references remain anywhere in `app/routes/employee.py` after the swap. New pytest coverage added to `tests/test_util.py`: `TestEmployeeClockTimezone` (6 tests, right after the existing `TestBusinessTimezone` class) — still needs a real `pytest` run to confirm, not just `py_compile`/the standalone extraction, same missing-legacy-bundle/no-pytest sandbox gap as every other change this session.

- **Deferred Sick-hour (Unplanned Time) compensation via overtime match (Ganesh, 2026-09-04, from a real approval-time hesitation: Sruthi didn't want to approve Ganesh's Unplanned Time request as-is because he wanted to make the hours up with overtime instead of having them debited from his 40-hour Unplanned pool)** — explained to Ganesh with a worked example before building anything, since the real mechanic needed was narrower than it first sounded: approving Unplanned Time already zeroes that day's compliance target via `engine.leave_minutes_on()` — there is never a shortfall/strike to erase for an approved leave day, approved is approved either way. The actual ask was entirely about **when** the 40-hour Unplanned pool gets debited for it — immediately at approval (today's only behavior), or deferred until end-of-month pending an overtime match, with a Super Admin deciding the fallback (Sick time vs. Unpaid Time) if nothing gets matched by then.

  New `LeaveRecord` fields (`app/models.py`): `wants_compensation` (bool, default False — the employee's own opt-in checkbox, Unplanned Time only), `compensation_status` (nullable, one of new `m.LEAVE_COMP_PENDING`/`_MATCHED`/`_RESOLVED_UNPLANNED`/`_RESOLVED_UNPAID` — same "generic status strings, not a new enum per table" convention as `LEAD_ACCEPTED`/`LEAD_DENIED`), `compensation_deadline` (the month-end of the leave's own `start_date`, set at approval time), `compensation_minutes_needed` (the actually-approved minutes — respects partial approval), plus `compensation_resolved_by`/`_at`/`_note` for the eventual manual disposition. New `CompensationLink.pending_leave_id` (nullable FK to `leave_records.id`) discriminates an ordinary shortfall-day link (NULL, unchanged) from a link paying off a deferred leave debt.

  `leave_approve()` (`app/routes/admin.py`): when the leave being approved is Unplanned Time with `wants_compensation=True`, sets `compensation_status=LEAVE_COMP_PENDING`, `compensation_minutes_needed=<the approved minutes>`, `compensation_deadline=<end of that month>` — the leave itself is still approved and still excused immediately, only the pool debit is deferred. `engine.leave_balance_v2()`'s used-minutes loop now skips any `LeaveRecord` whose `compensation_status` is `pending` or `matched` when summing Unplanned Time's Used total — so the 40-hour pool genuinely isn't touched while a match is still possible, exactly mirroring how Planned/Unplanned balances already treat a still-`requested` row as "pending," not "used."

  The employee-facing match request (`overtime.html`'s existing "Request an Overtime ↔ Missed Hours match" card, self-service `POST /leave/match-request`) now accepts either an ordinary shortfall date OR a pending leave debt in the same "Missed Hours day" dropdown, via a `"leave:{id}"` sentinel value alongside the plain ISO dates `request_compensation_match()` already parsed — chosen over a second separate form specifically so this reuses every existing piece: the same `engine.compensation_window_ok()` same-calendar-month check (works for free since `CompensationLink.shortfall_date` is always populated with the leave's own `start_date` even for a leave-debt link), the same Team-Lead/Super-Admin multilevel approval flow, and the same `CompensationLink` table/routes rather than a parallel structure. New `engine.leave_debt_allocated_minutes(db, leave_id)` sums allocated minutes across every link targeting a given leave debt (mirrors `shortfall_allocated_minutes()`); `engine.allocate_surplus_minutes()` gained an optional `leave_debt_id` param that, when set, sources the deficit from `LeaveRecord.compensation_minutes_needed` instead of `DayStatus.variance_minutes` — the actual greedy oldest-first allocation loop underneath is completely unchanged and shared either way, so a leave-debt match and an ordinary shortfall match can never drift apart in how they divide up a surplus day. `engine.evaluate_link()` gained an early branch for `pending_leave_id is not None`: sets that one link's own `fully_compensated`, and flips the leave's `compensation_status` from `pending` to `matched` once the AGGREGATE across every link targeting that debt reaches the full amount needed — so, matching Ganesh's own worked example, a first partial link (say 2h of an 8h debt) leaves it `pending`, and a second link finishing the remaining 6h is what actually flips it to `matched`.

  **Double-spending an overtime day between an ordinary shortfall match and a leave-debt payoff is prevented for free**, not by new code: `engine.surplus_minutes_used_by_date()` already sums claimed minutes across EVERY `CompensationLink` for an employee regardless of what each one targets, and both allocation paths already call it — confirmed via a dedicated pytest test and an independent standalone plain-Python simulation, both showing an 8h surplus day correctly splits into "4h already claimed elsewhere, 4h left" rather than ever offering the same hour twice.

  New Super-Admin-only route `POST /admin/leave/{leave_id}/resolve-compensation` (`resolve_leave_compensation()`) is the manual fallback for a debt whose deadline has passed with no full match — `disposition=unplanned` leaves the record as Unplanned Time and flips it to `resolved_unplanned` (the 40-hour pool now counts it as Used, same as if compensation had never been requested); `disposition=unpaid` additionally changes `LeaveRecord.type` to `Unpaid Time` and flips it to `resolved_unpaid` (Unpaid has no cap, so nothing further is "used" against anything). No server-side deadline re-check on this route by design — a Super Admin reviewing it after the fact already implies the deadline's relevance has been judged; the admin UI only ever shows the Resolve buttons once `compensation_deadline < today` in the first place. `leave.html` (employee), `overtime.html` (employee, the match-request picker + `match_links` history table), and `admin/leave.html` (a new "Sick-hour compensation in progress" card, Super-Admin-only, listing every still-`pending` debt with a live "matched so far" figure and Resolve buttons once overdue, plus status notes added to the existing "Approved leaves" table) all surface the 4 states in plain language.

  **Deliberate scope boundary**: admin-direct linking (`add_complink()`, Person Detail/Overtime Management's admin-initiated "Link a shortfall to make-up day(s)" form) was NOT extended to target a leave debt in this pass — only the employee self-service `/leave/match-request` flow can, matching exactly where Ganesh described the workflow living ("employee... requests to match the hours... later in the month"). Flagged here in case an admin ever needs to initiate one on an employee's behalf.

  No `app/validation.py` changes; `app/engine.py` did change (`leave_balance_v2()`, `allocate_surplus_minutes()`, `evaluate_link()`, plus the new `leave_debt_allocated_minutes()`), so the pytest+verify_strikes hard rule applies — same missing-legacy-bundle/no-pytest sandbox gap as every other `engine.py` change this session. Verified via `py_compile` across every touched `.py` file; a standalone plain-Python reimplementation (`sim_deferred_leave_comp.py`, no sqlalchemy — dicts instead of an ORM session) of the allocation/double-spend logic, run against Ganesh's own worked 8h/2h/6h example and the double-spend guard scenario — 6/6 checks passed; 9 new pytest tests added to `tests/test_leave_v2.py` (5 in `TestLeaveBalanceV2` covering each `compensation_status` value's effect on Used, plus a new `TestDeferredCompensationMatch` class with 4 tests covering the exact worked example, the double-spend guard, and `leave_debt_allocated_minutes()`'s own aggregation) — still needs a real `pytest` run to confirm, not just `py_compile`/the standalone simulation; and a Jinja render+HTML-balance harness (bare `Environment`, hand-mirrored `hm`/`hm_signed`/`mdy` filters and the real extracted `approval_progress_steps()` global, same technique this project's history already established for every template-only verification) rendering `leave.html`/`overtime.html`/`admin/leave.html` against fixture `LeaveRecord`/`CompensationLink` objects covering all 4 compensation states plus an overdue-vs-not-due-yet admin row — all three templates rendered correctly with every expected string present and zero unclosed/mismatched HTML tags.

- **Unplanned (Sick) Time's entitlement switched from a flat full-year pool to monthly proration (Ganesh, 2026-09-04, same day as the deferred-compensation feature above)** — reported live: "everyone will have 40 hours of Unplanned Time from now until Jan 1" when it's only September, i.e. the flat-pool design from the 2026-08-27 policy clarification (`entitlement[m.LEAVE_UNPLANNED] = cfg_int(cfg, "unplanned_hours_year_cap") * 60`, the full year's amount available Jan 1) was showing everyone the complete 40 hours with a third of the year still to go. Confirmed via AskUserQuestion (3 questions) before touching `engine.py`, since this directly reverses a decision that was itself an explicit management clarification just a week earlier: (1) yes, prorate it monthly, reversing Aug 27; (2) basis = calendar-year elapsed (1/12 of the annual cap per full completed calendar month since Jan 1), not tied to Planned Time's own hire-date-based accrual; (3) forward-only — no retroactive recompute of leave already approved/used earlier this year against the old flat entitlement.

  New pure function `engine.unplanned_time_prorated_entitlement_minutes(cfg, as_of, hire_date=None)` reuses `full_months_elapsed()` (already battle-tested via Planned Time's own accrual) rather than inventing new date math: `months = full_months_elapsed(max(Jan 1 of as_of's year, hire_date), as_of)`, `entitlement = round(cap_minutes * months / 12)`. `leave_balance_v2()`'s one line changed from the flat `cfg_int(...) * 60` to `unplanned_time_prorated_entitlement_minutes(cfg, as_of, emp.start_date)` — everything else in that function (used/pending loop, the deferred-compensation skip from the bullet above, the calendar-year-reset scoping) is untouched.

  **The hire-date floor is a deliberate addition beyond what was literally asked**, flagged here rather than silently added: without it, an employee hired in November would show up with ~10/12 of the annual Sick pool already "earned" on their very first day, purely because 10 calendar months had elapsed since Jan 1 org-wide — an obvious loophole a pure calendar-only calculation would have. `hire_date` floors the count at the employee's own `start_date` (not `start_date + probation_days`, since Unplanned Time — unlike Planned Time — stays available *during* probation per `LEAVE_TYPES_NO_PROBATION_BLOCK`). A long-tenured employee's `start_date` is always earlier than the current year's Jan 1, so this floor is a no-op for them and the calculation is purely calendar-based, matching what was actually asked.

  **Known, accepted consequence, documented in the function's own docstring, not silently hidden**: entitlement is 0 for every day of the year's (or a new hire's own) first calendar month except its very last day — `full_months_elapsed()`'s own "a month counts once its last day has passed" rule means Jan 30 reads 0 but Jan 31 jumps straight to a full month's worth (200 minutes at the default 40h/year cap), the same boundary Planned Time's accrual already has for a hire on the 1st. This is the real tradeoff of prorating against the original flat pool's "sick time can happen any day, especially right after joining" reasoning — worth revisiting if a January or first-week-of-employment sick day proves this too strict in practice.

  No `app/validation.py`/`legacy/import_legacy.py` changes; `app/engine.py` did change (`leave_balance_v2()`, new `unplanned_time_prorated_entitlement_minutes()`), so the pytest+verify_strikes hard rule applies — same missing-legacy-bundle/no-pytest sandbox gap as every other `engine.py` change this session. Verified via `py_compile`; a standalone extraction of the real `unplanned_time_prorated_entitlement_minutes()`/`full_months_elapsed()`/`cfg_int()` source (not hand-copied) executed against 11 worked cases (zero on Jan 1, zero through Jan 30, the Jan-30-to-Jan-31 boundary jump, 5 months by Jun 15 matching the exact "it's September" scenario Ganesh reported, 11 vs. 12 months at year-end, a new hire's own start-date floor, a long-tenured hire correctly behaving identically to no-floor-at-all, a mid-month hire skipping its own partial first month, and a non-default configured cap) — all 11 passed, including one self-caught correction where my own first-draft test asserted 0 on Jan 31 (wrong — the real, intentional `full_months_elapsed()` semantics give 1 full month's credit exactly on a month's last day, matching Planned Time's own precedent) before the docstring/test were fixed to describe the actual boundary correctly. One existing test (`test_unplanned_type_has_no_capped_entitlement`, already stale since the 2026-08-27 cap change gave it a real non-None entitlement) was renamed and rewritten to assert the new prorated figure instead of `None`; a new `TestUnplannedTimeProration` class (9 tests) was added to `tests/test_leave_v2.py` covering the same 11 scenarios — still needs a real `pytest` run to confirm, not just `py_compile`/the standalone extraction.

## Layout

- `app/util.py` — formatting/audit helpers shared by routes and templates; also the one place `now_local()`/`today_local()`/`BUSINESS_TZ` live (see Hard rules above)
- `app/engine.py` — all business math (statuses, variance, strikes, recompute, ledger)
- `app/validation.py` — PRD §4 entry rules (overlaps block, gaps flag, 4h cap, backdate window)
- `app/routes/` — auth (dev login), employee (Today/My Month/Leave/Overtime/Holidays/Support/Profile — Overtime includes the V2 self-service Missed-Hours-match-request flow behind `LEAVE_MANAGEMENT_V2_ENABLED` (moved here from Leave 2026-08-22; its `POST /leave/match-request` route path is unchanged, only the page it's requested from moved); Today includes Task Planning's `/plan/*` routes, no flag), admin (dashboard/roster/lists/leave/overtime/holidays/config/audit/support — leave includes the Special Paid Time grant behind the same flag; overtime includes the compensation-match approve/reject decision card, also moved from leave 2026-08-22), tickets (Ticketing System — raise/list/detail/comment/status-change), exports (XLSX/CSV)
- `app/holiday_bulk_upload.py` — Excel upload for Holiday rows (name/date only — one shared calendar), same shape as `app/leave_bulk_upload.py`
- `app/auth.py` — the Entra ID swap point; session/role gating stays
- `legacy/` — streaming ODS reader (survives the 700 MB file), extractor, importer, strike verifier
- `legacy/cache/import_report.json` — every oddity the import tolerated
